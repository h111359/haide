# Build AIH: a portable, file-based AI product-development harness

Prompt version: 20260922_173409Z
Generated (UTC): 2026-09-22 17:34:09Z
Supersedes: AIH_Build_Prompt_v20260922_134714Z.md
Revision basis: confirmed multi-folder product workspace and option A—human workspace configuration changes while execution is idle, including during an open request, with affected scope and evidence reassessed. Retains previously agreed requirements.

Act as a senior software architect and implementation engineer. Build the complete framework described below, including working Python infrastructure, agent instructions, conventions, portable skills, a local web portal, tests, and operating documentation.

Implement and verify the solution; do not deliver only a proposal, scaffolding, or a reduced first version. All requirements are in scope. Work in a sensible implementation sequence without asking me to prioritize features. Resolve routine engineering details yourself. Ask only about consequential ambiguities that cannot be resolved through inspection or this specification.

First inspect the available workspace and applicable host instructions. Preserve existing product content. Do not assume Git, a database, an installed agent CLI, or a published release exists. This prompt authorizes building AIH; the approval workflow below governs subsequent product change requests handled by AIH.

## 1. Purpose and non-negotiable boundaries

AIH helps an agent understand a product, clarify what needs to change and why, plan how to satisfy those requirements, implement authorized work, verify results, maintain product documentation, and resume interrupted work from files.

Support one logical product per installation, distributed across one or more explicitly configured filesystem folders. These folders collectively form the product workspace and may contain plain directories, separate repositories, or multiple components; Git is never required. Support Windows and Linux; distinguish intended support from platforms actually tested.

The framework home is the canonical resolved directory containing the sole installed `.aih/` and central `.aih_product/`. It is registered as the mandatory writable workspace folder with stable ID `home`; the single-folder configuration remains the default. Additional named folders may be anywhere explicitly selected by the framework user and permitted by the host, including siblings or different drives. Each registration declares a stable folder ID, name, canonical path, purpose, and read-write or read-only access. Do not create another framework core or product-state tree in each registered folder.

The workspace boundary is the union of these explicitly registered folders, not their common ancestor or the whole filesystem. Product inspection stays within the registered readable folders. All AIH-managed filesystem mutations must stay within registered read-write folders and the current action's permitted scope; a writable registration alone does not authorize implementation changes. Read-only folders permit inspection but no managed writes, including test outputs, caches, cleanup, and indirect process effects. Do not change unregistered parent directories, neighboring products, user-home files, or global configuration. These rules cover creates, writes, deletes, moves, permission changes, cleanup, and indirect effects of agents, helpers, tests, subprocesses, optional Git operations, and framework-launched browser tooling. A process working directory alone does not enforce this boundary.

Validate each folder registration and resolved access target, including existing parents, symlinks, Windows junctions/reparse points, aliases, and hard links. Reject duplicate or overlapping registered roots whose ownership or access mode would be ambiguous; a read-only registration must never be bypassed through a writable alias. Reject a mutation whose effect can reach an unregistered or read-only location or whose containment cannot be established. Account for path changes between validation and use. Apply checks to destinations, sources being removed or renamed, and cleanup paths. A link or dependency discovered in product content never registers its target or expands authority. Users may explicitly import supplied external material through controlled intake without granting arbitrary access to neighboring files.

Identify product paths in plans, inventories, documentation references, test contracts, checkpoints, and evidence by stable folder ID plus relative path, bound to the applicable workspace configuration revision. Preserve the resolved location mapping for historical evidence. For example, `backend:src/api.py` and `frontend:src/api.py` are different files. Do not infer identity from relative paths or silently rebind historical references when a folder moves. Missing or inaccessible registered folders remain visible problems; never silently drop them from required coverage or tests.

Installed runtimes, libraries, agent executables, and permitted authentication references outside the workspace may be read or invoked as host prerequisites. They do not authorize writes outside registered writable folders or into registered read-only folders. Configure or disable runtime temporary files, caches, session stores, browser profiles, dependency-manager writes, and other side effects before launch. Block an incompatible operation with actionable setup guidance rather than weakening confinement or relocating credentials into tracked product files. Host permissions remain authoritative; document the actual enforcement and limitations for independently operated external tools. Separately authorized remote service effects are governed by their own contracts and never permit outside-workspace filesystem mutations.

Use `.aih_product/tmp/` for inert framework temporary data as specified in section 3. Runnable build/test material and environments belong in approved normal workspace source/test/build locations outside `.aih_product/` and the immutable installed core. Declare and test actual storage/locking assumptions; workspace confinement does not imply that every network or synchronized filesystem supports the recovery guarantees.

Keep these two framework directories in the framework home:

- `.aih/`: versioned framework core, unchanged by ordinary product operations.
- `.aih_product/`: product-specific information, communication, history, documentation, and state.

Use `.aih_product/` consistently in generated files, commands, instructions, schemas, and documentation. Paths beginning with `.aih/` or `.aih_product/` are anchored to the framework home, independently of the current product folder or subprocess working directory. Do not create alternative product-state roots.

All framework behavior, deterministic helpers, adapters, workers, and backend components must be Python. The only non-Python harness-script exception is the pair of optional `.aih/menu.cmd` and `.aih/menu.sh` launchers, limited to locating/invoking the shared Python menu and reporting minimal runtime/startup errors. They must contain no workflow, state, agent-execution, or other framework business logic. Browser HTML, CSS, and JavaScript are permitted for the portal interface. Product implementation languages are unrestricted. Direct Python operation must remain fully supported without the launch wrappers; do not require Node or PowerShell to operate the harness.

Markdown and validated YAML/JSON files are authoritative. Do not use a database, including SQLite, an embedded database, or a database cache. Optional search indexes must be disposable files or in-memory structures.

Do not require or initialize Git by default. Installation, history, diffs, revision checks, verification, and recovery must work without Git. Provide Git integration as an optional, configurable skill extension.

All registered folders share one product definition, documentation tree, required test inventory, and execution owner. Only one change request may be open at a time. There is no backlog, queued future request, or concurrent change-request execution. Closed requests remain in separate historical folders with a metadata catalog and short summaries, loaded only when relevant. A separate read-only question channel is allowed. Initial bootstrap and explicitly invoked documentation-only maintenance are scoped system operations as specified below; they do not authorize product implementation changes.

Across this installation, allow only one operational action at a time, including change work, questions, setup, maintenance, test execution, and manual external handoffs. While an action is starting, running, stopping, awaiting confirmed termination, or holding an external handoff reservation, Stop is the only enabled operational control. Reject other commands at the shared backend; do not save, import, submit, approve, change settings, close requests, or queue replacement actions while busy. Passive navigation, help, viewing existing artifacts, downloads of existing artifacts, and status/log observation remain available. Internal authorized substeps of the current action may continue; they are not competing user actions.

Only after the action completes or Stop finishes safely and ownership is reconciled may the user choose another action. A blocked request with no executing owner is idle but remains open. At that point permitted drafts and questions are available without authorizing unrelated change work. External direct edits cannot be universally prevented: treat input edits as unsubmitted drafts for the next explicit idle command, and reconcile changed source or instructions before further reliance. No automatic dispatch follows Stop and there is no pending-command queue. The narrowly required external-stop confirmation and evidence reconciliation belong to the Stop/release flow itself.

## 2. Core organization

Organize `.aih/` around these immediate directories:

- `prompts/`: shared behavioral instructions, loaded when needed.
- `conventions/`: authoritative formats, schemas, structures, naming rules, templates, and validation contracts.
- `skills/`: one directory per portable skill, the generated metadata-only `catalog.yaml`, and the human-readable `README.md` catalog. Deliver the eight initial skills specified in section 13.
- `engine/`: all shared Python infrastructure, including the CLI, state helpers, adapters, workers, portal, tests, and supporting interface assets.

Place the single agent entry point at `.aih/run.md`. Include `.aih/README.md` for quick-start knowledge, `.aih/USER_GUIDE.md` as the maintained detailed user guide, and the optional `.aih/menu.cmd` and `.aih/menu.sh` launchers as core root files. Core version and integrity manifests may also be root files. Keep menu functionality and shared Python infrastructure under `engine/`; do not introduce additional immediate core directories for these features.

Prompts specify behavior. They reference conventions instead of redefining formats, schemas, directory structures, or templates. Conventions must not reference prompts or depend on which prompt consumes them. Engine validators implement the authoritative contracts without independently maintained conflicting schema definitions.

Keep `run.md` concise and stable. Do not hard-code a skill roster or profile names into it. It must route execution using submitted actions, validated product state, applicable instructions, discovered skill metadata, and the active request or identified system operation.

Ordinary runs must not modify `.aih/`, including generated catalogs, bytecode caches, logs, temporary files, or bundled skill resources. Route durable runtime material into its owning product communication/request/system-operation record and inert temporary data into `.aih_product/tmp/`. Disable Python bytecode generation for core and bundled skill execution; do not redirect executable bytecode into the information-only product-state tree. All other runtime effects must satisfy the workspace boundary in section 1.

Core installation, explicit maintenance, and upgrades are separate operations. Core repair, catalog/resource regeneration, and upgrades require no open request and no other execution owner, including setup, Q&A, maintenance, and external handoffs. Ready-to-close, paused, and blocked requests still count as open. A request blocked by a broken helper may therefore need explicit cancellation or rejection before repair; do not invent an in-request core-maintenance exception. Preserve the incomplete request and current-state notice before releasing its slot.

### Deterministic operations belong to Python scripts

Provide named Python entry points and shared helper APIs for every deterministic framework operation. Agents must invoke these implementations instead of reimplementing their functionality through ad hoc commands, generated scripts, manual state edits, or repeated model reasoning. The portal and agent routes must use the same implementations and validation contracts.

Cover initialization, workspace folder registration and revision history, permission-aware containment, folder-qualified path resolution, scope-impact checks and temporary-data management, safe file inventories and supported fact extraction, content hashing/diffs, schema validation, sensitive-input screening and authorized historical redaction, submission snapshots, questionnaire parsing, requestor-form generation and answer-import matching/review, amendment revision capture, revision checks, single-action ownership and manual-handoff release, state transitions, locking and transactions, validated edit application, catalog/index/link maintenance, task status updates, test-contract validation and registered test execution/result capture, repair-budget tracking, logs/checkpoints, explicit request archival, diagnostics, and upgrade mechanics. Product tools and test runtimes may retain their own languages; harness orchestration and bookkeeping remain Python. The optional OS menu launchers are limited to the bootstrap exception specified above.

Scripts collect and validate facts and apply specified operations. Agents interpret evidence, resolve ambiguity, choose authorized solutions, produce semantic content/patches, and diagnose failures. Do not turn extraction heuristics into asserted business requirements or expect a deterministic validator to prove semantic correctness.

Expose a compact metadata/help catalog with supported operation IDs, inputs, effects, required permissions, and structured result contracts. Return concise status, changed artifact references, actionable errors, and conflict details; keep verbose evidence available by reference. Use typed, validated operations rather than generic command templates or arbitrary-command endpoints. Agents should not need to read implementation source to invoke routine operations.

Missing or broken helper functionality must produce an explicit framework-gap diagnosis. Do not silently bypass its validation/transaction behavior or modify the immutable core during ordinary product work. Changes to helpers use the explicit core-maintenance workflow. Preserve direct human editing of permitted product files and detect/validate such edits; do not claim scripts enforce restrictions against external actors with filesystem access.

### Runtime token efficiency

Minimize avoidable model token consumption while preserving the full required analysis, documentation, tests, evidence, and instruction compliance. Keep orchestration instructions compact; delegate repeatable extraction, comparison, validation, and bookkeeping to Python.

Read metadata and relevant documentation branches before content leaves, load skill bodies/resources only on demand, and use source fingerprints and dependency maps to select affected material. Reuse still-valid analysis, compact checkpoints, and source-grounded summaries instead of repeatedly regenerating them. Summaries must retain source/version references, consequential decisions, unresolved issues, and recovery context. Invalidate affected reuse when source content, applicable instructions, workspace folder membership/locations/access, extraction logic, or schema/core versions change. Scope cached observations by folder identity and relevant configuration revision so similarly named files in different folders never collide.

Send compact operation and test summaries plus relevant failure details to the agent; retain complete available sanitized evidence in files for targeted inspection. Paginate or select verbose outputs and label omissions instead of silently truncating necessary context. Keep historical requests out of routine context unless a concrete question requires them. Persist any reusable indexes/summaries as validated files or in memory, never a database or a write inside the immutable core.

An unchanged, already documented input set should not require another semantic generation call merely to repeat validation. Initial baselining may require substantial reading; incremental refreshes should target affected sources and topics. Do not achieve token savings by skipping required tests, omitting required coverage, using stale assumptions, changing the selected agent silently, or discarding necessary instructions.

Record actual token usage by operation/run/segment when the adapter supplies it, and expose useful totals in diagnostics/portal status. Report unavailable usage as unavailable; do not invent counts or represent missing telemetry as zero. Distinguish measured token savings from proxy measurements such as model calls or context bytes.

## 3. Product information organization

The framework home contains these central files and immediate directories for the whole product workspace:

- `.aih_product/config.yaml`
- `.aih_product/state.yaml`
- `.aih_product/input/`
- `.aih_product/output/`
- `.aih_product/change_requests/`
- `.aih_product/documentation/`
- `.aih_product/ledger/`
- `.aih_product/instructions/`
- `.aih_product/tmp/`

Do not add separate top-level memory, checkpoint, execution, communication, or skills directories. Immediate directory names must not overlap between `.aih/` and `.aih_product/`.

Keep `.aih_product/` non-executable: information and validated state only, including `tmp/`. Do not place executable scripts, plugins, hooks, application code, installed packages, virtual environments, bytecode, or runnable test fixtures there. Treat examples and accepted attachments as inert data; never import or execute them. Actual deployment scripts, infrastructure code, tests, runnable fixtures, build outputs, environments, and product source belong in approved normal product locations within the workspace and outside this folder. Core portal assets belong in `.aih/engine/`.

Use `tmp/` for disposable inert framework data in uniquely identified operation-owned subdirectories. Apply the same containment, sensitive-data, and permission rules as other storage. Inert tool caches may use it only if they do not contain executable material. Exclude temporary data from semantic product inventories, documentation baselines, and product-content fingerprints. Keep durable journals, checkpoints, ownership reservations, authorization, and essential evidence in their owning records, never solely in `tmp/`. Cleanup must be recoverable and ownership-aware, preserve data needed by active or uncertain operations, and never follow a path escape. A restart may recover or remove abandoned temporary data only after ownership is reconciled.

Configuration holds supported declarative settings, including the registered workspace folders. State holds current execution references, effective workspace revision, reconciliation status, and other workflow status. Keep detailed history in its owning records rather than continually enlarging global state.

### Workspace configuration and changes

Define the workspace registry schema in `.aih/conventions/` and store its product-specific values in `.aih_product/config.yaml`. Include the stable product/workspace identity, mandatory `home` entry, other stable folder IDs, display names, canonical locations, purposes, access modes, and configuration revision. Keep observed accessibility, baseline freshness, and diagnostics distinct from human-selected permissions. Folder IDs remain stable across deliberate relocation and are never reassigned to unrelated content. The framework home cannot be removed, changed to read-only, or relocated through an ordinary additional-folder edit; such an edit must not split or orphan central state.

Only the framework user may add, remove, relocate, or change access for workspace folders, through portal administration or equivalent explicit local configuration commands. Agents may report a missing dependency and suggest a folder but must not register it or expand their own permissions. Validate human file-based edits through the same administrative contract; file contents or claimed identity alone do not establish authorization. Existing host permissions remain authoritative.

Allow these product configuration changes whenever execution is idle, including while a change request is open, blocked, or Ready to close. Reject them while any action is starting, running, stopping, uncertain, or reserved for external execution. This is product-scope administration, not core maintenance; it does not require closing the active request. Do not weaken the separate no-open-request rule for core maintenance or upgrades.

The save transaction records the before/after registry revision and the explicit human change, checks path/access conflicts, and marks affected analysis, plan authorization, test evidence, documentation, and baseline coverage for reassessment. Preserve unaffected evidence with a recorded justification; unknown impact is unresolved, not automatically harmless. Withdraw affected Ready to close status. Saving configuration never starts an agent, runs tests, moves product files, or expands the approved business scope. The next explicit applicable action reconciles the workspace change against the current request and its authorization before implementation can proceed.

New or relocated product areas require inventory and documentation baselining before implementation relies on them. Use the explicit authorized reverse-engineering/documentation workflow in section 9. With an open request, keep its scope-impact findings, documentation increment, and evidence in that request; with no request, use the existing setup or documentation-maintenance operation. Missing baseline coverage prevents opening a new request. Reassess affected requirements and regenerate or reapprove affected plans where necessary; do not demand a new approval for unrelated still-valid work.

Removing a folder removes membership only; it never deletes its files, request records, documentation history, or evidence. Access revocation takes effect for subsequent actions, including recovery and cleanup. Retain unresolved references and required tests until their scope is explicitly reconciled; removing or making a folder read-only cannot convert a failed or unexecuted required check into a pass. A location edit changes the registered mapping without moving files and requires validation of the new location and content. Interrupted operations referring to a removed or relocated folder must not continue writing through stale paths or resurrect the old registration automatically.

Direct configuration edits discovered during an action do not change its recorded permissions live. Detect the mismatch, stop safely if authority or containment is uncertain, and reconcile through the explicit idle administration path. Keep workspace revisions and disposition events in the existing ledger and owning request/system-operation records rather than adding another immediate product directory.

### Request and operation records

Each change request has a stable identifier and its own central directory covering all affected workspace folders. Define conventions for its request metadata, submitted input revisions and attachments, evolving requirements definition, clarification rounds, questionnaires and requestor exchange records, amendment drafts and immutable revision histories, separate solution analysis, approved implementation plan and tasks, response history, execution records, checkpoints, verification evidence, documentation impact, and closure outcome. Create artifacts when needed; avoid empty folder and template proliferation.

Store the sole open request under `change_requests/active/<request-id>/`. On closure, move its complete record to `change_requests/history/<request-id>/` through a recoverable transaction. Maintain a metadata catalog under `change_requests/` with each request's stable ID, short scope/outcome summary, status, dates, location, and significant documentation/decision references. Completed, cancelled, and rejected outcomes remain distinguishable. Resolve links through stable IDs so archival does not break references.

Normal runs consult current product documentation, its persistent current-state notices, and the active request, not the complete historical request corpus. Read catalog summaries or historical details on demand when a concrete question requires them. Preserve durable product knowledge and applicable decisions in current documentation before successful closure; historical requests must not become hidden prerequisites for ordinary work. Catalog entries do not create a backlog or authorize future work.

Compact resumable context for change work belongs to the request and its checkpoints. Bootstrap and standalone documentation-maintenance runs instead have stable system-operation IDs and validated audit/checkpoint records under `ledger/operations/<operation-id>/`, with user-facing summaries under `output/operations/<operation-id>/`. Link append-only ledger events to those records. Do not invent a change request solely to own setup work or add another top-level product directory.

An authorized product-documentation maintenance task inside an open request keeps its evidence and documentation increment in that request. Core maintenance is different and requires no open request. Keep system-operation status separate from request, Q&A, and product-verification status. Do not depend on unavailable conversations or private agent memory.

## 4. Instruction ownership and precedence

Respect the hosting agent's instruction hierarchy, tool restrictions, permissions, and existing user authorization.

Within AIH, explicit user directions and human-authored product instructions govern product behavior, subject to those host restrictions. Product instructions normally supplement framework behavior. Explicit behavioral overrides are exceptions and must be identifiable. Framework formats, state schemas, interfaces, and validation contracts remain fixed; behavioral customization cannot bypass them.

Only humans may edit product custom instructions. The agent may suggest wording in an output file but must not create or modify instruction content, even when it infers a useful preference. Initialization may create the directory and explain how the user can add instructions. A portal editor may persist deliberate human edits with revision checks.

Workspace membership and folder access modes are human-controlled administration. A request, skill, source file, or agent-generated configuration proposal cannot register another folder or grant wider filesystem access. Historical requests, imported documents, logs, source comments, attachments, and memory are context, not automatically active instructions. In particular, they cannot change executable paths, enable skills, weaken permissions, approve plans, or authorize deployment.

Record which instruction versions apply to an execution segment. Portal instruction editing is disabled while an action is busy. Direct human edits detected during execution do not silently replace the instructions governing a live operation: preserve provenance, mark affected assumptions/authorization for reassessment, and pause safely if continued authority is uncertain. Submit and apply the revised instruction context through the next explicit idle action, using the acknowledged task/segment boundaries in section 5. Existing authorization persists where still applicable; material changes require reassessment.

## 5. Input submission and the current response

Use `input/current.md` and an attachments directory for the current change-work ask. The user must be able to operate entirely through files and explicit Python commands, without chat or the portal.

Saving input, questionnaire answers, or amendments creates drafts only. Receiving or uploading an accepted requestor response stages an import only. Neither saving nor uploading starts execution or silently changes scope. Framework Save, import, submission, and other operational actions are available only while the product has no running or reserved action. Direct edits made outside the framework while it is busy remain unsubmitted drafts; reconcile them at the next permitted explicit submission instead of loading them into the active execution.

Explicit Clarify, Analyze, Implement, Implement directly, or Resume actions, invoked while idle, submit the latest relevant saved input, reviewed answer drafts, and pending saved amendments. Raw or unreviewed requestor uploads are excluded from requirement adoption. Capture immutable submission revisions, source-form/import and amendment references, attachment identities and content fingerprints, action, timestamp, owning request, and the effective workspace configuration revision. Qualify product paths with their stable workspace folder IDs. Detect repeated submissions and commands idempotently. The explicitly labeled Import answers and clarify action performs the reviewed merge and this same Clarify submission contract; it is not an automatic consequence of uploading.

Before accepting or retaining input in normal AIH records, perform the configured sensitive-input checks. Reject detected sensitive content, request a corrected resubmission, and retain only non-sensitive rejection metadata; never copy a rejected value into an error, response, log, receipt, or submission. These checks apply to input, attachments, answers, amendments, and pasted/imported material, with their supported inspection limits stated. For accepted input, preserve the previous submitted version before reusing it. Do not overwrite newer user edits while snapshotting or clearing drafts. Validate partial or contradictory questionnaire edits and explain required corrections. Historical preservation is subject only to the explicitly human-authorized sensitive-value redaction exception specified in the evidence rules.

Accept no replacement command or new submission while an action is starting, running, stopping, awaiting confirmed termination, or reserved for manual external execution. Do not queue it for later adoption. Stop is the only enabled operational control; passive navigation, status/log viewing, and viewing or downloading existing artifacts remain available. Enforce the same rule in the CLI, API, and helpers, not just the portal. A duplicate invocation of the same accepted operation may return its existing identity idempotently, but cannot create another run. After the action finishes, or Stop finishes safely and ownership is released, the user may explicitly select the next action. For agent execution, ordinary adoption or release boundaries are the completion of an entire plan task or agent segment, with an acknowledged, recorded handoff. Stop may safely terminate an incomplete task/segment: preserve partial work and evidence, mark it unfinished, confirm child-process termination, and do not falsely mark a task complete. Newly submitted edits are reconciled by the subsequent explicit action against that persisted boundary; no automatic command chaining follows completion or Stop.

Scope changes invalidate affected plan approval, verification, and documentation freshness. Cosmetic changes need not invalidate unrelated evidence; record the determination.

Use `output/current.md` as the single current response for change work. Before replacing it, preserve its previous version in the owning change request. Include request, submission, and run references, outcome, blockers/questions, and next action. Additional evidence belongs to the request and can be linked from this response. Requestor questionnaires are separate downloadable plain-text exchange artifacts stored with their owning request; link them from the current response and portal without creating another competing current response.

## 6. Independent read-only questions

Provide a separate communication lane for questions, explanations, and investigation that do not modify the product. Its existing questions and answers remain readable at all times. A new Ask action is available when no other operational action is running or reserved, including when no request exists, setup is pending but idle, or the sole request is open or blocked with its worker stopped. Separate communication does not permit concurrent execution.

Use `input/questions/current.md` as its draft entry point, with stable submitted question records under `input/questions/`. Submit through an explicit Ask action under the product-wide action reservation; reject Ask while busy and do not queue it. Store answers under `output/questions/<question-id>/`, as Markdown and, when useful, JSON or other inert files. Provide a compact navigation index. These files must not overwrite the current change-work response.

Questions do not create change requests, occupy the active-request slot, change request approval, or resume blocked work. They may refer to an existing request.

Read-only means no changes to product source, durable product documentation, product configuration, custom instructions, or change-work state. The harness may persist the question, answer, permitted attachments, and its own communication/execution metadata.

Use read-only agent permissions where supported and have the trusted worker write communication outputs. If read-only enforcement is unavailable, report the limitation and offer a constrained or manual route instead of claiming protection through prompting alone.

Answers identify sources by workspace folder ID and relative path, relevant observed content and workspace versions, and uncertainty. Read-only question execution may inspect registered readable folders but writes its answers only to the central permitted communication area. Managed implementation and Ask cannot run concurrently. If external edits nevertheless change inspected files, detect and report the changed or mixed observation, or repeat the affected inspection within the same permitted action, instead of presenting it as a consistent verified snapshot. Do not modify the product to demonstrate an answer. Apply the same sensitive-input admission rules to questions and attachments before normal communication records are retained.

If a question requests a modification, explain that it requires an explicit change-work action. Never silently convert it into a second request. A question answer does not itself authorize implementation or resolve a blocking request questionnaire without explicit submission to that request.

## 7. Change-request lifecycle and authorization

Implement explicit stages:

1. Open the sole new request through explicit submission only after the documentation baseline covers the current configured workspace. Setup, independent questions, and local draft preparation may precede that gate while idle; they do not open a request. An already-open request may receive explicit amendments and blocker-resolving clarification while idle even if a workspace change has made baseline coverage incomplete; implementation remains subject to workspace and documentation reconciliation.
2. Invoke Clarify iteratively to establish what needs to change and why: the problem, intended outcomes, affected users, required behavior, scope, constraints, and acceptance criteria. Apply reviewed user/requestor answers and submitted amendments while preserving the evolving requirements definition and its history.
3. Invoke Analyze iteratively to assess how to satisfy the clarified requirements, apply submitted answers and amendments, resolve questions, and generate or revise a sequential task plan with tests and documentation work. Planning is an activity inside Analysis; there is no separate Plan command.
4. Obtain human approval of the identified plan and scope revision.
5. Start implementation only when explicitly requested.
6. Handle submitted modifications within the still-open request.
7. Verify, pass required tests, update documentation, and assemble evidence.
8. When every completion condition is satisfied, mark the request Ready to close. The framework user explicitly invokes Close successfully; revalidate the gates, then archive it and release the active-request slot.

Clarification owns requirements understanding, not implementation design. Its deliverable records the problem and rationale, stakeholders/users, desired outcomes, current versus required behavior, in-scope and out-of-scope work, functional and nonfunctional requirements, explicit constraints, acceptance criteria, assumptions, and unresolved questions. Acceptance criteria express observable outcomes and conditions for success, not a prematurely selected implementation or test framework.

During clarification, the agent may perform read-only inspection of existing code, documentation, interfaces, and other available evidence to understand current behavior, identify dependencies or feasibility concerns, and discover missing requirements. Record observations and contradictions without choosing a solution, changing product implementation, or committing to architecture, technologies, algorithms, component changes, implementation tasks, or test tooling. Clarification may write its requirements, questions, evidence references, responses, and workflow records. It may also perform narrowly factual known-defect and documentation-freshness bookkeeping: add or update an evidence-backed known-defect entry, or mark an affected documentation topic stale, with source and request references. This exception does not authorize repairs, redesign, substantive business-documentation rewriting, or recording inferred intent as an approved requirement.

Technical constraints explicitly supplied by the user remain requirements and retain their provenance. Do not discard them merely because they concern technology, and do not infer that an existing implementation choice is a required constraint. For example, "must work offline" belongs in clarification; selecting a local storage technology belongs in planning unless the user has explicitly required that technology.

Analysis owns the implementation approach and includes planning: solution design, architecture and technology choices, affected components, implementation tasks, and the verification strategy. Preserve traceability to the clarified requirements and distinguish design decisions from requirements. If planning exposes a missing requirement or consequential ambiguity, preserve the draft plan and return that question to clarification within the same request. Stop change work when the gap is consequential. Update the requirements from submitted answers, reassess affected plan approval and evidence, and resume planning from the clarified definition. Do not silently redefine requirements to suit a preferred solution.

Clarify must neither implement nor decide how the change will be implemented. Analyze must not implement. Approving a plan alone must not start implementation. Offer a clearly labeled combined Approve and implement action if useful; record both intentions within the one explicitly accepted operation. Each user-invoked action reserves the product until it finishes or safely stops. Reject all other operational actions during that reservation, including Save, import, Ask, approval, closure, settings changes, and another execution command; Stop is the only operational control. Passive reading, navigation, log/status observation, and download of existing artifacts remain available. The current action may perform its own already-authorized internal steps, including sequential implementation tasks, tests, documentation, and evidence recording. Finishing one action never silently starts a different command.

The explicit Implement directly action skips a separate user-invoked Analyze command and separate plan approval, but never skips the task plan. Before changing product implementation, perform the necessary analysis internally and persist a sequential plan containing implementation, test creation/execution, documentation, and evidence tasks. Record direct implementation authorization bound to the submitted scope and resulting plan, not a fictional human approval. This route need not create every optional analysis file. It does not waive consequential clarification, user selection of unrelated defects to include, passing tests, documentation, execution logs, results summaries, or host approvals. Material scope expansion still requires explicit authorization.

Authorization applies to identified scope across the registered workspace folders. Human workspace configuration changes are permitted while idle even with this request open, but access registration does not amend business scope or authorize implementation. Reconcile affected requirements, analysis, plan approval, documentation, and verification before implementation continues. Preserve valid prior authorization; do not repeatedly request it. Material scope amendments require current authorization rather than inheriting an unrelated approval or shortcut.

Distinguish workflow phase, request lifecycle, worker execution status, and task outcome. A stopped or successful CLI process is not a completed request.

When a consequential blocker occurs, record the immediate evidence, relevant known-defect/documentation status, response, and checkpoint, then stop the entire change-request workflow safely. These necessary records include generating the requestor questionnaire needed to resolve a clarification blocker. The user may download existing forms while execution is stopping. Once the action has safely stopped and its ownership is released, the user may stage answers/amendments or explicitly invoke clarification to address that blocker. Those communication and clarification actions do not authorize repairs, unrelated tasks, automatic scope adoption, or implementation resumption. Do not continue independent implementation tasks or choose different product work. The portal remains readable, and the independent question lane can run when the product is idle. Optional unanswered questions may use clearly documented defaults.

Reject attempts to open another request while one is open. Do not silently close, replace, archive, or queue the active request.

Separate stopping an execution from closing a request. Stop leaves the request resumable and waits for confirmed safe termination or reconciled external ownership; it does not require completing the current task successfully or passing tests. Once idle, the user may explicitly close the request as cancelled or rejected, preserving partial changes, evidence, and unresolved outcomes. Do not automatically roll back files. Before releasing the active-request slot, persist a current-state notice accessible from current product documentation and the request catalog. It identifies retained changes, affected or stale documentation, unresolved defects, verification uncertainty, and archived evidence using stable references. The next authorized work must reconcile the affected knowledge before relying on it. Cancellation/rejection does not require a full documentation rewrite or passing the full test suite merely to close; the notice must truthfully preserve that unfinished state. These outcomes are not successful completion.

Successful completion requires implemented scope, passing required tests, satisfied verification and acceptance criteria, current affected documentation or a justified no-impact result, and recorded evidence. Skipped, failing, stale, or unexecuted required tests do not count as passed. Define genuinely inapplicable checks explicitly rather than inventing test passes. Passing all gates sets Ready to close, which remains an open request and continues to occupy the sole active-request slot. Do not automatically archive it. The explicit human Close successfully action rechecks gate validity against current relevant content before recording successful closure and archiving. Changed content or stale evidence withdraws readiness and explains the work required; it cannot be hidden by accepting an earlier ready state.

Integration and deployment are separate milestones. Do not require Git, merging, or production deployment to complete a request unless its explicit scope requires them. Subsequent changes after closure require a new request.

### Iterative Clarify command and amendment history

Provide one repeatable Clarify command, with `clarify` as its CLI operation. It handles the first interpretation, answers from the framework user or an external requestor, submitted amendments/additional information, changed human instructions, and contradictions discovered in later rounds. It resumes from persisted request state instead of restarting requirements work or relying on conversational memory.

The framework user operates AIH and submits work; the requestor supplies business needs and answers and may be a different person. Record the attributed requestor separately from the actual framework submitter. A name written in a returned form is attribution, not authenticated identity or implementation authority.

At each invocation, reconcile and snapshot the latest relevant saved input, reviewed answer drafts, pending saved amendments, and applicable instructions. Compute changes since the preceding round. Apply consistent submitted information to the one canonical `analysis/interpretation.md`, preserving its earlier revisions and source links. Update the authoritative questionnaires and their catalog. Identify conflicts, withdrawn assumptions, changed acceptance criteria, unanswered questions, and impacts on existing analysis/approval/evidence. Do not apply arbitrary last-answer-wins or last-file-wins rules; a clearly submitted superseding decision may resolve an earlier statement, while unresolved contradictions require clarification.

An unchanged invocation should report the valid current result and reuse existing artifacts without gratuitous model calls, duplicate questions, duplicate amendment application, or revision churn. Track invocation/submission attempts as needed for audit without creating artificial semantic changes. Meaningful new answers or amendments may produce another bounded question batch. Preserve completed answers unless the associated requirement/question changed; mark stale answers for review rather than silently discarding them.

Each round concludes with an updated interpretation and a concise account of applied changes, open questions, blockers, and next action. Generate the outstanding requestor questionnaire as specified below before pausing for those answers. Distinguish waiting for framework-user answers, waiting for requestor answers, unresolved conflicts/other blockers, and requirements ready for Analysis. Readiness describes the current evidence; it is not plan approval. Clarify never chooses implementation design, generates an implementation plan, invokes Analyze automatically, or starts implementation. When ready, offer the explicit Analyze action. Analysis may still apply submitted requirement answers or raise new requirement questions within the same request.

Invoke Clarify only after the preceding action completes or Stop has completed safely and ownership has been released. A busy invocation is rejected without committing a new submission or queuing Clarify. Normal handoffs occur between complete plan tasks or completed agent segments and record an acknowledgement; a safe Stop may instead leave an incomplete segment with explicit partial state. The next permitted Clarify action reconciles submitted information against that persisted state and performs requirements clarification only. Existing scoped authorization remains recorded, while further implementation requires its explicit applicable action. Resumption can continue a supported native session or start a clearly identified new segment from persisted evidence; the user should not need to reconstruct the conversation. Persist progress so interruption after receiving accepted answers, committing a submission, or updating interpretation can be reconciled without duplicate effects.

Provide an Amendments area within Current request > Input and clarification, readable throughout the open request, with an Add amendment action enabled only when idle. Each amendment has a stable ID, optional short title, plain-text/Markdown description of what changed or what new information is available, optional reason, source/attribution, creation and save timestamps, ordering, and links to affected requirements/questions or a superseded amendment when known. The framework user can change their mind, supply additional details, correct a previous statement, or withdraw an earlier proposed requirement. These are explicit request inputs; changing a form does not silently rewrite submitted history.

Keep amendment drafts and their histories inside the owning request, for example `amendments/<amendment-id>/draft.md` with immutable `revisions/<revision>.md` snapshots and a compact `amendments/catalog.yaml`. Define the exact schema and paths in conventions. Preserve every amendment revision explicitly saved through the framework and every submitted revision; unsaved keystrokes need not be archived. For direct edits in an external editor, capture the accepted changed version at the next permitted framework Save, intake, or submission action and retain the preceding captured version; do not claim that intermediate external saves never observed by the framework were recorded. Document the framework save operation for file-only users who need every revision captured. Show draft, saved-not-submitted, submitted, applied, superseded, withdrawn, and needs-clarification states with source/round references as applicable. Submitted corrections or withdrawals append a new revision/event rather than erasing previous content, except for the separately authorized historical sensitive-value redaction procedure. Readable catalog/status views are derived from those records and the existing ledger; do not create a competing requirements definition.

The next Clarify invocation takes all relevant saved pending amendments into account along with reviewed answers and the latest saved request input. Show this submission set before execution; saving or selecting an amendment alone does not apply it. Preserve earlier input snapshots and record the difference when `input/current.md` itself is edited. Link a direct input edit to an existing amendment where explicitly identified, or capture its provenance as an input revision change; do not silently lose it or manufacture duplicate amendments for the same submitted content.

Record how each submitted amendment affected the interpretation, which questions changed, which prior statement it superseded, and whether it remains unresolved. Preserve original wording alongside any agent-written summary. If a requestor answer implies a changed requirement, expose that implication in import review and link it to the answer or a reviewed amendment instead of silently expanding scope. A conflict between an amendment and a returned answer requires an explicit user resolution unless an already submitted supersession resolves it.

Material requirement changes invalidate affected analysis, plan approval, and verification under the existing rules; preserve valid unrelated evidence and the original decision history. Amendments remain part of the complete archived request after completed, cancelled, or rejected closure. Historical records are read-only for ordinary workflow operations and cannot be amended by importing a late response; the dedicated human-authorized sensitive-value redaction exception remains auditable. Subsequent product changes require a new request when the active-request rules allow it.

### Iterative Analysis command

Keep Clarify as the separate requirements-understanding command. Provide one repeatable Analyze command for the Analysis phase, with `analyze` as its CLI operation. Remove the standalone Plan action from CLI and portal workflows. The same Analyze command handles initial analysis, submitted questionnaire answers, submitted amendments/additional directions, updated human-authored product instructions, and plan generation or regeneration according to persisted state.

At each explicit invocation, snapshot the latest submitted input and answers, reconcile state, and identify changes since the previous analysis revision. Apply authorized amendments to the interpretation, review affected assumptions and design decisions, and update only impacted analysis artifacts. Human-owned instructions remain human-edited; applying directions does not authorize the agent to edit their files. Analyze is accepted only while idle, after any preceding execution has completed or safely stopped and its ownership is released. Reject busy invocations without submitting revisions or queuing another command; reconcile the acknowledged task/segment boundary or explicitly unfinished Stop checkpoint at the next permitted invocation.

When consequential information is missing, persist the analysis and questionnaire and pause. Requirement gaps return to clarification within the same request; implementation/design questions belong to Analysis. The next explicit Analyze invocation may apply submitted answers and continue the saved analysis; it must preserve the distinction between requirements understanding and design decisions. Do not require a different command merely to apply answered questions, and do not advance while consequential questions remain unanswered.

When requirements and necessary design decisions are sufficiently resolved, generate or revise the plan automatically and stop before implementation. Bind the plan to interpretation, submission, instruction, workspace configuration, and relevant documentation/content revisions. Repeated Analyze invocations with unchanged relevant inputs should report the current result without gratuitously rewriting plans or resetting progress. Retain previous revisions and invalidate only affected approval/evidence when changes are material. Never erase completed task history during regeneration; record which tasks remain valid and which require rework.

### Analysis records

Define these files under the owning request's `analysis/` directory. Their formats belong in core conventions, not in behavioral prompts:

- `interpretation.md`: the canonical current request definition, rewording and consolidating submitted input, answers, and amendments. Include what and why, stakeholders, behavior, scope, explicit constraints, acceptance criteria, assumptions, open questions, and source/revision references. Clarify establishes this file; Analysis refines it only from submitted directions and decisions. Do not maintain another competing requirements definition. Distinguish inferred/proposed requirements from accepted ones.
- `questions.md`: a catalog of stable question IDs and revisions, category, context, intended respondent, status, submitted answer summaries, and links to the authoritative Markdown questionnaire/answer revisions, requestor exchange records, clarification rounds, and relevant amendments. Preserve one editable source for each answer; this catalog must not become a second independent answer store. Distinguish draft answers from submitted answers.
- `solution_assessment.md`: an evidence-based architectural critique of feasibility, alternatives, tradeoffs, applicable practices, recommended approaches, and unresolved design choices. Consider relevant security, performance, data integrity, maintainability, dependencies, compatibility, deployment, migration, and recovery concerns. Distinguish observations from assumptions. Suggestions do not silently change requirements or authorize work.
- `unrelated_issues.md`: issues and defects discovered outside the current scope, with stable IDs, supporting evidence, affected areas, test impact, investigation status, user disposition, and links to the product known-defects catalog.
- `plan.md`: the versioned sequential implementation plan generated by Analysis, or generated internally by Implement directly before implementation changes.
- `documentation_increment.yaml`: the request's planned documentation changes and their subsequent application/verification evidence.

Keep the four interpretation, questions, assessment, and unrelated-issues records separate during normal Analysis. If no questions or unrelated issues are found, record that concise finding and its review scope rather than inventing content. Preserve revision history and relationships without duplicating full evidence in every file.

Use additional `impact_analysis.md`, `risks_and_decisions.md`, and `verification_strategy.md` files when substantive detail warrants them. For simple changes, keep concise equivalent sections in the assessment or plan. Cover current behavior, affected interfaces/components, dependencies, compatibility and operational consequences; risks, assumptions and decisions with rationale; and requirements-to-tests/verification mapping. Avoid empty files and generic best-practice boilerplate.

### Sequential plan and implementation contract

Each plan task has a stable ID, sequence, intended outcome, requirements/decision references, specific required changes, affected files and folders qualified by workspace folder ID and relative path, action types such as create/modify/move/delete, dependencies, and completion criteria/evidence. A task may affect several registered writable folders, but must state each affected component and any cross-folder dependencies. Inspection of read-only folders never authorizes changes there. Identify new paths as well as existing ones. If an affected path genuinely cannot be resolved yet, include a bounded investigation task and revise the plan from its evidence before performing the dependent change.

Execute implementation tasks sequentially in plan order. Record task states and outcomes; do not silently skip, reorder, or execute implementation tasks concurrently. Material changes to scope or approach require a revised plan and the applicable authorization. Preserve successful task evidence when valid and explicitly invalidate work affected by amendments.

Every plan includes explicit tasks to create or update tests for changed behavior, execute the complete maintained required test suite, repair authorized failures and rerun tests, apply the documentation increment, verify documentation, and record implementation results. Plan documentation work as part of the request rather than postponing it as future debt.

Implement is a separate explicit command. It uses the identified plan, its valid approval or direct-implementation authorization, current human-authored product instructions, and relevant current documentation. Before starting or resuming, reconcile changes to those inputs, the registered folder mapping/access modes, and the actual product files. Complete the applicable workspace-scope and documentation reconciliation before relying on newly included or relocated areas. Missing, stale, or materially inconsistent prerequisites prevent implementation until resolved.

### Tests and unrelated-defect disposition

The required test suite covers the current product tests and retained applicable regression tests from earlier change requests. Keep executable tests in normal product source locations within the workspace; archived request evidence does not supply executable historical test copies. Maintain one defined suite inventory and coverage/acceptance mapping across the configured product folders. Every registered suite has a test-run contract declaring its intended environment, prerequisites, expected effects, isolation and cleanup arrangements, folder-qualified working/source/output/cache/temp locations, applicable workspace revision, and authorization. Suite results are recorded centrally. A suite reading a read-only folder must place every managed output in an authorized writable location; if its runner cannot do that, report the required test as blocked. Execute it only in its configured test environment and within authorized effects. External-service effects need their applicable authorization; they never permit filesystem writes outside the registered writable folders or into read-only folders. Missing infrastructure, permission, safe isolation, or a workspace-compatible runtime blocks that required suite and successful completion rather than permitting an unapproved fallback environment.

Run relevant checks during tasks and the complete required suite before successful completion. Diagnose failures, repair authorized in-scope defects, and rerun the complete suite after repairs until it passes against the final relevant content or a repair limit/blocker is reached. An in-scope repairable failure starts this repair loop; it does not by itself require another authorization. Missing prerequisites, unexecuted required tests, and unresolved failures prevent a passing result.

Bound automatic repairs with a configurable budget and no-progress detection. Default to at most three unsuccessful repair cycles for the same unresolved failure. A cycle records diagnosis, the authorized attempted repair, and rerun evidence; use a stable failure identity so cosmetic changes, repeated commands, new segments, or portal restarts cannot silently reset the count. Support configured elapsed-time limits and measured token limits where the adapter supplies reliable usage; do not fabricate usage when unavailable. Pause the request as blocked when a limit is reached or evidence shows no meaningful progress, preserving every attempt and the remaining failure. An explicit user continuation may extend the budget with a recorded reason and scope, retaining prior history and valid authorization. It never waives test requirements or converts failed, skipped, stale, or unexecuted required tests into passing results.

Never remove, weaken, disable, or reclassify a test merely to obtain a pass. Changes to expected behavior and corresponding tests must trace to authorized requirements and remain reviewable. Do not execute obsolete incompatible historical test versions simply because an old request contains them.

When an unrelated issue is found, record it and ask the user which defects to include in this request, defer, or investigate further. Do not automatically expand implementation scope. Included defects become explicit interpretation amendments, plan tasks, test coverage, and documentation impacts; obtain the applicable approval for materially expanded scope. Implement directly does not bypass this user selection.

Maintain all established unfixed defects in the current product documentation's known-defects area, with stable IDs, symptoms, reproduction/evidence, affected content/components, impact, known workarounds when supported, disposition, and originating request or system-operation references. Label suspected issues as unconfirmed until evidence supports defect status. Keep request findings linked to this current catalog, and update resolved status after verified fixes. Defect entries describe product condition and do not create another active request or authorize future work.

A deferred defect that causes any required test to fail keeps the request blocked from successful completion. Recording or acknowledging that failure is never a waiver. The user may authorize its repair in the current request or close the request as cancelled/rejected; do not claim completion with a failed required test. Deferred defects that do not invalidate required tests or acceptance criteria may remain documented.

The independent read-only question lane retains its existing permissions. Findings from that lane may be reported in answers, but recording them in durable product documentation requires an explicitly invoked authorized change-work or documentation-maintenance action. A question answer never starts that action automatically.

## 8. Questionnaires

Editable Markdown checkbox questionnaires inside the change request are authoritative. The portal reads and edits the same files, using revision checks. Questionnaire editing, saving, respondent reassignment, export regeneration, import, review persistence, and submission are operational actions permitted only while idle. Existing forms and records remain viewable and downloadable while busy. Direct external edits remain unsubmitted drafts until a permitted explicit submission reconciles them. Define the exact format in conventions.

Each question has a stable ID and revision, context, intended respondent (Framework user or Requestor), a plain-language explanation, answer instructions, and an always-available free-text field. Choice questions specify single-choice or multiple-choice rules, clear options, and a meaningful recommended option with a short reason where applicable. Recommendations start unchecked and are not implied approval. Use direct free-text questions when facts or examples are needed instead of inventing unsuitable choices. The agent may suggest the intended respondent; the framework user can change the assignment.

During clarification, questions and recommended choices concern the problem, rationale, outcomes, users, required behavior, scope, constraints, and acceptance criteria. Do not ask the user to select implementation approaches during this phase. Implementation alternatives belong to planning and must be identified as design choices rather than user requirements. Questions raised during planning that concern missing requirements return to clarification.

Gather questions in one batch after reasonable investigation. Ask later batches only when new information creates new consequential uncertainty. Make it easy to answer all questions in one copy-paste form.

Mark consequential blockers separately from optional choices. Explain documented defaults for optional unanswered questions. Preserve user wording and answer revisions. Handle mutually exclusive checks, malformed edits, partially answered forms, and a free-text answer modifying a checked choice without silently guessing.

Submitted answers remain linked to the definition, decisions, plan, and affected execution. Human approval and authorization fields cannot be self-issued by the agent.

### Requestor questionnaire: explanations and plain-text export

Support requestors who do not use AIH. At the end of each clarification round, automatically generate a separate UTF-8 `.txt` document for the marked outstanding requestor questions. Generate it when the round pauses for external answers, not only after clarification is complete. Provide preview and download; allow explicit regeneration from the current question revisions through the same helper. Reuse a still-current unchanged export, retain superseded exports, and report when no requestor questions remain instead of producing empty forms. Reopening an answered topic requires a visible new question revision or explicit re-request, with provenance.

Each form contains a readable product/request title, short business summary, form reference/revision and generation time, completion instructions, and the selected questions grouped in a useful order. Use an ordinary text editor workflow: mark `[x]`, write after `Answer:` or `Comments:`, and return the completed file. Provide an I don't know / Needs discussion response. A requestor must not need AIH, an account, a portal login, Markdown knowledge, or technical metadata editing to complete the form. Keep stable question references and form identity compact and ask the requestor to retain them.

For every exported question, include all of the following in plain business language:

- The actual question, phrased as one understandable decision or information request.
- An Explanation describing the relevant situation, unfamiliar terms, and exactly what information is being requested.
- Why we ask: how the answer affects the required behavior, scope, users, or acceptance of the change.
- A short neutral example when it helps understanding, explicitly an illustration rather than a prefilled answer or assumed requirement.
- How to answer: choose one, choose all that apply, describe in your own words, or provide the requested value. Include answer space and room for comments or an alternative.

Explanations and examples must clarify what and why without selecting architecture or leading the requestor to an unapproved business decision. Preserve any explicit technical constraint supplied by the requestor as attributed input, while keeping the wording accessible. Distinguish essential answers from optional questions and explain applicable optional defaults. Keep explanations concise but sufficient for a non-technical reader; an internal requirement ID alone is not an explanation.

Store outgoing immutable form snapshots under the request's `clarification/exports/<form-id>/` and accepted incoming originals plus review records under `clarification/received/<receipt-id>/`, with round records under `clarification/rounds/<round-id>/`. Do not create a normal received-content record for an input rejected by the sensitive-input checks. Immutable preservation is subject to the explicit human-authorized historical sensitive-value redaction exception, with non-sensitive audit evidence. Use versioned filenames with request/form references and a UTC generation timestamp. The exact formats, export layout, and matching rules belong in conventions. Internal metadata carries submission, interpretation, question/version, content fingerprint, and audience references. The plain-text file is an exchange snapshot of the authoritative Markdown questionnaire, not a second live answer catalog. Downloading or forwarding it does not change the question state to answered or prove that the requestor received it.

Link the latest applicable form from `output/current.md` and the clarification page, retaining all preceding form and round references with the request. The framework user downloads and shares it through their chosen channel. This feature does not itself send email/messages or grant external sharing authority.

### Returned answers: upload, review, and continue clarification

Provide one convenient import flow in the clarification page:

1. **Upload requestor answers** accepts file selection and drag-and-drop for the generated `.txt` form; **Paste answers** supports responses returned as text. State supported types/size limits and give actionable encoding/format errors. Check for detected sensitive input before preserving a normal receipt, logging content, or proposing changes. Reject detected sensitive content with a request for corrected resubmission and only non-sensitive rejection metadata; discard any framework-controlled transient intake according to the sensitive-input rules. Do not silently redact and accept the form. For an accepted input, preserve its original content, assign a receipt ID, and identify the target request/form. Uploading or pasting only stages that accepted receipt; no agent runs and no active answer is overwritten.
2. **Review imported answers** displays the original question and explanation, previously saved answer where present, proposed received answer, attributed respondent, and status for each match. Match by form/request identity and stable question ID/revision, not filename, display order, or guessed meaning alone. Show missing, unknown, duplicate, altered, contradictory, and stale items explicitly. Offer acceptance of all non-conflicting matched answers as a convenience, plus per-answer accept, retain existing, edit as a user correction, or leave unresolved controls. Preserve the source wording and every reviewed correction's attribution.
3. **Save reviewed answers** applies the reviewed mapping to the existing authoritative Markdown answer drafts using revision checks. This saves drafts only. **Import answers and clarify** performs the reviewed merge, snapshots the selected answer revisions plus current relevant saved input and pending saved amendments, and explicitly invokes the normal Clarify action. Explain that this button submits and continues requirements clarification. It must not require a second generic approval dialog after the review action.
4. Show the saved receipt/submission/round identity and clear progress, then open the resulting interpretation, new questions, or unresolved conflicts. Partial forms may be submitted deliberately; unanswered consequential questions remain blockers and appear in the next relevant questionnaire. No response is invented for a blank answer or I don't know.

The combined action must bind its review to the form, question, existing answer-draft, input, and amendment revisions actually displayed. A changed revision requires refreshing/reconciling the review; never overwrite intervening user edits or silently include newly changed requirements unseen by that review. Explain and resolve unsaved edits elsewhere before submitting. Check the global idle gate and reserve the one accepted operation before committing the reviewed merge, immutable submission, and its starting execution identity through a recoverable transaction, then launch that worker. Startup already counts as busy; this record is not a queued command or permission for another action. If another action owns the product, reject the combined action without merging drafts, creating a submission, or scheduling later work. A launch failure leaves an inspectable saved submission; once no worker or uncertain ownership remains, the user may explicitly resume it. Retrying the same operation cannot duplicate receipt application, amendment effects, or runs.

For an accepted older returned form, show its original questions. Answers to unchanged identified question revisions can be proposed for review; answers to changed or withdrawn questions require explicit resolution. Conflicting currently saved/submitted answers must never be overwritten on import without review. A missing form reference or question ID offers explicit manual matching with provenance; do not guess by position. An accepted form belonging to another or closed request is retained as an unassigned receipt for correction in the current intake context, with no mutation or reopening of that other request. Define the temporary unassigned intake under the existing `input/` directory; after explicit association, move/link the receipt into its owning request through a recoverable helper operation, preserving its ID and source. If no request is open, offer read-only inspection and guidance to start an allowed new request; do not create a request from an upload implicitly.

In accepted inputs, text outside recognized answer fields is retained and displayed for review. New information, changed wishes, or additional requirements in comments can be explicitly linked to a proposed amendment or accepted as attributed requirement input; avoid duplicating the same change as both a new answer and a separate unlinked amendment. A requestor response cannot approve a plan, enable a skill, change human-owned instructions, expand execution permissions, or start implementation. Record the requestor as the attributed source and the framework user as reviewer/submitter.

Expose equivalent deterministic CLI/helper operations to export forms, receive files or pasted text, inspect/review mappings, save answer drafts, manage amendment revisions, and submit Clarify. File-only users can edit the authoritative Markdown answer drafts and amendment files directly, with the same snapshot/conflict rules. Preserve accepted imported originals and their links when transferring answers manually, subject to the dedicated historical-redaction exception. Apply sensitive-input checks before saving user corrections or newly pasted material as normal answer records. Import matching, parsing, revision checks, rendering, and audit bookkeeping belong to Python helpers; semantic explanation writing, requirement interpretation, and ambiguity resolution belong to the clarification skill and the user review.

Keep all accepted drafts, exported snapshots, accepted received originals, reviews, submissions, and resulting interpretation revisions linked in the request audit history, subject to the explicit historical sensitive-value redaction exception. Unreviewed receipts remain distinct from active answers and are not silently loaded as instructions on the next run. While clarification is waiting with no active worker or reservation, the user may regenerate forms, stage/review answers or amendments, and explicitly submit blocker resolution. While any action remains busy, only Stop and passive viewing/download are available; no upload, draft save, review merge, or Clarify submission is accepted or queued. These communication steps never authorize unrelated change work or automatic implementation resumption.

## 9. Product documentation as current product knowledge

Maintain documentation automatically as a normal workflow responsibility. Consult relevant documentation during requirements clarification and before planning and implementation. During initialization, use deterministic inventory and agent-assisted reverse engineering to establish an evidence-based baseline, recording unknowns instead of fabricating facts.

One central documentation tree describes current implementation across all registered product folders, including registered read-only components where relevant. Use stable folder IDs and relative paths in source references; directory boundaries do not create separate products or competing documentation trees. Track intended requirements, observed implementation, verification, and deployment separately. Planned future behavior stays in its request until implemented; approved unmet requirements may appear in the requirements catalog with explicit status.

When documentation, implementation, and human intent disagree, record the discrepancy and distinguish observation from approved meaning. Ask before changing business meaning. Agent inference must never silently become an approved requirement.

Preserve human-authored content. Use file revisions and reviewable diffs without requiring Git. Update affected sections rather than rewriting all documentation on every run.

Track source dependencies and fingerprints across folders, including component interfaces and shared dependencies. Bind provenance to the relevant folder identity, location mapping, and workspace revision. Check drift at run start and after relevant changes; metadata scans may cover the tree, but agents should read only relevant documentation content. External and manually managed components need provenance, last-known state, and explicit unknowns when live inspection is unavailable.

After each relevant change, identify affected documents, update them from evidence, link the owning request or system operation and verification, record unresolved issues, and refresh navigation. If an implementation stops partway through, do not present its old documentation as fully current: record known drift and reconcile it on resumption. If the request is instead cancelled or rejected, preserve a current-state notice in current documentation with retained changes, affected canonical topic IDs, unresolved defects, verification uncertainty, and archived evidence references. Make this notice discoverable without reading the historical request. The next authorized work must reconcile affected knowledge before relying on it. A read-only question may disclose the notice and uncertainty but cannot perform a documentation rewrite; assign reconciliation to an authorized documentation task or permitted no-request maintenance. Closing unsuccessfully does not require passing tests or a full documentation rewrite, and a notice is not a queued request.

Clarify has a narrow exception for factual documentation bookkeeping: record an evidence-backed known-defect entry or mark an affected topic stale, linking the source and request. It may not repair the product, redesign behavior, rewrite substantive business documentation, or convert inferred intent into an approved requirement. Broader documentation changes retain their normal authorization and ownership rules.

Every request maintains `analysis/documentation_increment.yaml`, including requests using Implement directly. During Analysis or internal plan preparation, record affected canonical document IDs/sections, proposed amendments, reasons, source requirements/decisions, and owning task IDs. During implementation, record actual changes, status, before/after content fingerprints or diff references, and verification evidence. Planned changes must never be reported as already applied.

The increment is a request-specific audit record; the documentation tree remains the canonical current product description. Refresh the increment when scope or actual implementation changes. Include known-defect catalog updates when relevant. If no documentation changes are needed, record a justified no-impact determination in the increment rather than omitting it.

Completion requires affected documentation updates or that justified no-impact determination, plus evidence that the increment is reconciled with actual results. Documentation failures remain visible and prevent successful completion.

The rebuild target is enough specification to recreate a functionally equivalent product, with documented acceptance tests defining equivalence. The required reconstruction-readiness gate is a recorded completeness and traceability review of business rules, interfaces, expected results, dependencies, acceptance criteria/tests, and recovery prerequisites. Identify gaps and unsupported claims. Until an independently performed reconstruction exercise supplies evidence, report reconstruction as **specified but not demonstrated**; passing this review or ordinary product tests must not be represented as a demonstrated rebuild. An independent reconstruction exercise is not a mandatory completion gate under this specification. If one is explicitly authorized and performed, record its scope, prerequisites, results, and remaining limitations separately. Do not claim bit-for-bit reproduction or production data recovery from prose alone. Inventory necessary external resources and gaps. Actual operational scripts and IaC stay in normal workspace source folders; documentation contains explanations, versioned/content-fingerprinted references, and inert examples.

### Reverse engineering existing products

Provide a Python `reverse-engineer` command with initial-baseline and incremental-refresh modes, plus deterministic inventory and operation status/resume interfaces. It must work on one or several registered existing product directories without Git, a database, or preexisting AIH documentation. Reuse the same pipeline for first-portal-start bootstrap.

First inventory available authorized source files, configurations, interfaces, tests, build/deployment definitions, and existing documentation across the selected registered folders through deterministic helpers. Respect each folder's access mode, detect duplicate identity, and record unavailable folders without silently reducing product coverage. Extract supported facts without importing or executing inspected product code, hooks, or attachments. Preserve source references and extraction limitations; do not expose credential values. Inventory is evidence collection, not proof of runtime behavior.

Then use the configured agent to synthesize relevant architecture, components/dependencies, domain concepts, observed behavior, interfaces, data flows, operational context, and requirements supported by evidence. Populate the existing balanced documentation tree and applicability catalog, preserving human-authored content. Label inferred requirements, assumptions, contradictions, missing external information, and unverified behavior explicitly. Never convert inference into approved intent or mark product tests as passed because documentation was generated.

Reverse engineering writes product documentation and its own framework records while leaving product implementation, source configuration, executable tests, deployment code, and human-owned instructions unchanged. Newly identified defects are documented with evidence/uncertainty; repairs and refactoring require the established change-request workflow. This operation is distinct from read-only Q&A because it may maintain durable documentation.

For incremental refresh, compare source fingerprints and documented dependencies, inspect changed or newly relevant material, reconcile affected leaves/indexes, and preserve unaffected content. Record scope, provenance, gaps, before/after references, and validation results. Validate the tree and links through scripts. Do not claim that a structurally valid inventory or a partially generated tree is complete product documentation.

Initial bootstrap is an audited setup operation without a change request. Resolve the explicitly configured folder list before product inventory or semantic generation; use the framework-home-only default if no additional folders have been configured. Finish the complete initial documentation baseline for that workspace before opening the first request. Baseline readiness is bound to workspace scope, not just a once-ever initialization flag. Record a baseline-completion check covering the authorized local product inventory, applicability of every required category, evidence-backed relevant content, provenance and explicit unknowns, the reconstruction specification review, and valid catalogs, links, and tree structure. An inventory alone or incomplete generation is insufficient. Unknown external facts and unverified runtime behavior may remain clearly identified; completion does not require resolving every real-world uncertainty or claiming tests passed. Preserve their limitations in the baseline.

While setup is pending and its worker is stopped, allow prerequisite configuration, permitted Q&A, and local drafting, but keep request creation disabled. Active setup follows the same global Stop-only rule. After explicit setup continuation passes the baseline check, release setup ownership and enable request creation as a separate explicit action. Later standalone documentation maintenance is allowed only when no request is open. If a request is open, reverse engineering must be an explicitly authorized documentation task within it, using its documentation increment and evidence records. A user-invoked refresh requires an idle action slot; an already authorized implementation may perform its own planned documentation subtask under the same action. An idle worker does not remove an existing blocker. Reject competing standalone maintenance or report the blocker; do not silently interrupt implementation, unblock a request, or create a second workflow writer.

When the user changes folder membership, locations, or access while idle, retain the previous baseline and mark affected coverage/freshness unresolved. Saving the registry does not run reverse engineering automatically. Offer an explicit documentation reconciliation using the same pipeline, scoped to changed areas and their dependencies. If a request is open, this is a bounded, explicitly authorized documentation task in that request with its increment and evidence, and may resolve a workspace-change blocker; it is not unrelated work or an independent system writer. It does not waive other blockers, plan approval, or the Clarify/Analyze phase boundaries. With no open request, use setup continuation or authorized standalone documentation maintenance. Before implementation relies on newly added or relocated areas, complete their required baseline and reconcile affected cross-folder dependencies. Before any new request opens, the baseline must cover the current configured product scope. Keep removed areas and unresolved obligations identifiable in current-state notices/history where needed; never erase discrepancies to manufacture completeness.

Persist progress before agent switches/interruption, reuse valid completed extraction/generation, and resume through the shared recovery helpers. If profile configuration, compatibility, credentials, or permissions prevent semantic generation, retain useful deterministic results and report documentation as pending/partial with actionable setup or manual-handoff guidance. Never silently select another agent or install a CLI.

## 10. Strictly balanced, progressively readable documentation tree

Use navigation catalog nodes internally and actual documentation only in terminal content leaves. The root catalog must be small enough to read first without loading the entire corpus.

Give every node a stable ID. Each non-root node has exactly one structural parent. Cross-references may link topics but are not structural edges. Maintain one canonical copy of a topic.

Every child catalog entry identifies its node ID, location, title, scope summary, applicability/status, and when to read it. An agent must be able to follow these entries to a needed topic without reading unrelated leaves.

Enforce these initial rules:

- At most eight direct children per catalog.
- Strict balance: the deepest and shallowest content leaves differ by at most one edge from the root.
- Target at most 1,500 words per content leaf. Split at meaningful boundaries; record justified exceptions for indivisible reference material.
- No structural cycles, missing targets, multiple parents, unreachable content, or duplicate canonical IDs.

These are supported configuration parameters, not schema overrides. Depth grows dynamically as content grows. Keep topic grouping meaningful while splitting, merging, or redistributing navigation catalogs. Do not replace strict balance with an unrestricted hierarchy. Avoid empty content and artificial padding documents.

Automatically reorganize when necessary. Preserve IDs and human-authored content, repair links, and record structural changes. When a topic is split, retain its ID for the continuing canonical topic, assign new IDs to new topics, and preserve mappings for old references.

Perform reorganizations through recoverable, revision-checked multi-file transactions. Supported readers must use shared read locks, versioned snapshots, or revision-change detection with retries to avoid observing a half-rewritten tree. Do not promise equivalent consistency to external readers bypassing that protocol. Validate the invariants after committed changes. Resolve IDs independently of physical paths. Search may supplement navigation but cannot replace the tree.

## 11. Documentation coverage

Maintain a progressively navigable applicability catalog covering every category below. Mark categories applicable, not applicable with rationale, or unknown needing investigation. Create substantive documents only when relevant; do not generate empty templates for every category.

Cover:

- Product purpose, scope, capabilities, stakeholders, users, terminology, domain knowledge, and definitions.
- Business requirements, rules, processes, acceptance criteria, and requirement-to-implementation-to-test traceability.
- Nonfunctional requirements, service objectives, performance, capacity, availability, accessibility, localization, and supported platforms.
- Architecture, component responsibilities, solution design, architectural decisions, alternatives, and tradeoffs.
- Product/workspace structure, registered folder IDs, purposes, access modes, component ownership, cross-folder dependencies, important paths and entry points, without assuming repositories exist.
- Data structures, logical and physical models, storage, data ownership, lineage, quality, retention, privacy, and migrations.
- ETL and transformations, pipelines, orchestration, schedules, semantic models, reports, dashboards, and analytics definitions.
- Algorithms, functional logic, APIs, interface contracts, integrations, events, queues, and external services.
- UI definitions, visual and interaction design, web interfaces, navigation, user flows, error behavior, and user help.
- Networking, authentication, authorization, roles, secrets references, threat models, security controls, and relevant compliance constraints.
- Dependencies, versions, licenses, build requirements, configuration, feature flags, and environment differences.
- Deployment procedures and script references, releases, infrastructure, cloud services, resource inventories, and relevant cost assumptions.
- Unit, integration, system, acceptance, performance, and security testing; test data, fixtures, expected results, and evidence.
- Backup, recovery, disaster recovery, restore validation, operational continuity, and rebuild prerequisites.
- Logging, monitoring, alerting, incident response, troubleshooting, administration tools, and support interfaces.
- Operational processes, schedules, maintenance, data operations, escalation, and handover.
- User guides, onboarding, administration guides, support materials, known limitations, a dedicated known-defects catalog, deprecation, and retirement.
- Any additional product-specific knowledge needed to preserve its definition and support functional reconstruction.

For relevant content, record provenance, update dates, source fingerprints, owning requests or system operations and decisions, verification status, and unresolved review items. A navigation catalog must not become a huge flat copy of all documentation.

## 12. Changes ledger and evidence

Use append-only structured change and decision events under `ledger/`, plus a generated readable index. Ordinary corrections append superseding events; they do not silently rewrite history. The explicit human-authorized historical-redaction exception below is the only sensitive-value removal exception to normal immutable-record preservation.

Give events stable IDs and links to requests or system operations, applicable submissions, runs, affected artifacts, and decisions. Record human workspace configuration revisions and their affected scope/evidence dispositions. Preserve folder-qualified artifact references and their historical location mapping; later removal or relocation must not rewrite the meaning of past events. Detailed change-work evidence remains in its request; bootstrap/standalone maintenance evidence belongs to its system-operation record. Ledger and state updates must be recoverable and idempotent across interruptions.

Preserve decisions, outcomes, test and verification evidence, and sanitized execution events. Apply configurable retention to verbose CLI output; document the effective policy and never prune silently. Essential request history and decision evidence are not disposable verbose logs.

Record useful summaries and observable actions, not private chain-of-thought. Do not claim to capture unavailable conversations. Append-only files and hashes provide auditability but are not tamper-proof against an actor able to rewrite them.

### Sensitive input and historical redaction

Screen human submissions, attachments, requestor uploads/pasted answers, amendments, and other intake for sensitive values before accepting or copying them into normal AIH records. Reject detected sensitive input and require a corrected resubmission; do not silently sanitize it and then treat it as the exact original. Record only non-sensitive rejection metadata such as time, intake category, status, and a safe correction instruction. Do not retain rejected content or expose its values through logs, errors, previews, temporary files, indexes, or telemetry. Design bounded intake to validate before durable storage and avoid unsafe upload spooling. Detection is best effort, not a promise to detect every secret.

If the rejected source already exists because a human edited a local draft or supplied an existing file, do not silently rewrite or delete that human source. Refuse its submission and additional persistence, identify the problem without repeating the value, and ask for corrected input. Accepted originals, submissions, amendments, and evidence retain their normal exact-preservation rules. Execution output is still sanitized before persistence; it is not an original human submission to be silently altered and adopted.

If a secret is discovered after storage, support an explicit human-authorized historical-redaction operation scoped to identified sensitive material in AIH-owned copies. Remove the value from affected retained originals, revisions, archives, logs, indexes, temporary data, and framework-managed recovery copies where present; reconcile changed fingerprints and derived records so the value is not regenerated or restored. Use a recoverable operation that does not create additional secret-bearing backups. Append a non-sensitive audit event recording authorization, affected record IDs, the redaction reason, and any limits; never include the secret or replace unrelated history. Preserve the fact that an original was redacted rather than claiming byte-for-byte preservation afterward.

This exception does not authorize blanket history editing or agent changes to human-owned custom instructions. Human-owned instruction corrections remain direct human edits. Follow the global idle/ownership rules, preserve any open request and its scope, and invalidate affected evidence explicitly. Report copies outside AIH control as outside the operation's guarantee; do not claim to erase external exports, provider records, or independent backups.

### Implementation logs and results summaries

Every implementation attempt, including normal Implement, Implement directly, resumed segments, failed attempts, and cancelled execution, has durable records under its owning request's execution records. Use per-run/segment identities and define `implementation_log.jsonl` and `implementation_summary.md` in conventions. Create their initial records before product implementation starts, and update progress throughout the run.

The log records timestamps, run/segment and task IDs, relevant input/plan/workspace revisions, available execution events, intentions before consequential observable actions, results afterward, observed file changes and evidence references, test invocations/results/content versions, documentation updates, errors, cancellations, and checkpoints. Flush records incrementally through validated helpers rather than relying on a final model response. Distinguish attempted, confirmed, failed, and uncertain actions; summarize decisions without private chain-of-thought and redact sensitive data before persistence.

The human-readable summary explains what was requested and attempted, which tasks completed or remain incomplete, what actually changed, test and verification outcomes, documentation increment status, included/deferred defects, blockers, uncertain outcomes, recovery guidance, repair-budget usage where applicable, and next action. A successful implementation presents Ready to close; only the explicit human Close successfully action archives the request. Link it to detailed evidence so a user can investigate an unexpected result or failure. Process exit status and product task outcome remain separate.

A crash can prevent a final agent-authored summary. The worker and recovery path must retain available events/checkpoints, reconcile files and side effects, and produce a clearly labeled reconstructed summary on recovery. Do not fabricate a complete action history or claim visibility into operations a CLI did not expose. Preserve unknown outcomes until evidence resolves them, and inspect whether an action succeeded before retrying it. Logs and summaries remain part of the archived request after closure.

## 13. Portable skills, catalog, and discovery

Follow the published Agent Skills specification: each skill directory contains `SKILL.md` with valid YAML metadata and behavioral instructions, plus optional resources. Use the standard metadata extension mechanism for AIH-specific information. Discovery returns metadata, compatibility, availability, enabled status, and locations without returning instruction bodies.

Load a skill body only when selected; load its resources only when needed. New installed skills must be discovered without editing `run.md`. Handle disabled, malformed, duplicate, and incompatible skills with actionable diagnostics. Ambiguous duplicate identities must not be resolved silently.

Every delivered skill must be usable outside AIH by copying its directory, without an exporter, installed AIH engine, portal, database, or hard-coded `.aih_product/` location. Ordinary skills must not require Git. A purpose-specific skill may declare intrinsic dependencies, such as Git and a repository for the optional Git skill. Declare genuine runtime/tool dependencies and accept explicit input/output locations.

Resolve shared conventions and portability as follows:

- `.aih/conventions/` is the authoritative authoring source for shared framework formats, schemas, and templates.
- During core build or release, bundle each skill's required conventions, guidance, and deterministic Python helpers into that skill as generated, versioned resources. Standalone skills must invoke their bundled helpers for those operations, preserving the same contracts without requiring an installed AIH engine.
- Record their source versions and hashes; validate equivalence and reject stale or conflicting bundles.
- AIH integration uses canonical contracts. Standalone execution uses the matching bundled resources.
- The delivered skill is already self-contained. Ordinary product runs never regenerate resources or change the core.
- Required behavioral guidance must also be available in the skill package; an external reference to a shared prompt cannot be its only implementation.

Keep framework state transitions in the engine integration layer. Standalone skill execution must not require recreating the entire AIH lifecycle.

Every skill must provide a compact standalone invocation contract. Record explicit input/output locations, the permitted named workspace folders and their access modes, intended scope and effects, the initiating user instruction, and applicable constraints. Qualify product paths with their folder identity; standalone use may supply a single folder or an explicit list without relying on a global AIH installation. For implementation work requiring a plan, include that plan plus approved-plan or direct-implementation authorization. Read-only, requirements-only, setup/reverse-engineering, and documentation-only invocations record their applicable explicit scope and authority; they do not acquire an implementation-plan requirement merely because they write their permitted records or documentation. Reject missing or contradictory consequential authority before side effects. Persist the effective contract and result/evidence references with the output; do not claim stronger identity verification or consent evidence than the hosting environment provides.

Standalone means usable outside the installed AIH directory, not unrestricted filesystem access. Accept explicitly identified workspace folders when `.aih/` is absent, and enforce the same union-of-registered-folders containment, per-folder read/write restrictions, and host-permission boundaries without requiring AIH global state, an active-request record, or a portal. Installed-AIH portability fixtures must stay inside its workspace while copying skills outside the installed core. A bundled helper must not create bytecode, caches, or executable artifacts in `.aih_product/` or write outside the invocation's allowed writable folders. A helper cannot broaden the registered folder set, use a common ancestor as a shortcut, or mutate read-only components. Skill metadata, examples, and declared effects are never permission grants.

### Initial skill roster

Create all eight skills in the initial complete delivery. The seven core skills are enabled by default; `git-workflow` is installed but disabled by default. Availability still depends on declared prerequisites and the selected profile. Enabled does not mean authorized to execute, and a required unavailable or disabled capability must produce an actionable blocker rather than silently skipping its work or enabling another skill.

Use the following stable IDs as package directory names under `.aih/skills/`. Each package must contain working behavioral guidance, its input/output contracts, needed resources/helpers, and standalone usage examples; catalog entries or placeholder `SKILL.md` files alone do not satisfy delivery.

| Skill ID | Responsibility and selection conditions | Main outputs |
|---|---|---|
| `clarify-requirements` | Iteratively establish what must change and why: problem, users, outcomes, current versus required behavior, scope, constraints, acceptance criteria, contradictions, and unanswered questions. Apply reviewed framework-user/requestor answers and submitted amendments; explain questions for non-technical requestors. Use during Clarify or when a requirement gap is exposed. Read-only investigation and the narrowly authorized evidence-backed known-defect/staleness bookkeeping are permitted; choosing an implementation is not. | Versioned canonical interpretation, explained internal/requestor questionnaires, reviewed answer/source links, round summaries, amendment dispositions, and explicit unresolved requirements. |
| `analyze-and-plan` | Iteratively apply submitted answers and amendments; assess feasibility, alternatives, dependencies, architectural and operational risks, affected components, and unrelated defects; generate or revise the sequential implementation, testing, documentation, and evidence plan. Use during Analyze and internally for explicitly authorized direct implementation. | Required analysis records, sequential tasks with affected paths, requirements-to-task/test mapping, verification strategy, and planned documentation increment. |
| `implement-plan` | Execute authorized tasks sequentially using the identified plan, current product instructions, and relevant documentation. Make scoped product changes and repair authorized defects. Support normal and direct implementation while preserving prerequisite checks and checkpoints. | Product changes, task outcomes and evidence, incremental implementation log, readable results summary, and recoverable incomplete-work records. |
| `test-and-verify` | Design and create/update tests during authorized implementation; execute the complete maintained required suite through deterministic helpers; investigate failures and verify acceptance criteria against the actual checked content. Test strategy may inform Analysis, but test-file changes and execution must respect the current action's authorization. | Product tests, suite inventory and requirement-to-test mapping, execution results, failure analysis, content-bound verification evidence, and explicit unexecuted or stale checks. |
| `reverse-engineer-product` | Establish or refresh understanding of an existing product from authorized source, configuration, tests, interfaces, and existing documentation. Use for initial baselining, authorized incremental refresh, and evidence collection where appropriate. Distinguish observed facts, inferences, and unknowns without changing product implementation. | Evidence-backed baseline content, source references and fingerprints, discovered issues, documentation gaps, and explicit extraction/verification limitations. |
| `maintain-documentation` | Apply authorized documentation increments and maintain current product knowledge, known defects, applicability coverage, traceability, catalogs, cross-links, and the balanced tree. Use for request documentation tasks and authorized bootstrap/maintenance operations. | Current documentation, applied/verified increment records, known-defect updates, catalog/link changes, structural-change records, and documentation validation results. |
| `answer-product-questions` | Answer independent read-only questions from relevant current or explicitly identified historical evidence, including while change work is blocked and its execution owner has stopped. Follow the global single-action rule. Preserve the separate question-lane permissions; do not alter implementation, request scope, product instructions, or durable product documentation. | Answer files under the question output area, question-owned records, source/content references, and explicit uncertainty or freshness limitations. |
| `git-workflow` | Optional extension for Git operations governed by configured rules and existing scoped authorization. Declare Git and repository dependencies; do not make them prerequisites of ordinary framework operation. | Results and evidence for authorized inspection, branch, diff, commit, push, or PR operations. Installation/enablement alone grants none of these actions and never grants integration, publication, deployment, or destructive authority. |

The `clarify-requirements` package must include standalone guidance, conventions, examples, and helpers for iterative rounds, explained plain-text requestor forms, answer-import review, and amendment history. Accept explicit local input/output locations outside the installed AIH core but within the invocation's permitted workspace; using a portal or another installed skill must not be required. Framework request lifecycle and execution ownership remain in the integration layer.

The skill roster does not introduce extra user-facing workflow stages or automatically runnable commands. Analysis owns planning, so do not add a separate planning skill or Plan command. Direct implementation must invoke the necessary analysis/planning capability internally and persist its plan before product changes; it must not require a separate Analyze invocation or fabricate human approval. Standalone `implement-plan` must include the analysis/planning guidance and helpers needed for this mode inside its own package; it must not depend on another installed AIH skill. Apply the same self-contained rule to other cross-skill capability reuse.

Test failure diagnosis does not authorize unrelated repairs. `test-and-verify` reports failures and their scope relationship; `implement-plan` performs authorized product fixes. Tests may be authored only within the authorized implementation scope. A deferred defect failing a required test continues to block successful completion; skills may not weaken or suppress tests to report success. Apply the registered test-run contracts and repair budgets, including explicit no-progress pauses, without erasing attempt history.

Reverse engineering owns baseline discovery and source-grounded interpretation; documentation maintenance owns applying and organizing that knowledge. Share contracts/helpers and reuse valid evidence rather than generating competing baselines or repeating source reads. An authorized bootstrap or maintenance operation may use both skills under the same operation identity. Ordinary read-only questions may inspect existing evidence but must not invoke documentation-writing effects.

Initialization, approvals, state changes, locks, snapshots, controlled execution, catalog generation, archival, and routine recovery bookkeeping remain deterministic Python engine operations, not separate agent skills. All skills use the required helpers. After interruption, the owning skill resumes its semantic work from reconstructed evidence and checked state; recovery never silently grants new authority. Keep shared routing in `run.md`; do not duplicate the whole orchestrator in each package.

### Catalog artifacts and metadata

Deliver `.aih/skills/catalog.yaml` as the compact machine-readable catalog and `.aih/skills/README.md` as its human-readable counterpart. The README explains when to use each skill, its outputs, important boundaries, and links to its package; do not duplicate full skill instructions in either catalog. The portal's Settings > Skills view and CLI listing use the same discovery contract.

Define the catalog schema and validation rules under `.aih/conventions/`. Skill package metadata is the authoritative source for the derived installed catalog; retain AIH-specific fields through the standard metadata extension mechanism. Every entry must identify:

- Stable ID, display name, package version, and concise purpose.
- Selection conditions/capabilities and the cases in which the skill must not be used.
- Input/output contract references and supported standalone parameters, including explicit input/output locations.
- Possible effects and write scope, prerequisites, and required authorization checks; declared effects are descriptions, never authority grants.
- Genuine tool/runtime dependencies and compatibility requirements.
- Package location, metadata/resource fingerprints, and referenced convention/helper bundle versions needed for integrity checks.

Generate and validate both catalogs through Python during core build, installation, upgrades, or explicit core maintenance. Derive them from the same metadata to prevent independently edited conflicting rosters. Include all eight initial packages. Ordinary discovery, listing, selection, and execution must not regenerate a catalog or write anything into `.aih/`.

Discovery reads and validates package metadata without passing skill bodies to the model or executing package code. It must identify newly installed skills without changes to `run.md`, compare the installed metadata against the generated catalog, and report missing/stale catalog entries or malformed, duplicate, and incompatible packages. Read-only discovery can report validated newly installed metadata in its effective in-memory inventory; catalog regeneration and installation integrity reconciliation remain explicit maintenance operations. An integrity/identity problem must not be silently bypassed to execute a package.

Keep installed metadata separate from dynamic state. Store supported enable/disable configuration by stable skill ID in `.aih_product/config.yaml`; compute effective enabled status, current dependency availability, and compatibility from that configuration and validated diagnostics. Do not write product-specific status into the core catalog. Display installed, enabled, available, and authorized as distinct concepts. Permit configuration changes only while operationally idle, using shared revision checks and acknowledged execution boundaries, and preserve effective skill/version references for historical run segments. Installation/catalog regeneration also requires the no-open-request core-maintenance gate.

Load compact metadata first, select only the capabilities required by the current authorized action, and load each selected skill body/resources on demand. Do not routinely read the entire catalog's linked resources or every skill body. New extensions use the same discovery and portability contracts; the initial roster must cover the complete agreed workflow without depending on unspecified future skills.

Skill discovery must not execute code or install dependencies. Skill enablement is declarative, but adding executable adapters or changing trusted executable settings is explicit core/local administration. Metadata and product documents must never trigger arbitrary dynamic imports, installation, or command execution.

## 14. Agent profiles and CLI adapters

Provide a common Python adapter contract, a functional Codex CLI adapter, and manual handoff. Support multiple named profiles and installed adapters without coupling profile names to agents or changing `run.md`.

Profiles identify adapter, trusted executable reference, supported model/settings, timeouts, permissions, credential environment-variable references, and supported runtime/cache/session locations within registered writable workspace folders, never credential values. Support a default profile, per-capability assignments, and explicit per-run selection. Document precedence and record the effective selection and workspace-registry revision for every execution segment.

Validate settings against the selected adapter's schema. Reject arbitrary command templates, executable configuration, and unrestricted argument strings. Resolve privileged executable settings only through deliberate local administration; ordinary requests and agent-generated content cannot modify them.

Provide diagnostics that resolve the executable, detect its version, check adapter compatibility and workspace confinement, inspect documented authentication status where available, and return actionable setup guidance. Diagnostics are operational actions subject to the shared idle/busy gate. Distinguish credentials being present from verified service access. Do not install or upgrade CLIs automatically.

Verify invocation syntax against official documentation and installed help. For Codex, use its documented non-interactive execution and event interfaces. Support plain product directories using the documented non-Git option where compatible; never initialize Git to satisfy an adapter check. Verify resume syntax separately instead of assuming all start options also work for resume.

Each adapter declares and implements availability checks, start, available output/event streaming, run/session identifiers, exit status, timeout, cancellation, and supported resumption. If native resumption is unavailable, start a new session from persisted harness state and label it accurately. Never silently switch agents.

Invoke processes with argument arrays, no shell interpolation, explicit working directories identified by registered root ID and relative path, and an environment containing only required operational/authentication variables. Pass the complete applicable registry and its revision, including read-only roots, to the adapter. Enforce the workspace confinement policy for the process and its children: configure or disable temporary files, caches, session records, logs, bytecode, and other indirect writes before launch. Filesystem mutations must remain within the union of registered writable roots; read-only roots grant no mutation permission. Inert temporary data uses operation-owned locations under the single framework home's `.aih_product/tmp/`; executable environments or build fixtures use authorized ordinary product source/test/build locations within writable roots. Existing installed executables, runtime libraries, and permitted credential references outside the workspace may be read or invoked as host prerequisites; that grants no permission to write or refresh credentials, caches, global configuration, or logs there. If an adapter cannot represent and enforce the configured root collection and access modes with available host controls, block its launch with actionable guidance. Never substitute the roots' common ancestor, silently omit a required root, treat a read-only root as writable, or otherwise relax the boundary. Preserve host restrictions and user-approved permissions. Do not silently enable unrestricted execution, suppress inherited rules, or bypass approval controls.

Every agent receives instructions to follow the single framework home's `.aih/run.md`, use the deterministic helpers, and respect the identified action, request/question/system-operation ID, applicable submission, profile, segment, registered root identities/access modes, and workspace-registry revision. Refer to affected product files by root ID and relative path, not an ambiguous path relative to whichever shell directory happens to be active. Bootstrap/standalone maintenance use explicit operation parameters and source revisions instead of fictional request/submission records. Prevent recursive spawning of the same run through explicit parent/run ownership checks. A product's additional folders do not create additional framework instances, active requests, or operational owners.

Portal execution uses documented non-interactive modes. If required approval cannot be handled, safely end or stop the current segment and offer manual handoff once ownership is reconciled. Profile or capability changes are accepted only while the framework is idle and apply to the next explicitly invoked action or segment; they cannot be submitted to a live process. Record an acknowledged handoff at a completed task or agent-segment boundary before a normal ownership transition. Safe Stop may terminate an incomplete segment and must record its unfinished work accurately. Do not assume conversation state transfers between different CLIs.

Preparing a manual handoff acquires an explicit reservation bound to the initiating instruction, permitted scope, request or system-operation identity, submitted revision where applicable, plan/approval or direct-mode authorization, profile, effective root registry/access modes and revision, and unique handoff ID. Persist it before exposing executable handoff instructions. It reserves the sole operational slot, survives portal restarts, and forbids other actions, including workspace reconfiguration, even when external execution cannot be observed. A prepared handoff is not proof that an external agent has started. Returning output does not itself release ownership or prove completion. Stop provides the release workflow: request external termination, obtain the user's confirmation that the external process has stopped or never started, accept needed returned evidence within that workflow, reconcile actual files and results across affected roots, record uncertain attribution, and then release the reservation. Never infer termination from a stale heartbeat, a closed browser, or a deleted lock. If external termination remains uncertain, retain the reservation and allow no competing operation.

Persist resolved CLI versions and non-secret effective settings. Sanitize events before persistence or display; exclude credentials and private reasoning. A zero process exit does not prove task success.

## 15. Shared state, verification, and recovery

Define versioned schemas for configuration, state, the named workspace-root registry and its revisions, skill catalogs and their metadata/integrity references, skill enablement, requests and their catalog, submissions, interpretation/analysis artifacts, clarification rounds, amendment revisions/dispositions and their catalog, requestor form versions/receipts/import reviews, sequential plans/tasks, questionnaires and their catalog, approvals, defect dispositions, documentation increments, system operations and bootstrap progress, inventory/extraction results, reusable summaries and their dependencies, available usage metrics, events, checkpoints, documentation nodes, evidence, implementation logs/summaries, and run records. Include contracts for the fixed framework home, stable root IDs, canonical paths, purposes, read-write/read-only modes, validation/availability status, historical membership, workspace-change reconciliation, the global operational owner and its state, manual-handoff reservations and release evidence, baseline completeness, Ready to close and explicit closure, persistent current-state notices from unfinished closures, repair budgets/attempt histories, suite-specific test-run contracts, standalone invocation records, sensitive-input rejection/redaction metadata, and temporary-directory ownership/cleanup. Product file references identify a root ID and relative path and retain the applicable registry revision; never reinterpret historical references against a silently changed root path. Keep clarified requirements and implementation design/plan artifacts distinct, and bind each plan to its source requirements-definition and workspace-registry revisions. Portal, helpers, and agents must use the same contracts.

Global state includes the active request reference, phase and status, artifact references, analysis/submission/plan and workspace-registry revision references, root availability and per-root baseline status, unresolved workspace-change impacts, current clarification-round and draft answer/amendment references, current sequential task, blockers, next permitted action, checkpoint, run and segment IDs, selected profile, revision, verification status/content fingerprints, pending documentation updates, and current-state notices. Draft references are unsubmitted material, not pending commands. Q&A and system-operation records remain distinct from change-work records but share one operational ownership gate across every registered root. Track initialization/documentation-baseline completeness separately from product test and verification results. Ready to close is an open-request state; the slot remains occupied until an explicit successful-close transaction revalidates and records every gate, including relevant workspace configuration and root availability.

Define idle and busy independently of whether a request is open or blocked. Busy includes accepted action startup, running work, Stop requested, uncertain termination, and an unreleased manual-handoff reservation. While busy, reject every new operational action, including saving/importing/submitting inputs, Q&A, approvals, closure, settings changes, diagnostics, and other execution; do not queue commands or replacement submissions. Only Stop and its narrowly scoped termination/release steps may change operational state. Passive navigation, help, reading existing outputs, and status/event delivery remain available. Internal substeps already authorized by the current action, including logging, task progression, tests, and documentation, continue under that action's ownership. Duplicate requests bearing the same accepted operation ID may return its existing status but must not launch it again.

Reserve the operational slot atomically before accepting a submission or launch transaction. A startup record belongs to that one accepted action and is not a queue. Repeated portal/CLI starts reconcile the recorded owner rather than creating another. Once an action finishes, or Stop safely completes and releases ownership, a new action requires an explicit user invocation; do not automatically launch a saved draft, follow-up command, or the next lifecycle phase. An open or blocked request whose worker has stopped and whose ownership is reconciled is idle, so applicable draft, clarification, and Q&A actions can be accepted subject to their own scope gates. Core maintenance and upgrades additionally require that no request is open and that every setup, maintenance, managed-process, and external-handoff owner has stopped.

Implement safe YAML parsing, validation, atomic replacement, optimistic revision checks, and appropriate file locking. Multi-file operations require a journal or equivalent recoverable file transaction protocol; atomic replacement of one file is insufficient. Multiple workspace roots may lie on different filesystems: do not assume cross-root rename is atomic or advertise an atomic whole-product update. Persist the affected root identities, source/destination content versions, completed effects, and outstanding steps in a durable central journal, and reconcile partial outcomes explicitly. Keep durable journals under the framework home's product state, never only in temporary storage. Report conflicts rather than silently overwriting human or agent changes.

Accept workspace-registry changes only through explicit human administration while globally idle, including when a request is open; root configuration is product configuration, not core maintenance. Validate canonical disjoint roots, host permissions, the mandatory writable home root, stable identities, and access modes before a recoverable commit. Saving records the configuration change and conservatively marks affected interpretation, analysis, plan approval, documentation, verification, and Ready to close for reassessment; unresolved impact is a blocker, not an implicit approval. It does not launch an agent, expand the request's business scope, waive tests, or automatically resume work. Retain prior registry revisions, obligations, and historical references when membership changes. Removing a root only removes membership and future access: it never deletes its files, erases evidence, or silently discards failing tests or unresolved defects. Path changes retain the root's identity and history but require explicit reassessment of whether the relocated content matches; do not silently treat an unrelated folder as the same product material. Framework-home relocation is not an ordinary root edit.

An explicitly invoked, bounded documentation/reverse-engineering action reconciles newly added or changed areas before implementation relies on them. With an open request, record this work in its documentation increment and obtain the applicable explicit documentation authorization; do not require an unrelated standalone maintenance action that the open request would forbid. With no open request, use setup or documentation maintenance. A new request cannot open until the complete configured product baseline meets its gate. Record unresolved facts honestly. Unavailable roots block dependent work and applicable completion gates; marking one unavailable or removing it is not a shortcut around its outstanding obligations.

Detect direct edits through content/revision checks. Direct edits to inputs or answers made outside managed controls while busy remain unsubmitted drafts; they are considered only at the next explicit idle submission and never authorize live scope changes. Unexpected product-source edits invalidate affected evidence and require safe reconciliation. Provide documented stale-lock recovery, process identity checks, and interrupted-transaction reconciliation. Do not treat deletion of a lock as proof that its former process has stopped. Explain limitations when external agents bypass helpers.

Checkpoint before interruptions and operations with consequential partial outcomes. Record completed work, outstanding work, relevant content versions, and uncertain side effects. Normal adoption of a submitted revision or transfer between execution segments requires an acknowledged boundary after a complete plan task or completed agent segment. Because new submissions are rejected while busy, adoption occurs only on the next explicitly invoked action after ownership is released. Stop can end an incomplete task/segment after safely completing an indivisible operation or confirming controlled termination; it must not wait for a whole task to succeed or mark partial work complete. A safely stopped action records an interrupted handoff, and the next explicit action must reconcile that incomplete work before adopting a new revision.

On resumption, reconcile files, state, approvals, process status, evidence, the recorded registry revision, and the current root identities/access modes. Inspect whether an interrupted action already succeeded before retrying. A historical root reference or unfinished journal step does not authorize a write to a removed, relocated, now-read-only, or otherwise unavailable location. Revalidate current confinement and authorization for every recovery step, preserve unresolved partial outcomes, and ask for explicit human resolution where necessary. Use operation IDs and reconciliation to avoid duplicate external writes; if success cannot be established, report uncertainty instead of blindly retrying.

Verification must bind to the actual checked content, including uncommitted or non-Git files across all relevant roots. Use defined content manifests and relevant dependency fingerprints with root-qualified file identities and the workspace-registry revision. Record test selection, commands or invocation identifiers, results, environment, and checked versions. Enforce each registered suite's test-run contract, including authorized environment/effects, declared root-qualified working/output/cache locations, prerequisites, filesystem confinement, isolation, and cleanup. Tests must not mutate read-only roots; configure their outputs in permitted writable locations or report the required suite as blocked. Missing prerequisites, root access, or authorization leave required tests unexecuted and block successful closure. Preserve every repair attempt across resumes and apply the configured budget/no-progress rule; the default is three unsuccessful repair cycles for the same unresolved failure. An explicit continuation may extend the budget but cannot erase history or waive a required passing result. Source or relevant workspace-configuration changes invalidate affected evidence and Ready to close status.

Exclude volatile logs, answer timestamps, bookkeeping revisions, and operation-owned temporary data under `.aih_product/tmp/` from product-content fingerprints so routine record updates do not invalidate their own evidence. Durable evidence, transaction journals, and authorization records must never exist solely in temporary storage. Track documentation freshness and test evidence separately where their dependencies differ.

Stop child processes safely on supported platforms during Stop, cancellation, and timeouts. Record partial results and whether termination is confirmed. Stop is idempotent and distinct from request closure. Do not label work safely stopped, release an external-handoff reservation, or accept another operational action while a child or external process may still be changing files. Keep status and the Stop/release flow available while resolving uncertain ownership.

Workspace confinement, core immutability, read-only root behavior, and human-owned instruction protection need helper validation plus supported host permissions. Validate actual resolved targets, including link/junction escapes, aliases and indirect process effects, for every managed mutation and cleanup; each mutation must resolve inside a currently registered writable root and remain within its applicable scope. Sharing a filesystem ancestor with a registered root grants no access. Registry validation rejects overlapping or aliased root definitions rather than introducing ambiguous precedence between access modes. Direct configuration edits must not bypass the explicit human-administration, idle-state, revision, and validation gates or acquire effect during an active run. Checksums detect changes; they do not enforce a boundary against an actor with permission to rewrite everything. Describe actual enforcement and remaining limitations accurately.

## 16. Local web portal

Provide a Python command that starts the portal from the single framework home and offers browser opening subject to workspace confinement. Automatically launch a browser only when its managed profile, cache, and other filesystem writes can be kept inside registered writable roots using supported configuration; otherwise print the URL for the user to open independently. Do not promise control over an arbitrary existing default-browser profile. Bind to loopback by default, support a configurable port and no-browser operation, print the URL if browser opening fails, and handle port conflicts and shutdown cleanly. When the home's `.aih_product/` is absent, automatically initialize it and start evidence-based documentation baselining through the configured agent; do not stop at showing setup guidance. Additional workspace folders never receive independent `.aih/` or `.aih_product/` installations merely by being registered.

Target one local user. Do not require a shared server, cloud account, database, or Git. Keep the interface clear and focused on user actions rather than internal implementation details.

On first startup, acquire the shared operational slot and use an exclusive recoverable bootstrap operation: run Python initialization/validation, validate the root registry, collect a deterministic inventory across the configured roots, run semantic documentation generation through the configured compatible profile, and validate the resulting documentation. Start with the mandatory writable home root plus any additional roots explicitly preconfigured by the human through supported validated administration. Do not discover and enroll nearby folders automatically. Starting the portal with missing product state authorizes this setup sequence; editing ordinary drafts or subsequently saving workspace settings still never triggers agent execution. Profile selection follows the existing configured precedence and trusted local settings, not opportunistic discovery of any installed executable.

Keep initialization and generation outside HTTP handlers. Open a responsive progress view showing phases, completed work, pending documentation, gaps, errors, and required input. Persist the operation identity before launching workers. Browser refreshes, repeated starts, and concurrent portal processes must reuse/reconcile that operation rather than duplicate work. After confirmed termination and reconciliation, resume interrupted initialization through an explicit user action from recorded checkpoints, preserving user edits and already valid results. Merely reopening the portal must not silently restart a safely stopped operation. Initial documentation baselining must meet its documented completeness and traceability gate before any change request can be opened. Cover all applicable observed product areas and record applicability, evidence, gaps, and explicitly unknown external facts; completion does not require resolving every real-world uncertainty. While setup is pending but idle, allow prerequisite configuration, local drafting, and read-only Q&A. During an active bootstrap operation, apply the same Stop-only rule as every other action.

Distinguish a missing folder, partially initialized state, and invalid existing state. Resume recoverable partial setup; report invalid existing files without overwriting them or treating them as a fresh installation. A completed setup must not regenerate its documentation on every portal launch. Use the explicit refresh workflow when appropriate.

If no usable configured profile/authentication is available, complete the safe deterministic setup, retain its evidence, and display semantic documentation generation as pending setup. Guide profile selection/authentication or manual handoff without automatic installation, silent fallback, or false completion. Authentication presence alone does not prove that generation succeeded. After prerequisites are resolved, resume the recorded operation through an explicit user action. Distinguish generated documentation, unresolved gaps, and verified product behavior in status.

Use the portal as the main working interface. Provide these eight navigation pages, each using the same underlying files and validated Python helpers as the CLI:

1. **Overview**: active request, phase, next permitted action, blockers, current response, test status, documentation status, configured workspace folders and their availability, and selected agent profile. Offer Start request only when no request is open, the complete documentation baseline for the current configured product has passed its gate, and the framework is idle. Show initialization/maintenance progress, workspace reconciliation, and pending setup separately from implementation; expose available token usage without inventing unavailable values.
2. **Current request**: the complete workspace for input, clarification, analysis, the generated plan, approval, sequential implementation, verification, documentation, and outcome. Use the six tabs specified below.
3. **Ask a question**: independent question editor, attachments, explicit Ask action, progress, answer-file links, source/content references, and question history. The page remains readable while busy; editing, attachment intake, and Ask are enabled only while idle. A blocked request with stopped/reconciled execution can still permit Q&A under its read-only scope contract. Q&A cannot overlap another managed action or external-handoff reservation.
4. **Product documentation**: progressive tree, breadcrumbs, search, applicability, root-qualified provenance/source links, update dates, freshness, known defects, unresolved reviews, and persistent current-state notices for retained unfinished changes. Show documentation coverage and unresolved workspace-change impacts for each configured product area. Offer initial/incremental reverse engineering and refresh only when workflow ownership and permissions permit them. Explain when an open request prevents standalone maintenance and when affected knowledge requires reconciliation before reliance. For a workspace change during an open request, expose an explicitly authorized bounded documentation action within that request's increment so the workspace blocker can be resolved without closing the request; it neither expands implementation scope nor automatically runs Analyze or Implement. Show reconstruction status as specified but not demonstrated unless an independent reconstruction exercise has actually supplied evidence.
5. **Runs and recovery**: current and previous runs, sanitized live events, task/segment progress, checkpoints, errors, implementation/results summaries, evidence, available usage, and permitted stop/resume/recovery actions. Include bootstrap and maintenance records with their system-operation identities; distinguish process outcome from product-task outcome and documentation generation from verified product behavior.
6. **History and decisions**: a closed-request catalog with short summaries, dates, and completed/cancelled/rejected outcomes, plus a separate tab for the changes/decisions ledger. Load full historical records only when selected. Preserve links to evidence; do not create a backlog or implicitly reopen a closed request.
7. **Settings**: the Workspace administration panel described below, agent profiles, defaults and capability assignments, compatibility/authentication diagnostics, the searchable Skills catalog, supported declarative product settings, and a human-editable custom-instructions area. Skills shows purpose, installed version, enabled state, dependencies, compatibility, availability diagnostics, and links to usage/input/output details. Enable/Disable changes supported product configuration only, with its effective execution boundary; it does not install packages, invoke a skill, approve a plan, or grant Git/deployment authority. Keep privileged executable/permission settings distinct from ordinary content. Only deliberate human edits may change custom instructions or the workspace-root registry; agent suggestions belong in outputs. Accept settings changes only while idle, state when they take effect, and preserve recorded authorization. Disable operational diagnostics and configuration controls while busy; existing settings remain readable. Workspace folder changes are permitted with an open request while idle and trigger reassessment. Core maintenance, upgrades, and schema-migration controls separately require no open request and no execution owner.
8. **Help**: searchable detailed guide, getting-started walkthrough, workflow and authorization explanations, command reference, file locations, examples, troubleshooting, recovery, and limitations. Render the maintained `.aih/USER_GUIDE.md` and shared command metadata rather than maintaining a separate conflicting guide. Provide contextual links from relevant controls to stable help sections.

**Settings → Workspace** lists the fixed framework home, current registry revision, and every registered folder with its stable root ID/name, canonical filesystem path, purpose, Read-write/Read-only mode, availability/access status, documentation-baseline status, and unresolved reassessment. Make clear that these folders together describe one product with one active request and one operational owner. The home root is mandatory and writable; this panel cannot remove, downgrade, or relocate it. Show that `.aih/`, `.aih_product/`, central temporary data, documentation, and history remain in that one home.

Provide **Add folder**, **Edit**, **Validate access**, **Remove from workspace**, **Save changes**, and **Cancel**. Add/Edit presents a human-selected path, stable name/ID for a new root, purpose, and access mode; an existing root's stable ID is not silently replaced. Validate checks only the explicitly selected candidate and configured roots for canonical identity, existence, supported access and host constraints, and overlapping/aliased definitions; it does not enumerate arbitrary parent or sibling content or create a generic file-browser endpoint. Do not write probe files to read-only roots or to candidates before writable membership has been committed. Report any permission result that cannot be proven without a later authorized operation. Remove presents the affected dependencies and states that files and history are retained. Save revalidates and commits a revision through shared helpers; reject invalid or stale changes without partially changing effective membership. Cancel discards the unsaved form. All these operational controls and editing are disabled while busy, including during a manual-handoff reservation; the recorded configuration remains readable.

Before saving, explain affected product coverage and known dependencies and any impact that remains to be assessed. After saving, show workspace reconciliation as pending, withdraw affected approval/readiness and verification, and identify the next explicitly invoked analysis or documentation action. Do not start an agent automatically or imply that folder membership grants implementation authority. Newly included product areas require documentation baselining before implementation relies on them; with an open request, use its authorized documentation increment, and with no request use setup/documentation maintenance. Preserve recorded test and defect obligations when a folder becomes unavailable, read-only, or removed. Configuration can change while a request remains open only when all execution ownership has been released; this does not relax the no-open-request rule for core maintenance.

The Current request page has these six tabs:

- **Input and clarification**: current input and attachments, draft/submitted indicators, Save draft, repeatable Clarify, editable questionnaires, the canonical interpretation, and submitted revisions with reviewable differences. Include respondent assignment and explanations for every question; a Requestor questions panel with form preview/download, upload/drop and paste intake, reviewed answer matching, and Save reviewed answers / Import answers and clarify actions; an Amendments area with Add amendment, save, supersede/withdraw, status, differences, and immutable history; and a Clarification rounds view with applied changes, answer provenance, outstanding questions, and readiness. Before submitting, show which saved inputs, reviewed answers, and amendments will be included. Disable editing, Save draft, upload/paste intake, import, new export generation, amendment changes, and submission controls while busy; existing artifact views/downloads remain passive and accessible. Reject detected sensitive input before recording it, explain the required corrected resubmission without echoing the value, and preserve only non-sensitive rejection metadata. Show current responses and their request history here or through linked response panels.
- **Analysis**: the repeatable Analyze action, current analysis progress, architect assessment, question-and-answer catalog, optional impacts/risks/decisions, and unrelated issues with explicit include/defer/investigate controls. Apply selections through submitted revisions. Analyze generates/regenerates the plan; do not expose a separate Plan command.
- **Plan**: plan revision, source interpretation and applicable workspace-registry revision, ordered tasks and affected files/folders identified by root name and relative path, mandatory test/documentation tasks, differences from the previous plan, approval status, and Approve plan. Show missing/read-only roots and unresolved workspace changes that block a proposed task. Approval alone must not start implementation. A combined Approve and implement action, if provided, must explicitly represent and record both actions.
- **Implementation**: Implement, Implement directly, Resume, and Stop execution, with sequential task states, available live events, file-change evidence, the implementation log, and the results summary. Explain that Implement directly generates the required plan internally while skipping a separate Analyze invocation and plan approval; it does not waive tests, documentation, evidence, or unrelated-defect scope selection.
- **Verification and documentation**: required test results and checked content versions with applicable root identities and registry revision, failed/unexecuted checks, repair attempts, documentation increment, applied amendments, remaining gaps, workspace-change reconciliation, and linked evidence. Do not provide an accept-failure shortcut for required tests or make outstanding obligations disappear when workspace membership changes.
- **Outcome**: overall result, unmet completion conditions, unresolved defects, final summary, and distinct Close as cancelled/rejected actions. When every gate passes, show Ready to close while retaining the open request. Enable an explicit human Close successfully action only while idle; revalidate all gates and checked versions in its closure transaction before archiving and releasing the slot. Cancellation/rejection likewise requires idle ownership and records a persistent current-state notice covering retained changes, affected documentation, unresolved defects, verification uncertainty, and archived evidence; it does not require successful completion or automatically roll back changes. Stopping a process leaves the request open unless the user explicitly chooses a closure outcome.

Use a persistent header showing product, active request, phase, execution status, and selected profile, with explicit empty/initializing states. While idle, make the relevant next permitted action prominent and keep other allowed actions discoverable. While an action is starting/running, stopping, uncertain, or reserved for external execution, Stop is the only enabled operational control across every page. Passive navigation, existing-file downloads, help, and live status/log viewing remain available. Explain which action owns the slot and why other controls are disabled. Do not hide concurrent operation behind a different page, API, or keyboard shortcut. During clarification, distinguish waiting for the framework user, waiting for the requestor, unresolved conflicts, and ready for Analysis; offer the corresponding answer-import, amendment, Clarify, or Analyze action without automatically advancing stages. Use ordinary language on human-facing pages; IDs, hashes, and detailed technical evidence remain available on demand.

Keep Save draft, submit/run actions, Stop execution, and Close request visibly distinct. Saving forms and uploading requestor answers never run the agent. The reviewed Import answers and clarify action is an explicit combined submission/run command; its label and feedback must make that effect clear. Do not accept or queue new commands or same-request submissions while busy. A user must explicitly choose the next action after completion or safely completed Stop. Explain unavailable actions with their specific cause, such as a busy owner, open request, incomplete initial baseline, stale approval, unresolved blocker, missing profile, or failed required test. There is no request backlog or command queue.

Present revision conflicts with the competing changes and explicit resolution instead of overwriting edits. Settings changes must show their execution boundary. Link contextual help to the relevant action and workflow state. Display sanitized execution events through streaming or polling and provide readable summaries without requiring users to inspect raw logs for routine progress.

Run agent work outside HTTP handlers through a controlled worker/subprocess. Keep the portal responsive. Enforce the shared idle/busy gate in the backend transaction before accepting any operational request, including saves, imports, configuration, Q&A, and closure; disabling buttons alone is insufficient. Deduplicate repeated clicks, refreshes, and retried requests; return the existing operation identity for an idempotent retry and never launch duplicate work. Support Q&A as a separate communication record, subject to the same sequential action gate.

Manual handoff produces usable instructions and evidence expectations together with a durable scope-bound reservation. Clearly distinguish prepared/reserved, externally running when evidenced, Stop requested, external stop awaiting confirmation, reconciliation, and released states; do not label it running or completed without evidence. The reservation keeps Stop as the only operational entry point. Its Stop/release flow may request external termination, obtain confirmation that the external agent stopped or never started, receive returned evidence needed for reconciliation, and release ownership only after reconciliation. These are narrowly scoped steps of the same Stop action, not independently available imports or new work. Preserve the reservation across reloads/restarts and retain it if termination is uncertain.

Protect against path traversal, symlink/junction escapes, arbitrary filesystem access, cross-origin writes, and unsafe content rendering. Ordinary product-file endpoints resolve root ID and relative path through the validated current registry and enforce operation scope and access mode; they must not accept unrestricted absolute paths. Workspace administration accepts an explicitly human-selected candidate path solely for bounded validation and registration, not unrestricted browsing or mutation before registration. Unregistered ancestors and sibling directories remain outside the product workspace. Validate origins and hosts and protect state-changing requests. Render Markdown safely; do not execute HTML, scripts, commands, hooks, or active attachments supplied through product files. Serve user outputs safely.

Do not expose a generic arbitrary-command endpoint. Privileged agent executable and permission configuration must be separated from ordinary content operations. Keep credentials outside persisted configuration, tracked files, outputs, and logs; store only permitted references. Apply the sensitive-input rejection policy before receipt persistence and require corrected resubmission when a value is detected. Sensitive values discovered later follow the explicit human-authorized historical-redaction exception; do not silently rewrite history.

## 17. Installation, commands, and upgrades

### Windows/Linux menu launchers

Provide a shared interactive terminal menu as the Python CLI's `menu` operation. Supply optional `.aih/menu.cmd` for Windows and `.aih/menu.sh` for Linux; each only locates/invokes this Python menu with safely handled paths and arguments and reports minimal runtime/startup errors when Python cannot run. Keep all menu rendering, input validation, root resolution, settings, operation dispatch, lifecycle management, and framework/workflow error handling in Python. Do not interpret commands or hooks from product content in either launcher.

Support direct invocations such as `py .aih/engine/cli.py menu` on Windows and `python3 .aih/engine/cli.py menu` on Linux. Document supported Python selection and Linux executable permissions/terminal invocation. Do not claim universal desktop double-click behavior. Provide an actionable error if the required Python runtime is unavailable, without automatic installation.

The menu offers Open portal, Show current status, Configure/check agent profiles, Workspace settings, Initialization/reverse engineering, Validate framework/product state, Inspect/resume interrupted operations, Open help, and Exit. These actions use the same typed Python APIs and operational gate as the portal and CLI. Workspace settings opens the corresponding portal administration or invokes the same explicit human configuration helpers; it cannot silently enroll folders or begin agent work. While busy, expose Stop as the only operational choice; status, existing records, and help remain readable. Display the framework home, registered workspace folders, state and availability; preserve authorization, blockers, and recovery rules rather than introducing another workflow implementation.

Resolve the fixed canonical framework home as the parent directory containing `.aih/`, independently of the launching shell's working directory, then load and validate that home's product workspace registry. The home is the default mandatory writable root; the workspace may additionally include explicitly registered disjoint folders, including locations outside the home. During initial installation, validate the explicitly selected destination home and confine installation writes there before creating `.aih/`; additional product roots require explicit supported human configuration. An alternate command home must identify a valid installation and cannot silently change registry membership or grant access to its surrounding directory tree. Root IDs and relative paths select product locations; a root-selection argument is not a replacement for registration. Handle installation and additional-folder paths containing spaces and special characters. Repeated launches must detect/reuse the appropriate portal or report its status and must not create duplicate workers. Simply opening the menu or help must not start product work; choosing Open portal retains the agreed automatic first-run initialization behavior. Exiting the menu must not silently cancel an existing run or close a request; explicit shutdown follows the controlled process-lifecycle rules.

### Quick start and shared help

The required `.aih/README.md` provides the initial knowledge needed to start: purpose, supported prerequisites, local-core installation, Windows/Linux launch commands and direct Python alternatives, first-run initialization/documentation behavior, agent/profile setup, the fixed framework home versus configurable workspace folders and access modes, folder purposes, the basic Clarify -> Analyze -> approve -> Implement workflow, direct implementation, the independent question lane, and short troubleshooting/help links. Keep it concise enough to use as a quick start.

The required `.aih/USER_GUIDE.md` supplies detailed operation: phase boundaries, iterative clarification and analysis, explained requestor questionnaires, upload/review/resume, amendment histories, plan approval and direct implementation, sequential tasks, strict test completion and repair budgets, test-run environment/effects contracts, documentation increments, defect disposition, questions versus changes, profiles/skills/human instructions, reverse engineering, the complete configured-product baseline gate, logs/results, Ready to close and explicit successful closure, unfinished-closure current-state notices, recovery, history/ledger, and limitations. Explain the global Stop-only busy state, absence of command queues, acknowledged task/segment boundaries, manual-handoff reservations and Stop/release, multiple named workspace roots and read-only modes, the fixed home and central inert temporary data, explicit idle-time workspace administration during an open request, resulting approval/evidence/documentation reassessment, sensitive-input rejection and the human-authorized redaction exception, standalone invocation contracts, and the separate no-open-request core-maintenance gate. Include a multi-folder setup and recovery example covering unavailable or removed roots and preserved obligations, plus other realistic walkthroughs and a command reference grounded in the shared command catalog. Define stable section anchors for contextual help.

The portal Help page and menu help use this same maintained guide and command metadata. Support navigation/search and local reading without an agent run; help remains accessible when product initialization or authentication is incomplete. Render safely, check links/anchors against the installed version, and update help through explicit core maintenance/upgrades. Do not duplicate user help into behavioral prompts or load the entire human guide into every agent run.

### CLI and maintenance operations

Provide one discoverable Python CLI, usable from the framework home as `python .aih/engine/cli.py <command>` and from other working directories through an explicit valid home reference. Document the equivalent Python launcher usage on Windows and provide `--help` for every command, including the distinction between repeatable Clarify (what and why, applying reviewed answers and amendments), repeatable Analyze (how, including plan generation), and Implement (execute the authorized plan). Implement the named operations below; select and document exact arguments consistently. Every operational command uses the same atomic idle/busy and authorization gates as the portal. Busy commands return a clear structured rejection without persisting a new submission or queued action. Read-only status/help/existing-record inspection and the Stop/release flow remain available.

- Install a pinned core from a local directory, without requiring a release URL or Git. Keep all created or modified installation files within the validated destination home; reading an explicitly supplied installation source does not authorize writes to it or to host-global locations.
- List and validate the workspace-root registry; explicitly add, edit, remove membership, or change supported access modes through human administration. Use the same stable identities, canonical-path checks, revision checks, idle gate, and reassessment semantics as Settings → Workspace. These configuration operations are allowed while a request is open if execution is idle; they do not alter the core or start agent work.
- Initialize the single home's product information idempotently through Python helpers; portal first startup invokes initialization and documentation baselining across the default home and explicitly configured additional roots automatically when product state is absent. Do not open a change request before the complete current configured-product documentation baseline meets its documented gate; retain explicit unknown external facts without pretending they were verified.
- Inventory the configured product and perform initial or incremental `reverse-engineer` operations, with status, cancellation, and checkpointed resumption. For workspace changes with an open request, an explicitly authorized bounded documentation invocation uses that request's increment; with no open request, use setup/documentation-maintenance ownership. Never auto-run it merely because a root was saved.
- Expose the deterministic operation/help catalog and typed helper interfaces used by both agents and portal.
- Validate structure, schemas, state, documentation, skill bundles, and core integrity.
- Discover/list/validate skills from compact metadata and explicitly enable or disable supported skills by stable ID. Return machine-readable catalog/status results and readable summaries without loading instruction bodies into the response. Generate/reconcile the installed machine-readable and human-readable catalogs only through installation or explicit core maintenance.
- Diagnose profiles and CLI compatibility/authentication.
- Open the shared interactive Python menu, directly or through the optional Windows/Linux launchers.
- Start the portal with port and no-browser options.
- Display the shared local help and command reference without starting agent work.
- Clarify, analyze, approve, implement, implement directly, resume, stop execution, and close a request with an explicit human-selected outcome. Passing completion gates produces Ready to close; a separate Close successfully invocation revalidates them and archives the request. Use `clarify` for every requirements-clarification iteration and `analyze` for every analysis iteration and automatic plan generation; do not require or expose a standalone `plan` command.
- Export/preview requestor questionnaires, receive plain-text answers, inspect and explicitly apply reviewed answer mappings, and optionally combine that reviewed application with Clarify submission. Create/save/list/compare amendment revisions and their submitted/applied/superseded states. Pure export generation, receipt intake, draft editing, and review operations do not invoke an agent, but still require the shared operational slot to be idle. Reading/downloading an already existing export is passive. The combined command must explicitly opt into Clarify execution and reuse its contracts.
- Ask a read-only question.
- Prepare a scope-bound manual-handoff reservation and expose its Stop/release confirmation and returned-evidence reconciliation flow. Do not offer general evidence imports as a bypass of the reservation.
- Upgrade the core and perform explicit required state migrations only when no request is open and all managed and external execution owners have stopped.

Installation and repeated initialization preserve existing files. Templates become product-owned after initialization. Do not silently merge incompatible schemas or overwrite human content.

Provide a short host-agent integration snippet pointing to `.aih/run.md`. Preserve existing agent instruction files; do not replace their content. Direct invocation must also work without adding an integration file.

Core maintenance and upgrades require no open change request, including blocked and Ready to close requests, and confirmed termination/release of every setup, maintenance, child-process, and external-handoff owner. Idle-time human workspace-registry edits are product configuration and do not invoke or relax this core-maintenance gate. Do not create a repair exception for a broken helper while a request remains open; explain that the user may need to close it as cancelled or rejected first. Upgrades validate the incoming core and its version, detect unexpected installed-core modifications, preserve all product content and workspace identities/history, identify migrations, and support recoverable replacement. Update bundled skill resources only through explicit core maintenance. Place temporary inert upgrade data inside the single home's `.aih_product/tmp/` when product state exists; executable incoming core packages and staging belong in explicitly authorized writable workspace locations outside `.aih_product/`. The installed core remains at the fixed home; upgrading it does not move the home or install cores in additional roots. All install, upgrade, migration, rollback, and cleanup writes obey the current writable-root union and action scope, including indirect package-manager/runtime writes; a recorded former root is not permission for recovery writes after its removal. Do not invent a release URL or publish artifacts externally.

Document optional Git tracking and ignore guidance separately. Each workspace folder may independently be a plain directory or part of a repository; registering multiple folders must not require creating a common repository. Enabling Git support must not automatically authorize commit, push, PR creation, integration, deployment, or destructive operations. Optional Git helpers obey the current writable-root union and read-only modes for worktrees, configuration, caches, hooks, and credential/SSH side effects; a remote-operation authorization is not permission to modify files outside that boundary.

## 18. Tests and acceptance evidence

Use meaningful automated tests for Python behavior and contracts. Use temporary plain product directories without Git as the baseline, including a product spanning separate registered roots with no implied access to their common ancestor or intervening directories. Put runnable fixtures and executable test/build outputs in approved writable-root source/test/build paths outside `.aih_product/` and the installed immutable core; reserve the single framework-home `.aih_product/tmp/` for inert temporary data. Keep test-runner, subprocess, and browser profiles, caches, logs, and temporary writes within registered writable roots. Read-only roots remain read-only for indirect effects as well as direct edits. Do not substitute mocked agent behavior for a claim that a real agent followed instructions.

Demonstrate:

1. Installation and repeat initialization preserve content; core upgrades preserve product state and report migrations/conflicts. Core maintenance and upgrades are refused while any request is open or any setup, maintenance, or external execution owner remains active; an idle blocked request is still open. A broken helper does not silently bypass this gate.
2. All framework behavior, helpers, adapters, workers, and backend code are Python. The only non-Python harness scripts are the two optional menu launch wrappers with no framework business logic; direct Python operation remains complete. No database or default Git dependency exists.
3. Root organization is valid, immediate directory names do not overlap, and no product skills directory or executable product-state content is accepted. `.aih_product/tmp/` exists for operation-owned inert data; scripts, packages, virtual environments, bytecode, and runnable fixtures are excluded from it.
4. Ordinary runs leave the core and human-owned custom instructions unchanged.
5. Draft edits never trigger work; explicit actions accepted only while idle snapshot revisions. No new submission or replacement command is accepted or queued while an action is busy. At the next explicit idle action, submitted amendments invalidate affected approvals/evidence and are adopted through a recorded, acknowledged complete-task or complete-segment handoff, or reconciliation of the explicitly incomplete checkpoint after a safe Stop. An external edit during execution remains unsubmitted and cannot silently alter the executing instruction.
6. Only one request can be open, no backlog is created, blockers stop change work, and repeated commands do not launch duplicate processes. Starting, running, stopping, uncertain termination, and reserved external handoff all exclude a second operational action across change work, Q&A, setup, and maintenance. Busy portal controls expose only Stop as an operational action; Save, import, submit, Ask, approval, closure, and configuration changes are disabled, and the backend enforces the same rule. Passive navigation, help, status/log viewing, and existing-artifact viewing/download remain available. Duplicate requests can return the existing action identity without starting another action.
7. Plan approval alone does not implement; ordinary implementation requires current approval. Implement directly records its authorized exception to separate analysis invocation/approval while persisting the required plan before product changes.
8. Stopping execution differs from closing a request. Stop may safely terminate an incomplete task or segment after its indivisible operation is finished or controlled termination is confirmed; unfinished work remains unfinished, and no subsequent action starts automatically. Cancelled/rejected closure preserves partial outcomes and creates a persistent current-state notice linking retained changes, affected documentation, unresolved defects, verification uncertainty, and archived evidence. It permits a subsequent request without falsely requiring successful tests or a complete documentation rewrite merely to cancel; subsequent authorized work reconciles affected knowledge before relying on it.
9. Passing all required tests, valid verification, documentation, and evidence produces Ready to close while retaining the sole open-request slot. Only the explicit human Close successfully action revalidates the gates, archives the request, and releases that slot. Changed sources or invalidated evidence remove readiness. Neither zero exit status nor automatic completion of the last task closes a request.
10. One current change response is maintained with history in request records.
11. Independent Q&A works with no request and while a request is open or blocked with its worker stopped, writes answer files, preserves request state, and makes no product changes. It is refused while any operational action or external reservation is busy. External file changes affecting an answer's observed sources are detected and reported instead of presenting mixed observations as a verified snapshot.
12. Markdown questionnaires round-trip through file and portal editing, including free text, contradictory selections, partial submissions, respondent assignment, explanations, and revision conflicts. The authoritative answer source remains the request Markdown; exported forms and raw returned files are linked exchange records.
13. Documentation traversal reaches relevant leaves without loading unrelated content; applicability and provenance remain visible.
14. Grow and reorganize documentation across multiple capacity boundaries; validate eight-child limits, depth difference at most one, stable IDs, unique parents, reachability, cross-links, and preserved content.
15. Interrupted documentation transactions recover safely; external edits conflict or serialize without data loss.
16. Documentation drift and verification invalidation detect relevant non-Git file changes without self-invalidating on bookkeeping.
17. Skill discovery exposes metadata without bodies and detects new, disabled, malformed, duplicate, and incompatible skills without changing `run.md`. Validate all eight required initial packages, the seven enabled defaults, the disabled optional Git default, matching YAML/README catalogs, schema validity, resource/version fingerprints, and actionable stale-catalog diagnostics. Ordinary discovery, portal listing, and enablement leave the core catalogs unchanged; explicit maintenance regenerates them deterministically. CLI and portal show consistent installed/enabled/available states, and enablement alone cannot authorize work.
18. Copy each of the eight delivered skill directories outside the installed AIH core, but still inside a registered writable workspace root, and validate its declared input/output contract, usage example, resources, and helpers in clean fixtures without an installed AIH engine or fixed product-state path, supplying only declared dependencies. Validate the compact standalone invocation contract: explicit workspace roots and their access modes, root-qualified input/output locations, permitted scope/effects, relevant plan and current approval or direct-mode authorization where applicable, initiating instruction, and effective host permission boundaries recorded with results. No full AIH lifecycle or stronger identity assurance is implied. Use a separate workspace-contained repository fixture for `git-workflow`, including its declared operation-tool dependencies where needed. Detect altered bundled conventions. Claim behavioral portability only if an agent actually exercises the skill; distinguish package/helper checks from observed agent behavior.
19. Multiple profiles coexist; default, per-run, and capability assignments follow documented precedence. Changes are accepted only while idle and take effect for the applicable next segment at a recorded boundary, without altering a running segment.
20. A controlled fake CLI tests streaming, Unicode and special-character arguments, spaces in paths, authentication/version errors, malformed events, nonzero exits, timeouts, cancellation, child processes, and resumption limitations.
21. Adapter selection supports a product directory without Git. Missing CLIs, invalid settings, unsupported versions, and unavailable authentication produce actionable errors without silent fallback.
22. Manual handoff is functional, accurately labeled, and cannot falsely mark work as executed or verified. Its durable reservation binds the request/scope, submission, applicable plan, and handoff ID, survives portal restart, and excludes every other operational action until the Stop/release flow confirms the external worker has stopped and reconciles returned files/evidence. Uncertain ownership or attribution remains explicit. Evidence intake and external-stop confirmation required for release occur within that flow; they do not enable unrelated actions. Cancelling a prepared but unstarted handoff releases its reservation safely.
23. Atomic writes, revision conflicts, transaction recovery, stale-lock handling, and duplicate-action reconciliation work under simulated interruptions/concurrency.
24. Portal browser/no-browser startup, port conflicts, shutdown, primary workflow, separate Q&A, safe Markdown, origin protections, and file-boundary checks are exercised. Framework-launched browsers use registered writable-root profiles/cache; an uncontrolled browser launch that would write outside the workspace is replaced with the documented URL-only path.
25. Product content cannot become arbitrary commands, dynamic imports, privileged configuration, or executable portal content. Seeded sensitive input detected before acceptance is rejected without persisting its content in normal AIH records or echoing it in displayed diagnostics; only non-sensitive rejection metadata is kept, and corrected resubmission is required. Test best-effort detection limitations explicitly. For secrets discovered in stored records, test the explicit human-authorized historical-redaction exception: remove the secret from affected AIH-owned copies and append a non-sensitive audit event without general rewriting of history or claiming erasure of external copies.
26. Clarification artifacts and questionnaires capture what and why, scope, constraints, and observable acceptance criteria without selecting an implementation. Preserve explicit user technical constraints and distinguish them from observed existing technology. Exercise read-only investigation, Analysis-owned design choices, and a requirement gap discovered during Analysis that returns to clarification. Clarify may add evidence-backed known-defect entries or stale-topic markers with source/request references, but may not repair the product, redesign it, rewrite substantive business documentation, or treat an inference as approved intent. Test structural/state contracts automatically; claim the semantic phase boundary was followed only when an actual agent exercised it.
27. Repeated Analyze invocations handle initial analysis, submitted answers, additional directions, unchanged inputs, and plan generation/regeneration without a separate Plan command or automatic implementation. Preserve valid completed-task history and invalidate affected approvals/evidence.
28. The interpretation remains the single current request definition, and the questions catalog points to authoritative submitted answers without creating another editable answer store. Validate required analysis records, optional supporting files, sequential task order, affected paths identified by stable root ID and relative path, workspace configuration revisions, and requirements-to-task/test traceability.
29. Normal and direct implementation both persist plans with mandatory test and documentation tasks and execute tasks sequentially. No shortcut omits logs, results, tests, documentation, or user selection of unrelated defects.
30. Run retained regression tests from earlier requests as part of the complete maintained suite. Repair authorized failures and rerun the suite; test suppression or unexecuted required tests cannot produce successful completion. Exercise the configurable repair budget, defaulting to three unsuccessful repair cycles for the same unresolved failure, and no-progress detection. Reaching the applicable cycle, time, or available measured-token limit stops the worker as blocked with attempt history and evidence preserved. Only explicit authorized continuation extends a budget; it never silently resets the history or relaxes completion gates.
31. Unrelated defects require user scope disposition, unfixed established defects appear in current known-defect documentation, and a deferred defect failing a required test blocks completion without exception. Recording the blocker must not launch unauthorized repairs or another request.
32. Every request has a documentation increment with planned/applied/verified distinctions or a justified no-impact result. Reconcile it against actual changes, evidence, and current documentation before successful closure; cancelled/rejected closure uses the current-state notice for unfinished reconciliation.
33. Every implementation attempt has durable incremental logs and a readable results summary. Simulate crashes between action intent and result, recover an explicitly reconstructed summary, retain uncertain outcomes, and prevent blind duplicate execution.
34. Explicit successful or cancelled/rejected closure moves records to separate historical folders, preserving IDs, links, outcomes, and summaries, subject only to the documented human-authorized sensitive-data correction exception. Normal execution reads current documentation and the active request, including unresolved current-state notices; historical evidence is fetched on demand through its catalog.
35. First portal startup without product state initializes the single framework-home state, inventories configured product roots, and starts configured-agent documentation generation once. Refresh/restart/concurrent-start scenarios do not duplicate workers, operation IDs, or writes. A request cannot be opened until the complete initial documentation baseline for the configured product roots passes its coverage/completeness gate; adding a root makes its required local coverage part of that gate. Explicitly recorded unknown external facts do not alone prevent baseline completion; missing required local coverage does. Setup, Q&A, and local drafting remain available when setup is pending and no action is busy, while active bootstrap obeys Stop-only operational controls.
36. Interrupted bootstrap resumes without overwriting human content. Invalid existing state is reported rather than reset. Missing profile, incompatible CLI, unavailable authentication, or permission limitations leave semantic work visibly pending with actionable guidance and no silent fallback.
37. Initial and incremental reverse engineering preserve implementation/source/test/instruction fingerprints across configured roots while writing evidence-based documentation, recording stable root IDs, relative paths, and the observed workspace configuration revision. Extracted facts, inferred requirements, unknowns, generated content, and actually verified behavior remain distinguishable.
38. Bootstrap and standalone documentation maintenance keep audit/checkpoint records in the single framework-home product state without a synthetic request, duplicate framework/product-state home, database, or Git. Every operation obeys global single-action ownership. Documentation work within an open request uses that request's authorization and documentation increment; core maintenance/upgrades remain prohibited until there is no open request and all other execution ownership has been released.
39. Inventory, parsing, validation, state updates, test execution/result capture, catalog maintenance, and archival route through tested deterministic helpers. Check equivalent bundled helpers for standalone skills. Test structural/helper behavior separately from actual agent compliance with the mandatory-use rule.
40. Unchanged input sets reuse valid extraction/summaries and avoid unnecessary semantic generation. Source, instruction, extractor, or schema/core changes invalidate affected reuse. Incremental runs select relevant documents without dropping required coverage or full regression tests.
41. Compact outputs retain evidence references and label omitted detail; full available sanitized evidence remains inspectable. Adapter-reported token usage is recorded accurately, and missing usage remains unavailable rather than fabricated as zero. Distinguish actual token measurements from proxy evidence.
42. Windows/Linux launch wrappers invoke the shared Python menu without duplicating logic; direct Python invocation also works. Exercise root resolution from another working directory, paths with spaces/special characters, missing runtime diagnostics, repeated launch deduplication, and menu exit/shutdown behavior. Report which OS launcher paths were actually exercised rather than inferring cross-platform success.
43. README quick-start commands, USER_GUIDE command references, menu help, portal help, and contextual section links match the implemented catalog and installed version. Help is accessible before initialization/authentication and does not invoke an agent or modify the core.
44. Exercise all eight portal pages and six Current request tabs with meaningful workflow states, inspecting the rendered interface. Verify explicit draft/submission differences, repeated Analyze and generated plan approval, direct-implementation explanation, defect scope selection, revision conflicts, profile effective boundaries, and distinct stop/closure actions.
45. Portal next-action/disabled-action explanations match validated state. Failed required tests cannot be accepted as a completion exception; historical browsing does not reopen/queue work, and independent Q&A remains available when blocked change work has stopped and no other action or external reservation is busy. Confirm log/result views distinguish process status from task outcomes, Ready to close from closed, and bootstrap documentation from product verification.
46. Repeat Clarify through initial questions, framework-user answers, imported requestor answers, changed wishes/additional information, partial responses, and an unchanged invocation. Preserve the single interpretation and prior revisions, reassess stale answers and affected downstream approval/evidence, and finish ready for Analysis without invoking Analyze or implementation automatically.
47. Generate a versioned plain-text requestor form at the end of a waiting clarification round with only the applicable marked questions. Check each question has an explanation, why it matters, answer instructions, and a neutral example where useful. Verify the file can be completed in an ordinary text editor, contains stable form/question references, keeps earlier exports, and does not require portal access. Review explanation usefulness with meaningful examples; structural checks alone cannot prove clarity for a non-technical reader.
48. Exercise upload/drop and paste, review of matched answers, partial and unknown answers, duplicate IDs/receipts, missing references with manual matching, altered question text, stale forms, newer local answers, answers for withdrawn questions, wrong/closed-request forms, and free-text proposed amendments. Apply sensitive-input rejection before persistence; preserve accepted originals, distinguish attributed requestor from actual submitter, prevent unreviewed answer adoption, and verify these inputs cannot grant plan approval or implementation authority. Busy-state refusal must not retain the attempted import as a queued submission.
49. Verify Save reviewed answers only saves drafts, while Import answers and clarify commits the reviewed merge and explicit submission when idle. Atomically reserve the single accepted action across merge, submission, and worker launch. Simulate revision changes during review, repeated clicks, interruption between merge/submission/worker launch, and a launch failure. Recover without lost newer edits, duplicate effects, a second writer, or a pending replacement command; report draft, submitted, starting, running, and failed states accurately. Recovery of the accepted action is not a queue of later actions.
50. Preserve amendment IDs, every accepted framework-saved/submitted revision and observed external edit, original wording, source, sequence, supersession/withdrawal, and interpretation effects, subject to sensitive-input rejection and explicitly authorized historical redaction. Exercise an amendment contradicting an imported answer, direct edits of current input, repeated submission of the same amendment, and explicit correction of earlier wishes. Verify traceability survives completed/cancelled/rejected archival and ordinary later runs use current interpretation rather than reapplying historical amendments.
51. Every registered test suite has a validated test-run contract naming its environment, prerequisites, expected effects, isolation/cleanup, and required authorization, including root-qualified filesystem/cache effects, required access modes, and the applicable workspace configuration revision. Missing permissions or infrastructure block required execution and closure. Tests run only in configured environments with authorized effects; independently authorized external-service effects do not permit filesystem writes outside registered writable roots or into read-only roots.
52. The reconstruction documentation gate reviews completeness and traceability of business rules, interfaces, expected results, dependencies, and recovery prerequisites. Passing that review is reported as specified but not demonstrated until an independent reconstruction exercise has actually supplied evidence. Such an exercise is not silently added as a mandatory initial completion gate, and ordinary implementation tests cannot establish reconstructed equivalence.
53. The workspace is the union of explicitly registered canonical roots, with managed creates, writes, renames, deletions, and cleanup confined to its writable subset, including subprocesses and indirect runtime effects. The parent containing `.aih/` remains the mandatory writable framework home, not an authorization for other unregistered locations. Exercise noncontiguous roots, path traversal, prefix lookalikes, symlink/junction/reparse escapes, alternate aliases and hard links where supported, and changed targets between validation and use. Validate disjoint root registration and reject duplicate, nested, or aliasing roots that make membership/access ambiguous; resolving a path never grants access to an unregistered common ancestor. Check final targets and parent paths for create/rename/delete operations, including both roots involved in a move. Report platform-specific checks actually run; a lexical prefix check alone cannot establish confinement.
54. Installed outside-workspace runtimes and host-permitted prerequisites may be read/invoked without granting outside-workspace mutation. Exercise redirected or disabled temporary files, caches, session records, bytecode, global configuration, credential refresh, and optional Git/tool side effects. A tool or adapter that cannot honor the registered roots, read-only modes, or host permissions is visibly blocked; a single-directory sandbox is never widened to a common ancestor to imitate multi-root support. Credentials are not copied into tracked product files to work around confinement; direct external user application behavior is not falsely claimed to be controlled by AIH.
55. Temporary directories are uniquely owned by operations, excluded from semantic/source fingerprints and documentation inventories, and cleaned through confined deterministic helpers. Active operation data is protected, interrupted cleanup is recoverable, and temporary data never holds the only copy of durable evidence, checkpoints, or authorization. Executable temporary artifacts remain in authorized ordinary writable-root paths outside `.aih_product/`; Python bytecode writing into product state or the immutable core is disabled.

56. Settings → Workspace lists stable root IDs, names, paths, purposes, and access modes, and supports explicit human Add folder, Edit, Validate access, and Remove from workspace operations. Configuration changes are accepted only when global execution ownership is idle, including while a request is open or blocked with its worker stopped. A busy worker, uncertain termination, reserved external handoff, or another operational action prevents both UI and backend changes. Validate and Save cannot overlap execution through a race; the agent cannot expand its own workspace, and saving configuration never starts an agent or grants implementation scope.
57. Workspace configuration revisions and audit history preserve root identity and prior mappings. Test add, removal, relocation, and writable/read-only changes with an open request: affected analysis, plan approval, documentation, verification, and reusable extraction are marked stale and require explicit reconciliation before reliance. Relocation revalidates actual content and access rather than treating an old relative path as proof of unchanged identity. Unaffected evidence may remain valid with recorded justification. Missing/inaccessible roots produce actionable blocked states; the mandatory framework-home root cannot be removed or made read-only.
58. A newly added product root receives its required documentation baseline through an explicitly authorized reverse-engineering action; when a request is open, the work belongs to that request and its documentation increment. Implementation cannot rely on the new area until that coverage is reconciled. Read-only source roots support evidence-based documentation written to the central product state, while test collection, build tools, cache generation, and attempted repairs cannot write back into them. Test host adapters with genuine multi-root access support and reject incompatible configurations without silently relocating content, broadening permissions, or reducing required tests.
59. Removing workspace membership never deletes source files, historical root mappings, documentation-increment evidence, required failing-test obligations, or unresolved defects. Show affected dependencies and preserve their disposition for explicit review; removing a root cannot manufacture successful verification or closure. Archived requests remain intelligible through stable root-qualified references without granting access to removed locations.
60. Interrupt a task or transaction spanning roots on different filesystems. Preserve per-root progress and reconcile partial outcomes without claiming an atomic cross-filesystem commit, silently rolling back, or executing duplicate changes. If an idle workspace revision removes or restricts a path needed for recovery, recovery blocks the prohibited write and asks for an explicit authorized resolution; it never resurrects old permissions from a journal. Scope, pending recovery, test obligations, and the single action/request rules remain enforced across every root.

Perform a real Codex CLI smoke test if installed, authenticated, and permitted, using an isolated fixture within registered writable roots and a compatible configuration that keeps all managed writes inside those roots. Exercise a multi-root real-agent case only when the adapter and host support the declared roots; otherwise report that case as not live-tested rather than claiming support from a single-root run. If the basic real CLI smoke test cannot run, report the integration as not live-tested and explain the missing prerequisite. Distinguish credential discovery from actual successful agent execution.

Exercise the rendered portal and inspect its primary screens, including Settings → Workspace, with browser profiles and caches contained in registered writable roots. Distinguish automated tests, real agent execution, browser walkthroughs, and untested platform claims. Report tested filesystem/storage assumptions without inferring support for untested local, shared, or synchronized storage.

Create a small synthetic product fixture for an end-to-end demonstration: complete its initial documentation baseline, open a request, clarify it through multiple rounds, export explained questions for a separate non-technical requestor, import and review a partially completed returned form, add an amendment changing an earlier requirement, rerun Clarify until ready, then repeat Analyze through questions/answers and amendments until a plan is generated, approve it, execute its tasks sequentially, pass the full required suite, apply the documentation increment, reach Ready to close, and explicitly invoke the human Close successfully action to archive it. Run each new action only after the previous action has finished or safely stopped. Also demonstrate direct implementation with an internally generated plan, Q&A while blocked work is stopped, user triage of unrelated defects, a deferred defect blocking completion through a failed required test, repair-budget exhaustion and explicit extension, incremental implementation records with interrupted recovery, manual-handoff reservation/release, historical catalog lookup, and cancelled closure with its current-state notice. Include a separate existing-product fixture for automatic first-portal initialization, the initial-baseline request gate, reverse engineering, profile-unavailable recovery, and incremental documentation refresh. Include a multi-root fixture with writable frontend/backend folders, a read-only reference folder, and an unrelated sibling outside the manifest. Exercise an idle workspace addition during an open request, explicit baseline/reconciliation, implementation and required tests spanning roots, and a denied outside-root/read-only write. Keep every fixture and its managed write effects within its registered writable roots and label these fixtures as test examples, not user product requirements.

## 19. Delivery

Deliver the working core, conventions/templates, all eight complete standalone skill packages, `.aih/skills/catalog.yaml`, `.aih/skills/README.md`, the portal Skills catalog, Python CLI and shared menu, optional Windows/Linux launchers, the complete portal, tests, demonstration fixtures including requestor question/answer exchanges and amendment history, `.aih/README.md`, and `.aih/USER_GUIDE.md` with shared menu/portal help.

Document launcher/menu and direct-Python startup, the portal pages/tabs and contextual help, installation and the complete initial-baseline gate before opening a request, initial/incremental reverse engineering and system-operation recovery, mandatory deterministic-helper use, runtime token-efficiency and available usage reporting, supported Python/dependency versions, Windows/Linux operation, agent authentication and profile configuration, file-based and portal workflows, iterative requirements clarification and Analysis with automatic plan generation, requestor question explanations and plain-text exchange, upload/review/Clarify resumption, amendment capture and audit history, analysis-file ownership, sequential implementation, questions versus change requests, plan approval and direct implementation, full regression testing and defect disposition, documentation increments, implementation logs/results, cancellation/closure and historical catalogs, resumption, the initial skill roster and responsibility boundaries, catalog generation/discovery and diagnostics, skill enablement versus authorization, standalone skill usage/portability, custom-instruction ownership, documentation maintenance/rebalancing, state conflicts/recovery, upgrades/migrations, optional Git, retention, and known limitations.

Explain the single operational-action rule, Stop-only busy controls and passive access, absence of queued submissions, acknowledged task/segment boundaries and safe incomplete stops, persistent external-handoff reservation and release, Ready to close and explicit human successful closure, cancellation current-state notices, and the no-open-request gate for core maintenance. Document test-run environment/effect/authorization contracts, repair budgets and explicit extensions, narrow Clarify bookkeeping permissions, reconstruction completeness/traceability review and its specified-but-not-demonstrated status, sensitive-input rejection and the human-authorized historical-redaction exception, the compact standalone invocation contract, multi-root workspace configuration through Settings → Workspace, stable root-qualified references and configuration revisions, one mandatory framework home with central product state, optional read-only roots, direct and indirect write confinement to registered writable roots, and inert `.aih_product/tmp/` ownership/cleanup. Explain human-only workspace changes while globally idle, including with an open request, their effects on scope/approval/evidence, explicit documentation baselining for newly added areas, preserved obligations on removal, and partial recovery across roots without an assumed cross-filesystem atomic transaction. Give actionable guidance for incompatible tool/cache/browser configurations without weakening the write boundary or inventing untested storage guarantees.

Provide a traceability summary connecting major requirements to implementation and actual validation evidence. Report failures and missing checks explicitly. Do not claim the framework is fully verified merely because its unit tests pass.

Finish with a concise report stating what was built, exact installation/startup and first-use commands, what was actually tested, whether a real Codex run occurred, tested platforms, and remaining limitations. Do not replace requested functionality with unspecified future work or silently reduce scope.
