'use strict';
(() => {
const $ = s => document.querySelector(s);
const $$ = s => Array.from(document.querySelectorAll(s));
const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const asArray = value => Array.isArray(value) ? value : value && typeof value === 'object' ? Object.entries(value).map(([id,v]) => typeof v === 'object' ? {id,...v} : {id,value:v}) : [];
const pretty = value => typeof value === 'string' ? value : JSON.stringify(value ?? {}, null, 2);
const data = value => esc(JSON.stringify(value));
const iconPaths = {overview:'M3 3h7v7H3z M14 3h7v7h-7z M3 14h7v7H3z M14 14h7v7h-7z',request:'M7 3h10l4 4v14H3V3h4 M7 8h7 M7 12h10 M7 16h7',questions:'M21 11a9 9 0 0 1-9 9H3l2-5a9 9 0 1 1 16-4 M9 8a3 3 0 0 1 6 0c0 2-3 2-3 4 M12 15h.01',documentation:'M12 5c-4-3-8-2-10-1v15c4-2 7-1 10 1 3-2 6-3 10-1V4c-2-1-6-2-10 1v15',runs:'M4 5h16v14H4z M7 9l3 3-3 3 M13 15h4',history:'M3 11a9 9 0 1 1 2 7 M3 4v7h7 M12 7v5l4 2',settings:'M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8 M9 3h6l1 3 3 1 2 5-2 5-3 1-1 3H9l-1-3-3-1-2-5 2-5 3-1z',help:'M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18 M9 8a3 3 0 0 1 6 0c0 2-3 2-3 4 M12 16h.01'};
const icon = name => `<svg viewBox="0 0 24 24" aria-hidden="true"><path d="${iconPaths[name] || iconPaths.request}"/></svg>`;
const pages = [['overview','Overview'],['request','Current request'],['questions','Ask a question'],['documentation','Product documentation'],['runs','Runs and recovery'],['history','History and decisions'],['settings','Settings'],['help','Help']];
const requestTabs = [['input','Input and clarification'],['analysis','Analysis'],['plan','Plan'],['implementation','Implementation'],['verification','Verification and documentation'],['outcome','Outcome']];
const settingsTabs = [['workspace','Workspace'],['profiles','Agent profiles'],['skills','Skills'],['product','Product'],['instructions','Custom instructions'],['appearance','Appearance']];
const themes = {
 clear:{name:'Clear',description:'Cool neutrals · system sans',mode:'light',font:'system',heading:'system',colors:{bg:'#f4f6f8',surface:'#ffffff',raised:'#edf1f5',text:'#172337',muted:'#536279',border:'#cbd4df',accent:'#2457be','on-accent':'#ffffff',positive:'#166544',warning:'#805212',danger:'#b12c3b',focus:'#855bce'}},
 midnight:{name:'Midnight',description:'Deep blue · system sans',mode:'dark',font:'system',heading:'system',colors:{bg:'#101722',surface:'#192332',raised:'#243144',text:'#edf3fb',muted:'#b4c1d3',border:'#475973',accent:'#a5c5ff','on-accent':'#102244',positive:'#8cdbb3',warning:'#f4cc85',danger:'#ffa9b0',focus:'#c4acff'}},
 warm:{name:'Warm',description:'Paper neutrals · serif headings',mode:'light',font:'system',heading:'serif',colors:{bg:'#f5f1e9',surface:'#fffcf5',raised:'#efe7d9',text:'#322a25',muted:'#685b4d',border:'#cbbfae',accent:'#805033','on-accent':'#ffffff',positive:'#356140',warning:'#80520e',danger:'#a13239',focus:'#8754a4'}},
 contrast:{name:'Contrast',description:'Black & white · Arial',mode:'dark',font:'arial',heading:'arial',colors:{bg:'#050505',surface:'#101010',raised:'#252525',text:'#ffffff',muted:'#e0e0e0',border:'#999999',accent:'#ffdf60','on-accent':'#050505',positive:'#a8ffbb',warning:'#ffdf60',danger:'#ffb3b3',focus:'#70ddff'}}
};
const fonts = {system:"'Segoe UI',system-ui,-apple-system,sans-serif",humanist:"'Trebuchet MS',Verdana,sans-serif",serif:"Georgia,'Times New Roman',serif",arial:'Arial,Helvetica,sans-serif',mono:"ui-monospace,'Cascadia Code',Consolas,monospace"};
const S = {status:{},catalog:[],help:'',route:location.hash.slice(1)||'overview',drafts:{},dirty:new Set(),posting:false,polling:false,lastRevision:null,themeDraft:null,preview:false,modalActions:[],settingsSection:'workspace',requestTab:'input',searches:{},selectedRun:null,logFollow:true,online:true,expanded:{},helpPlatform:'linux'};
let toastTimer;
function toast(message){$('#toast').textContent=message;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').textContent='',8500);}
function notice(text,type=''){return `<div class="notice ${type}">${esc(text)}</div>`;}
function badge(value,type=''){return `<span class="pill ${type}">${esc(value)}</span>`;}
function label(value){return String(value || 'unknown').replaceAll('_',' ').replaceAll('-',' ');}
function date(value){if(!value)return 'Not recorded'; const d=new Date(value);return Number.isNaN(d.valueOf())?esc(value):esc(d.toLocaleString());}
function link(text,path,cls=''){return `<a class="${cls}" href="#${esc(path)}">${esc(text)}</a>`;}
function artifactLink(text,path){return path?`<a href="/api/artifacts?path=${encodeURIComponent(path)}" target="_blank" rel="noopener">${esc(text)}</a>`:'<span class="muted">Not yet recorded</span>';}
function artifactPanel(title,value){if(!value)return `<div class="card"><h3>${esc(title)}</h3><p class="muted">No ${esc(title.toLowerCase())} has been recorded yet.</p></div>`;return `<div class="card"><h3>${esc(title)}</h3>${typeof value==='string'?markdown(value):`<pre>${esc(pretty(value))}</pre>`}</div>`;}
function field(id,title,value='',type='text',hint=''){return `<div class="field"><label for="${esc(id)}">${esc(title)}</label>${type==='textarea'?`<textarea id="${esc(id)}" data-draft="${esc(id)}" ${busy()?'disabled':''}>${esc(value)}</textarea>`:`<input type="${type}" id="${esc(id)}" data-draft="${esc(id)}" value="${esc(value)}" ${busy()?'disabled':''}>`}${hint?`<small>${esc(hint)}</small>`:''}</div>`;}
function choice(id,title,options,value,hint=''){return `<div class="field"><label for="${esc(id)}">${esc(title)}</label><select id="${esc(id)}" data-draft="${esc(id)}" ${busy()?'disabled':''}>${options.map(o=>{const [v,t]=Array.isArray(o)?o:[o,o];return `<option value="${esc(v)}" ${String(v)===String(value)?'selected':''}>${esc(t)}</option>`;}).join('')}</select>${hint?`<small>${esc(hint)}</small>`:''}</div>`;}
function draft(id,original=''){if(!(id in S.drafts))S.drafts[id]=typeof original==='string'?original:pretty(original);return S.drafts[id];}
function dirtyState(ids){return ids.some(id=>S.dirty.has(id))?badge('Unsaved changes','warn'):badge('Saved draft');}
function button(text,local,cls='',extra=''){return `<button data-local="${esc(local)}" class="${cls}" ${extra}>${esc(text)}</button>`;}
function active(){return S.status.active_request || S.status.request || null;}
function operation(){const owner=S.status.operation||S.status.active_operation||S.status.execution;return owner?{...(S.status.current_run||{}),...owner}:null;}
function busy(){return S.posting || !!S.status.busy || !!S.status.owner || !!(operation() && ['starting','running','stopping','uncertain','reserved','reserved_external','external_stop_awaiting_confirmation'].includes(operation().status));}
function catalogEntry(id){return S.catalog.find(x=>x.id===id||x.operation===id||x.name===id);}
function allowed(id){id=({'run-tests':'verify','profile-diagnose':'doctor'})[id]||id;if(id==='stop')return busy()?{ok:true}:{ok:false,reason:'No active execution or external reservation to stop.'};if(busy())return{ok:false,reason:'An action owns the operational slot. Stop execution is the only available action.'};const all=S.status.available_actions;let found=Array.isArray(all)?all.find(x=>(typeof x==='string'?x:x.id||x.operation)===id):all?.[id];if(found!==undefined){if(typeof found==='boolean')return {ok:found,reason:found?'':'This action is unavailable in the current state.'};if(typeof found==='object')return{ok:found.available!==false&&found.enabled!==false,reason:found.reason||found.disabled_reason};if(typeof found==='string')return {ok:true};}const c=catalogEntry(id);if(c?.available===false||c?.enabled===false)return{ok:false,reason:c.reason||c.disabled_reason||'This action is unavailable in the current state.'};return {ok:true};}
function action(text,id,payload={},cls='',constraint=''){const a=allowed(id),reason=constraint||(!a.ok?a.reason:'');return `<span class="operation-control"><button data-operation="${esc(id)}" data-payload="${data(payload)}" data-constraint="${esc(constraint)}" class="${cls}" ${reason?'disabled':''}>${esc(text)}</button>${reason?`<span class="reason">${esc(reason)}</span>`:''}</span>`;}
function savedConstraint(ids){return ids.some(id=>S.dirty.has(id))?'Save these edits before submitting.':'';}
function pageHead(title,description,actions=''){return `<div class="page-head"><div><div class="eyebrow">${esc(S.status.product?.name||S.status.config?.product_name||'YOUR PRODUCT')} / WORKSPACE</div><h1>${esc(title)}</h1><p>${esc(description)}</p></div>${actions?`<div class="head-actions">${actions}</div>`:''}</div>`;}
function tabs(items,page,current){return `<nav class="tabs" aria-label="${page==='request'?'Current request sections':'Page sections'}">${items.map(([id,title])=>`<a href="#${page}/${id}" ${id===current?'aria-current="page"':''}>${esc(title)}</a>`).join('')}</nav>`;}
function empty(title,text,controls=''){return `<div class="card empty"><div class="empty-icon">${icon('request')}</div><h2>${esc(title)}</h2><p class="muted">${esc(text)}</p>${controls}</div>`;}
function table(headers,rows,emptyText='No records yet.'){return `<div class="table-wrap" role="region" aria-label="${esc(headers.join(', '))}" tabindex="0"><table><thead><tr>${headers.map(h=>`<th scope="col">${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr>${r.map(c=>`<td>${c}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="${headers.length}" class="muted">${esc(emptyText)}</td></tr>`}</tbody></table></div>`;}
function inline(value){return esc(value).replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/\[([^\]]+)\]\(([^\s)]+)\)/g,(all,title,url)=>{const decoded=url.replace(/&amp;/g,'&');return /^(https?:\/\/|#)/i.test(decoded)?`<a href="${url.startsWith('#')?'#help/'+url.slice(1):url}" ${url.startsWith('#')?'':'target="_blank" rel="noopener noreferrer"'}>${title}</a>`:title;});}
function markdown(text){const lines=String(text||'').split('\n');let html='',code=false,codeLines=[],list=false;function closeList(){if(list){html+='</ul>';list=false;}}for(const line of lines){if(/^```/.test(line)){closeList();if(code){html+=`<pre><code>${esc(codeLines.join('\n'))}</code></pre>`;codeLines=[];}code=!code;continue;}if(code){codeLines.push(line);continue;}let m;if((m=line.match(/^<a id="([a-zA-Z0-9_-]+)"><\/a>$/))){closeList();html+=`<span id="guide-${esc(m[1])}"></span>`;}else if((m=line.match(/^(#{1,6})\s+(.+)$/))){closeList();const level=Math.min(m[1].length,4),id=m[2].toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');html+=`<h${level} id="guide-${esc(id)}">${inline(m[2])}</h${level}>`;}else if((m=line.match(/^\s*[-*]\s+(.+)$/))){if(!list){html+='<ul>';list=true;}html+=`<li>${inline(m[1])}</li>`;}else{closeList();html+=line.trim()?`<p>${inline(line)}</p>`:'';}}closeList();if(code)html+=`<pre><code>${esc(codeLines.join('\n'))}</code></pre>`;return `<div class="markdown">${html}</div>`;}
function dialog(title,body,actions=[]){$('#modal-title').textContent=title;$('#modal-body').innerHTML=body;S.modalActions=actions;$('#modal-actions').innerHTML=actions.map((a,i)=>`<button data-dialog="${i}" class="${a.class||''}">${esc(a.label)}</button>`).join('');if(!$('#modal').open)$('#modal').showModal();}
function closeDialog(){$('#modal').close();S.modalActions=[];}
function applyTheme(preferences=S.status.config?.appearance||S.status.appearance||{}){let prefs=typeof preferences==='string'?{theme:preferences}:preferences;let name=prefs.theme||prefs.preset||'clear';if(name==='system')name=matchMedia('(prefers-color-scheme: dark)').matches?'midnight':'clear';const theme=name==='custom'&&prefs.custom?prefs.custom:themes[name]||themes.clear;Object.entries(theme.colors||{}).forEach(([k,v])=>{if(Object.hasOwn(themes.clear.colors,k)&&/^#[\da-f]{6}$/i.test(v))document.documentElement.style.setProperty('--'+k,v);});document.documentElement.style.setProperty('--font',fonts[theme.font]||fonts.system);document.documentElement.style.setProperty('--heading',fonts[theme.heading]||fonts.system);document.documentElement.style.fontSize=Math.min(22,Math.max(14,Number(prefs.size)||16))+'px';document.documentElement.style.colorScheme=theme.mode==='dark'?'dark':'light';}
function renderShell(){const current=S.route.split('/')[0];$('#navigation').innerHTML=pages.map(([id,title])=>`<a class="nav-link" href="#${id}" ${current===id?'aria-current="page"':''}><span class="nav-icon">${icon(id)}</span><span>${title}</span></a>`).join('');const r=active(),o=operation(),profile=o?.profile||S.status.effective_profile||S.status.selected_profile||S.status.config?.default_profile||'Not configured',name=S.status.product?.name||S.status.config?.product_name||'Local product';$('#workspace-name').textContent=name;$('#context').innerHTML=`${button('☰','open-nav','mobile-menu','aria-label="Open navigation" aria-controls="sidebar" aria-expanded="false"')}<div><div class="product">${esc(name)}</div><div class="context-detail">${esc(r?.title||r?.id||'No open request')}<span aria-hidden="true">·</span>${esc(r?label(r.phase||r.status):'Product workspace')}</div></div><div class="context-detail">${badge(busy()?label(o?.status||'Starting'):'Idle',busy()?'accent':'')}<span>${esc(typeof profile==='object'?profile.name||profile.id:profile)}</span></div><label class="run-profile hint">Next action profile<select id="run-profile" aria-label="Profile for the next explicit action" ${busy()?'disabled':''}><option value="">Capability assignment / default</option>${asArray(S.status.profiles||S.status.config?.profiles).map(p=>`<option value="${esc(p.id)}" ${S.runProfile===p.id?'selected':''}>${esc(p.name||p.id)}</option>`).join('')}</select></label><a class="appearance" href="#help/${esc(({overview:'portal',request:'workflow',runs:'activity',history:'activity',settings:({workspace:'workspace',skills:'skills',instructions:'skills',product:'maintenance',appearance:'appearance',profiles:'profiles'})[S.settingsSection]||'profiles',help:'quick-start'})[current]||current)}">Help for this page ↗</a><span class="live-update">Live status · ${S.online?'connected':'reconnecting'}</span>`;$('#busy-banner').hidden=!busy();if(busy())$('#busy-banner').innerHTML=`<div class="notice warning"><div class="flex between"><div><strong>${esc(label(o?.action||o?.operation||o?.type||'Execution'))} owns the operational slot</strong><p class="hint">${esc(o?.id||'Accepted action')} · ${esc(label(o?.status||'starting'))}. Other operational controls are unavailable until this action finishes or is safely stopped. Nothing is queued.</p></div>${action('Stop execution','stop',{},'danger')}</div></div>`;}
function workspaceRoots(){return asArray(S.status.workspace?.roots||S.status.config?.workspace?.roots||S.status.config?.roots||S.status.workspace_roots);}
function overview(){const r=active(),docs=S.status.documentation||{},tests=S.status.tests||{},setup=S.status.setup;let content=pageHead('Overview','Your product, current work, and next action.',button('Refresh status','refresh'));const blockers=asArray(S.status.blockers||r?.blockers);if(blockers.length)content+=notice(blockers.map(x=>x.message||x.reason||pretty(x)).join(' '),'warning');if(setup&&setup.status!=='completed'&&setup.status!=='complete')content+=setupCard(setup);content+=`<div class="grid wide-left"><div class="stack"><div class="card next-card"><div class="card-header"><h2>Current work</h2>${badge(r?'Open request':'No open request',r?'accent':'')}</div>${r?`<div class="request-title">${esc(r.title||r.id)}</div><p class="muted">${esc(r.summary||r.scope||'One request connects your requirements, plan, implementation, and evidence.')}</p><div class="meta-row"><span>${esc(label(r.phase||r.status))}</span><span>Last update ${date(r.updated_at||r.updated)}</span></div><div class="action-row">${link(nextActionLabel(r),`request/${nextTab(r)}`,'button-link primary')}${link('View run','runs','button-link')}</div>`:`<h3>What would you like to change?</h3><p class="muted">No change request is open. Start a request to change the product, or ask a read-only question.</p><div class="action-row">${link(docs.complete&&!busy()?'Start request':'Draft a request','request/input','button-link primary')}${link('Ask a question','questions','button-link')}</div><p class="reason">A draft does not open a request. Your first explicit request action requires the complete documentation baseline.</p>`}</div><div class="card"><div class="card-header"><h2>Latest response</h2>${link('Response history','request/input','subtle-link')}</div>${S.status.current_response?markdown(S.status.current_response):'<p class="muted">Responses will appear here after an explicit action produces a result.</p>'}</div><div class="card"><div class="card-header"><h2>Workspace folders</h2>${link('Manage workspace','settings/workspace','subtle-link')}</div>${workspaceRoots().length?workspaceRoots().map(root=>`<div class="root-entry"><div class="flex between"><strong>${esc(root.name||root.id)}</strong>${badge(root.access||'read-write')}</div><p class="hint wrap">${esc(root.purpose||'Product source')} · ${esc(root.availability||root.status||'Registered')}</p><details><summary class="hint">Folder details</summary><p><code>${esc(root.path||root.location)}</code></p><p class="hint">Identity: ${esc(root.id)} · Baseline: ${esc(root.baseline||'See documentation coverage')}</p></details></div>`).join(''):'<p class="muted">The framework home is the default product folder.</p>'}</div></div><div class="stack"><div class="card"><div class="card-header"><h2>Tests and verification</h2>${badge(tests.freshness||'Evidence based')}</div><div class="metrics"><div class="metric"><div class="caption">Passed</div><div class="stat">${esc(tests.passed??asArray(tests.results).filter(x=>x.status==='passed').length)}</div></div><div class="metric"><div class="caption">Failed</div><div class="stat">${esc(tests.failed??asArray(tests.results).filter(x=>x.status==='failed').length)}</div></div><div class="metric"><div class="caption">Not run</div><div class="stat">${esc(tests.unexecuted??tests.not_run??'—')}</div></div></div><p class="hint">A successful process exit alone does not verify the product.</p>${link('Review evidence →','request/verification','subtle-link')}</div><div class="card"><h2>Documentation</h2>${badge(docs.status||docs.baseline_status||'Baseline pending',docs.complete?'good':'warn')}<p class="hint section-gap">${esc(docs.summary||'Coverage is checked against registered product folders. Generated documentation does not prove product behavior.')}</p>${link('Explore documentation →','documentation','subtle-link')}</div><div class="card"><h2>Execution and usage</h2><dl class="meta-list"><dt>Operational slot</dt><dd>${busy()?'Owned':'Available'}</dd><dt>Tokens</dt><dd>${esc(S.status.usage?.total_tokens??S.status.usage?.tokens??'Unavailable')}</dd><dt>Requests</dt><dd>One open request maximum</dd></dl>${link('Inspect runs →','runs','subtle-link')}</div></div></div>`;return content+details();}
function nextTab(r){const phase=r.phase||r.status||'';return /analysis|question/.test(phase)?'analysis':/plan|approval/.test(phase)?'plan':/implement|stop|paused/.test(phase)?'implementation':/verif|test|doc/.test(phase)?'verification':/ready|complete/.test(phase)?'outcome':'input';}
function nextActionLabel(r){return {analysis:'Continue analysis',plan:'Review plan',implementation:'View implementation',verification:'Review verification',outcome:'Review outcome',input:'Continue request'}[nextTab(r)];}
function setupCard(setup){const steps=['Initialize files','Inventory product','Generate documentation','Validate documentation'];return `<div class="card section-gap" style="margin-bottom:1rem"><div class="card-header"><h2>Product setup</h2>${badge(label(setup.status||'pending'),'warn')}</div><p class="muted">${esc(setup.message||setup.reason||'Build an evidence-based documentation baseline before opening the first request.')}</p><ol class="step-list">${steps.map((title,i)=>`<li><span class="step-marker ${i<(setup.completed_steps||0)?'done':''}">${i<(setup.completed_steps||0)?'✓':i+1}</span><div>${title}${i===2?'<div class="hint">Requires a configured, authenticated agent or reviewed manual handoff.</div>':''}</div></li>`).join('')}</ol><div class="action-row">${link('Configure agent','settings/profiles','button-link')}${action('Resume setup','reverse-engineer',{mode:'initial'},'primary')}${action('Prepare manual handoff','handoff',{action:'reverse-engineer'},'')}</div><details><summary>Setup evidence</summary><pre>${esc(pretty(setup))}</pre></details></div>`;}
function details(){return `<details class="section-gap"><summary>Workspace details</summary><dl class="meta-list"><dt>Installed version</dt><dd>${esc(S.status.version||S.status.core_version||'See help')}</dd><dt>State revision</dt><dd>${esc(S.status.revision??'Unavailable')}</dd><dt>Workspace revision</dt><dd>${esc(S.status.workspace?.revision||S.status.config?.workspace?.revision||'Unavailable')}</dd><dt>Framework home</dt><dd><code>${esc(S.status.home||workspaceRoots().find(x=>x.id==='home')?.path||'Local installation')}</code></dd><dt>Open request</dt><dd>${esc(active()?.id||'None')}</dd></dl></details>`;}
function requestPage(parts){let tab=parts[1]||S.requestTab;if(!requestTabs.some(x=>x[0]===tab)){if(parts.length>2&&requestTabs.some(x=>x[0]===parts[2]))tab=parts[2];else tab='input';}S.requestTab=tab;const r=active();return pageHead('Current request','Turn a clear request into an authorized plan, working changes, and verified evidence.')+(r?`<div class="card" style="margin-bottom:1.1rem"><div class="flex between"><div><h2>${esc(r.title||r.id)}</h2><div class="meta-row"><span>${esc(r.id)}</span><span>Submitted revision ${esc(r.submitted_revision??r.submission_revision??r.scope_revision??'none')}</span><span>${esc(label(r.phase||r.status))}</span></div></div><div class="badge-row">${badge('Open','accent')}${badge(r.direct_authorization?'Direct implementation authorized':r.approval?'Human-approved plan':r.plan_revision?'Awaiting approval':'Plan not generated')}</div></div></div>`:notice('Drafting does not create a change request or run an agent. Clarify or Implement directly explicitly submits the saved scope.'))+(r&&asArray(r.blockers).length?notice(asArray(r.blockers).map(b=>typeof b==='string'?b:b.message||b.reason||pretty(b)).join(' '),'warning'):'')+tabs(requestTabs,'request',tab)+({input:requestInput,analysis:analysisPage,plan:planPage,implementation:implementationPage,verification:verificationPage,outcome:outcomePage}[tab]());}
function requestInput(){const r=active(),input=S.status.draft?.change?.text??S.status.draft?.change??r?.input??'',qText=(typeof S.status.questionnaires==='string'?S.status.questionnaires:S.status.questionnaires?.text)||S.status.questionnaire_text||'',questions=asArray(S.status.questionnaires?.questions||S.status.questions_catalog||r?.questions),rounds=asArray(r?.clarification_rounds||S.status.clarification_rounds),forms=asArray(S.status.forms),receipts=asArray(S.status.receipts),amendments=asArray(S.status.amendments||r?.amendments);return `<div class="grid wide-left"><div class="stack"><div class="card"><div class="card-header"><h2>Input and clarification</h2><span id="draft-state-change">${dirtyState(['change-title','change-text'])}</span></div><p class="muted">Describe what must change and why. Clarification establishes requirements; it does not select an implementation.</p>${field('change-title','Request title (optional)',draft('change-title',S.status.draft?.change?.title||r?.title||''))}${field('change-text','Request and amendments (required)',draft('change-text',input),'textarea','Markdown is supported. Include the desired outcome, reasons, and constraints. Never include credentials or secrets.')}<div class="action-row">${button(S.preview?'Edit input':'Preview input','preview-input')}${action('Save draft','save-draft',{lane:'change'},'primary')}${button('Discard unsaved edits','discard-change','',busy()?'disabled':'')}</div>${S.preview?`<div class="doc-preview section-gap">${markdown(draft('change-text'))}</div>`:''}<details><summary>Attachments</summary><p class="hint">Allowed inert text formats: .txt, .md, .json, .csv, .yaml, .yml. Maximum 1 MiB each. Attachments are never executed.</p><div class="upload-area"><label for="change-attachment">Add attachments to the saved draft</label><input id="change-attachment" type="file" accept=".txt,.md,.json,.csv,.yaml,.yml" multiple data-upload="attachment" ${busy()?'disabled':''}></div>${attachmentList(S.status.draft?.change?.attachments||r?.attachments)}</details><div class="separator"></div><h3>Submit saved input</h3><p class="hint">Clarify snapshots the latest saved input, reviewed answers, and active amendments. Saving alone starts nothing.</p><div class="action-row">${action('Clarify','clarify',{},'primary',savedConstraint(['change-title','change-text','questionnaire-text']))}${link('Help with clarification','help/clarification','subtle-link')}</div></div>${questions.length?`<div class="card"><h2>Questions and answers</h2><p class="hint">The request questionnaire is the authoritative answer source. Recommended options are never selected automatically.</p>${questions.map(questionCard).join('')}${field('questionnaire-text','Authoritative questionnaire (Markdown)',draft('questionnaire-text',qText),'textarea','Keep stable question IDs. Record choices and free text explicitly; contradictory choices require review.')}<div class="action-row">${action('Save questionnaire draft','save-questionnaire',{},'primary')}${action('Clarify with saved answers','clarify',{},'',savedConstraint(['questionnaire-text']))}</div></div>`:''}<div class="card"><h2>Requestor questions</h2><p class="muted">Exchange explained questions with someone who does not use this portal. Returned answers are reviewed before they become saved answers.</p><div class="action-row">${action('Generate requestor form','export-questions')}</div>${forms.length?table(['Form','Revision','Download'],forms.map(f=>[esc(f.id||f.title),esc(f.revision||f.version||'—'),artifactLink('View / download form',f.path||f.artifact||f.file)])):''}${field('returned-answers','Paste returned plain-text form',draft('returned-answers'),'textarea','Receiving a form saves an exchange receipt. It does not submit answers or start clarification.')}<label for="answers-file">Upload returned form (.txt or .md, maximum 1 MiB)</label><input type="file" id="answers-file" accept=".txt,.md" data-upload="answers" ${busy()?'disabled':''}><div class="action-row">${action('Receive answers for review','receive-answers',{},'primary')}</div>${receipts.map(receiptCard).join('')}</div><div class="card"><h2>Amendments</h2><p class="hint">Preserve the original wording and every accepted revision. An amendment affects the request only when explicitly submitted.</p>${field('amendment-title','Amendment title',draft('amendment-title'))}${field('amendment-text','Changed wish or additional information',draft('amendment-text'),'textarea')}${field('amendment-reason','Reason and source',draft('amendment-reason'))}${choice('amendment-supersedes','Supersedes an earlier amendment (optional)',[['','No supersession'],...amendments.map(a=>[a.id,a.title||a.id])],draft('amendment-supersedes',''))}<div class="action-row">${action('Save amendment','save-amendment',{},'primary')}</div>${amendments.length?table(['Amendment','Status','Actions'],amendments.map(a=>[`${esc(a.title||a.id)}<details><summary>Original wording and revision history</summary><pre>${esc(pretty(a))}</pre></details>`,badge(a.status||'draft'),`${action('Withdraw','save-amendment',{id:a.id,title:a.title,text:a.text||'',withdrawn:true},'btn-small')}`])):'<p class="hint section-gap">No amendments have been saved.</p>'}</div></div><div class="stack">${artifactPanel('Canonical interpretation',S.status.interpretation||r?.interpretation)}${artifactPanel('Current response',S.status.current_response)}<div class="card"><h2>Clarification rounds</h2>${rounds.length?rounds.map(round=>`<details><summary>${esc(round.id||'Round '+round.revision)} · ${esc(label(round.status||'recorded'))}</summary><pre>${esc(pretty(round))}</pre></details>`).join(''):'<p class="muted">Clarification rounds will record applied changes, answer provenance, outstanding questions, and readiness for Analysis.</p>'}</div><div class="card"><h2>Submitted revisions</h2>${asArray(r?.submissions||S.status.submissions).map(s=>`<details><summary>${esc(s.id||s.revision)} · ${date(s.created_at||s.created||s.time)}</summary><pre>${esc(pretty(s))}</pre>${artifactLink('Open submitted evidence',s.path)}</details>`).join('')||'<p class="muted">No submitted revisions yet. Saved drafts are not submissions.</p>'}${button('Compare saved and submitted input','compare-input')}</div></div></div>`;}
function questionCard(q){const id=q.id||q.question_id,options=asArray(q.options);return `<section class="callout section-gap" aria-label="Question ${esc(id)}"><div class="flex between"><strong>${esc(q.title||q.question||q.text||id)}</strong>${badge(q.blocker||q.consequential?'Consequential':'Optional')}</div><p class="hint">${esc(q.explanation||q.context||'Review the questionnaire before answering.')}</p><p class="hint">Why it matters: ${esc(q.why||q.why_it_matters||'This answer informs the request requirements.')}</p><div class="field"><label for="respondent-${esc(id)}">Respondent</label><select id="respondent-${esc(id)}" data-question-respondent="${esc(id)}" ${busy()?'disabled':''}><option ${q.respondent==='Framework user'?'selected':''}>Framework user</option><option ${q.respondent==='Requestor'?'selected':''}>Requestor</option></select></div><p class="hint">${esc(q.instructions||((q.kind==='multiple'||q.multiple)?'Multiple choices permitted; preserve consistent answers.':'Choose one option or provide a free-text alternative.'))}</p>${options.map((o,i)=>{const option=typeof o==='string'?o:o.label||o.text||o.value||pretty(o),recommended=o.recommended||q.default===option;return `<label class="check"><input type="${q.kind==='multiple'?'checkbox':'radio'}" name="question-${esc(id)}" data-question-choice="${esc(id)}" value="${esc(option)}" ${asArray(q.selected).includes(option)?'checked':''} ${busy()?'disabled':''}><span>${esc(option)}${recommended?' <span class="hint">Recommended, with your explicit choice required</span>':''}</span></label>`;}).join('')}<div class="field"><label for="answer-${esc(id)}">Free-text answer or modification</label><textarea id="answer-${esc(id)}" data-question-answer="${esc(id)}" ${busy()?'disabled':''}>${esc(q.answer||'')}</textarea></div>${q.example?`<p class="hint">Example: ${esc(q.example)}</p>`:''}<p class="hint">Questionnaire edits remain drafts until explicitly submitted.</p></section>`;}
function editQuestion(id,kind,value){const original=draft('questionnaire-text',typeof S.status.questionnaires==='string'?S.status.questionnaires:S.status.questionnaires?.text||''),lines=original.split('\n'),start=lines.findIndex(line=>line.startsWith('## '+id+' |'));if(start<0){toast('The questionnaire source could not resolve this stable question. Refresh and inspect the authoritative Markdown.');return;}let end=lines.findIndex((line,i)=>i>start&&line.startsWith('## '));if(end<0)end=lines.length;if(kind==='respondent')lines[start]=lines[start].replace(/respondent=[^|]+/, 'respondent='+value+' ');else if(kind==='answer'){const index=lines.findIndex((line,i)=>i>start&&i<end&&line.startsWith('Answer:'));if(index>=0)lines[index]='Answer: '+value.replaceAll('\n',' ');}else{const selected=$$('[data-question-choice]').filter(el=>el.dataset.questionChoice===id&&el.checked).map(el=>el.value);for(let i=start+1;i<end;i++)if(/^- \[[ xX]\] /.test(lines[i])){const option=lines[i].slice(6);lines[i]='- ['+(selected.includes(option)?'x':' ')+'] '+option;}}const result=lines.join('\n');markDirty('questionnaire-text',result);const editor=$('#questionnaire-text');if(editor)editor.value=result;}
function receiptCard(receipt){
 const matches=asArray(receipt.rows||receipt.matches||receipt.answers||receipt.mappings);
 return `<details class="section-gap"><summary>Review receipt ${esc(receipt.id)} · ${esc(label(receipt.status||'received'))}</summary><p class="hint">Review the original wording, choices, comments, and attribution before applying answers. Duplicate or stale answers require a deliberate correction; unresolved rows are not adopted.</p><div class="action-row">${button('Accept all matched answers','accept-matched','',`data-receipt="${esc(receipt.id)}" ${busy()?'disabled':''}`)}</div>${matches.map((m,i)=>{
  const key=`receipt-${receipt.id}-${i}`,current=asArray(active()?.questions).find(q=>q.id===m.question_id);
  return `<div class="callout section-gap"><strong>${esc(m.question_id||m.id||'Unmatched answer')}</strong>${badge(m.status||'Needs review')}<p><strong>${esc(m.question||current?.question||'Unknown original question')}</strong></p><p class="hint">${esc(m.explanation||current?.explanation||'No explanation was supplied.')}</p><p class="hint">Attributed respondent: ${esc(m.attributed_to||'Requestor')} · Question revision: ${esc(m.question_revision||'Unknown')}</p><div class="field"><label for="${key}-target">Match to current question</label><select id="${key}-target" data-draft="${key}-target" ${busy()?'disabled':''}><option value="">Unmatched / leave unresolved</option>${asArray(active()?.questions).map(q=>`<option value="${esc(q.id)}" ${(S.drafts[key+'-target']??m.question_id)===q.id?'selected':''}>${esc(q.id+' · '+q.question)}</option>`).join('')}</select></div><details><summary>Previously saved answer and original received fields</summary><pre>${esc(pretty({saved:{answer:m.previous_answer??current?.answer??'',selected:current?.selected||[],comments:current?.comments||''},received:{answer:m.answer||'',selected:m.selected||[],comments:m.comments||''}}))}</pre></details>${field(key+'-answer','Returned / reviewed free-text answer',draft(key+'-answer',m.answer||m.text||''),'textarea')}${field(key+'-selected','Returned / reviewed choices (one option per line)',draft(key+'-selected',asArray(m.selected).join('\n')),'textarea','Retain exact option wording. Changing an answer or choice requires Use edited answer.')}${field(key+'-comments','Returned / reviewed comments',draft(key+'-comments',m.comments||''),'textarea')}${choice(key+'-choice','Review decision',[['unresolved','Leave unresolved'],['accept','Accept returned answer'],['retain','Retain existing answer'],['edit','Use edited answer']],draft(key+'-choice','unresolved'))}</div>`;
 }).join('')}${!matches.length?`<pre>${esc(pretty(receipt))}</pre>`:''}<div class="action-row">${action('Save reviewed answers','review-answers',{receipt_id:receipt.id,review_revision:receipt.current_review_revision||receipt.review_revision,refresh_review:!!(receipt.current_review_revision&&receipt.current_review_revision!==receipt.review_revision)})}${action('Import answers and clarify','import-and-clarify',{receipt_id:receipt.id,review_revision:receipt.current_review_revision||receipt.review_revision,refresh_review:!!(receipt.current_review_revision&&receipt.current_review_revision!==receipt.review_revision)},'primary')}</div></details>`;
}
function attachmentList(attachments,lane='change'){return `<ul class="attachment-list">${asArray(attachments).map(a=>`<li><span>${artifactLink(a.name||a.id,a.path)} <span class="hint">${esc(a.size||'')} bytes</span></span>${action('Remove from draft','remove-attachment',{id:a.id,lane},'btn-small')}</li>`).join('')}</ul>`;}
function analysisPage(){const r=active(),issues=asArray(S.status.defects||r?.defects||r?.unrelated_issues);return `<div class="card"><div class="card-header"><h2>Analysis</h2>${badge('Revision '+(r?.analysis_revision||S.status.analysis_revision||'not generated'))}</div><p class="muted">Analyze applies submitted answers and amendments, assesses the solution, and creates or revises the implementation plan. It never implements.</p><div class="action-row">${action('Analyze','analyze',{},'primary',savedConstraint(['change-text','questionnaire-text']))}${link('Review generated plan','request/plan','button-link')}</div><details><summary>Analysis source versions</summary><pre>${esc(pretty(r?.analysis_sources||S.status.analysis?.sources||{interpretation:r?.interpretation_revision,workspace:S.status.config?.workspace?.revision}))}</pre></details></div><div class="grid section-gap">${artifactPanel('Architect assessment',S.status.assessment||S.status.analysis?.assessment||r?.assessment)}${artifactPanel('Questions catalog',S.status.analysis?.questions||r?.analysis_questions)}</div><div class="card section-gap"><h2>Unrelated issues and scope decisions</h2><p class="hint">Only explicit include/defer/investigate choices affect submitted scope. A deferred defect that fails a required test still blocks successful completion.</p>${table(['Issue and evidence','Status / impact','Draft disposition'],issues.map(issue=>[`${esc(issue.summary||issue.symptoms||issue.title||issue.id)}<div class="hint">${esc(issue.id)} · ${esc(pretty(issue.components||issue.paths||[]))}</div><details><summary>Evidence</summary><pre>${esc(pretty(issue.evidence||issue))}</pre></details>`,`${badge(issue.confirmed?'Confirmed':issue.status||'Suspected')}<p class="hint">${esc(issue.impact||issue.test_impact||'Impact requires assessment')}</p>`,`${choice('defect-'+issue.id,'Disposition',[['investigate','Investigate further'],['include','Include in this request'],['defer','Defer']],draft('defect-'+issue.id,issue.disposition||'investigate'))}${field('defect-reason-'+issue.id,'Rationale (optional)',draft('defect-reason-'+issue.id,issue.reason||''))}${action('Save selection','defect-disposition',{id:issue.id},'btn-small')}`]),'No unrelated defects have been recorded.')}<p class="reason">Save selections keeps draft decisions. Analyze submits them and reassesses affected scope and plan approval.</p></div><div class="grid section-gap">${artifactPanel('Risks and decisions',S.status.analysis?.risks||r?.risks)}${artifactPanel('Proposed documentation increment',S.status.documentation_increment||r?.documentation_increment)}</div>`;}
function planPage(){const r=active(),p=S.status.plan||r?.plan||{},tasks=asArray(p.tasks||r?.tasks||S.status.tasks),revision=p.revision||r?.plan_revision,approval=p.approval||r?.approval;return `<div class="card"><div class="card-header"><h2>Implementation plan</h2>${badge(r?.direct_authorization?'Direct implementation authorized':approval?'Human-approved':p.status||(revision?'Awaiting approval':'Not yet generated'),approval?'good':'')}</div><p class="muted">Review the proposed implementation. Approval records your agreement; it does not start work.</p><dl class="meta-list"><dt>Plan revision</dt><dd>${esc(revision||'Not generated')}</dd><dt>Source scope</dt><dd>${esc(p.scope_revision||p.bindings?.scope_revision||p.submission_id||p.bindings?.submission||r?.scope_revision||'Not submitted')}</dd><dt>Workspace revision</dt><dd>${esc(p.workspace_revision||p.bindings?.workspace_revision||'Not recorded')}</dd><dt>Source interpretation</dt><dd>${esc(p.bindings?.interpretation_revision||r?.interpretation_revision||'Not recorded')}</dd></dl>${p.stale_reason?notice(p.stale_reason,'warning'):''}<div class="action-row">${action('Approve plan'+(revision?' revision '+revision:''),'approve',{plan_revision:revision},'primary',!revision?'Analyze first to generate the required implementation plan.':'')}${link('Go to implementation','request/implementation','button-link')}${link('Analyze','request/analysis','subtle-link')}</div><p class="reason">Approval applies only to the exact current plan and submitted scope. Implementation is a separate explicit action.</p></div>${typeof p==='string'?artifactPanel('Plan',p):p.markdown?artifactPanel('Design and scope',p.markdown):''}<div class="card section-gap"><div class="card-header"><h2>Ordered tasks</h2>${badge(tasks.length+' recorded')}</div><p class="hint">Tasks execute sequentially. Required tests, documentation updates, and result records remain completion obligations.</p><div class="task-list">${tasks.map(task=>`<details class="task"><summary><span><strong>${esc(task.title||task.outcome||task.id)}</strong><span class="caption"> · ${esc(task.id)} · ${esc(label(task.status||'pending'))}</span></span></summary><dl><dt>Outcome</dt><dd>${esc(task.description||task.outcome||task.title)}</dd><dt>Requirements</dt><dd>${esc(pretty(task.requirements||[]))}</dd><dt>Affected paths</dt><dd>${esc(pretty(task.changes||task.paths||task.affected_paths||task.files||[]))}</dd><dt>Operations</dt><dd>${esc(pretty(task.operations||[]))}</dd><dt>Dependencies</dt><dd>${esc(pretty(task.dependencies||[]))}</dd><dt>Completion criteria</dt><dd>${esc(pretty(task.criteria||task.completion_criteria||[]))}</dd><dt>Evidence</dt><dd>${esc(pretty(task.evidence||[]))}</dd></dl></details>`).join('')||'<p class="muted">Analyze generates the task plan after consequential prerequisites are resolved.</p>'}</div></div><div class="action-row">${link('Review required tests','request/verification','button-link')}${link('Review documentation increment','request/verification','button-link')}${button('Compare plan revisions','compare-plan')}</div>`;}
function implementationPage(){const r=active(),run=operation()||asArray(S.status.runs)[0],tasks=asArray(S.status.plan?.tasks||r?.plan?.tasks||r?.tasks),done=tasks.filter(t=>t.status==='completed');return `<div class="card"><h2>Execute the authorized plan</h2><p class="muted">Inspect the current task, evidence, and durable checkpoint. Process status and task outcomes are recorded separately.</p><dl class="meta-list"><dt>Effective profile</dt><dd>${esc(pretty(run?.profile||S.status.effective_profile||S.status.config?.default_profile||'Not configured'))}</dd><dt>Plan revision</dt><dd>${esc(r?.plan_revision||S.status.plan?.revision||'Not generated')}</dd><dt>Task progress</dt><dd>${done.length} of ${tasks.length} completed</dd><dt>Checkpoint</dt><dd>${esc(pretty(run?.checkpoint||'No checkpoint recorded'))}</dd></dl><div class="action-row">${action('Implement','implement',{},'primary',savedConstraint(['change-text','questionnaire-text']))}${action('Resume','resume')}${action('Stop execution','stop',{},'danger')}${action('Prepare manual handoff','handoff',{action:'implement'})}</div></div><div class="card section-gap"><h3>Implement directly</h3><p class="muted">Generate the required task plan, then implement without a separate Analyze command or plan approval. Tests, documentation, evidence, and explicit unrelated-defect scope decisions remain required.</p><p class="hint">This explicit action authorizes only the saved submitted scope. It does not provide unrestricted execution authority.</p>${action('Implement directly','implement-directly',{},'',savedConstraint(['change-text','questionnaire-text']))}</div><div class="grid section-gap"><div class="card"><h2>Sequential task states</h2>${table(['Task','Outcome','Evidence'],tasks.map(t=>[esc(t.title||t.id),badge(label(t.status||'pending')),t.evidence?`<details><summary>View evidence</summary><pre>${esc(pretty(t.evidence))}</pre></details>`:'<span class="muted">Not yet recorded</span>']))}</div><div class="card"><h2>Live execution events</h2><div id="live-events">${eventList(run?.events||S.status.events)}</div><div class="action-row">${link('View full log','runs/'+(run?.id||''),'button-link')}${button('View results summary','view-results')}</div></div></div>${run?.handoff||S.status.handoff?handoffCard(run?.handoff||S.status.handoff):''}`;}
function eventList(events){const list=asArray(events).slice(-35);return list.length?`<div class="log" aria-label="Execution event log">${list.map(e=>`<div class="log-entry"><time>${date(e.time||e.created_at||e.created||e.timestamp)}</time><div>${esc(e.message||e.summary||pretty(e))}</div></div>`).join('')}</div>`:'<p class="muted">No execution events yet.</p>';}
function handoffCard(h){return `<div class="card section-gap"><h2>Manual handoff</h2>${badge(label(h.status||'Prepared for external execution'))}<p class="hint">Execution is not confirmed by preparation. This reservation excludes all other operational actions until Stop / release confirms termination and reconciles returned evidence.</p><pre class="manual-instructions">${esc(h.instructions||h.instruction||pretty(h))}</pre><div class="action-row">${button('Copy instructions','copy-handoff')}${artifactLink('Download handoff',h.path)}${action('Stop / release external handoff','stop',{},'danger')}</div></div>`;}
function verificationPage(){const r=active(),tests=asArray(S.status.tests?.results||r?.verification?.results||r?.tests),increment=asArray(S.status.documentation_increment?.items||r?.documentation_increment?.items||r?.documentation_increment?.topics||S.status.documentation_increment),failures=asArray(r?.repair_failures||S.status.repair_failures||r?.repair_attempts);return `<div class="card"><h2>Verification and documentation</h2><p class="muted">Completion requires passing required tests, current evidence, and applied documentation changes.</p>${table(['Required check','Result and freshness','Checked content','Evidence'],tests.map(t=>[`${esc(t.name||t.suite_id||t.id)}<p class="hint">${esc(pretty(t.requirements||t.task_id||'Registered complete suite'))}</p>`,`${badge(label(t.status||t.result||'not run'),t.status==='passed'?'good':t.status==='failed'?'bad':'warn')}<p class="hint">${esc(t.freshness||'Freshness must be checked at closure')}</p>`,`${date(t.time||t.timestamp)}<details><summary>Root and content versions</summary><pre>${esc(pretty(t.checked_content||t.fingerprints||t))}</pre></details>`,artifactLink('View evidence',t.path||t.evidence_path)]),'No required test execution evidence has been recorded.')}<div class="action-row">${action('Run required tests','run-tests',{},'primary')}${button('Review test contracts','view-test-contracts')}</div><p class="reason">There is no accept-failure shortcut. Missing authorization, stale results, and unexecuted required checks block completion.</p></div><div class="card section-gap"><h2>Documentation increment</h2>${table(['Item / target','Planned change','Applied / verified'],increment.map(i=>[`${esc(i.id||i.target||i.document)}<p class="hint">${esc(i.source_task||i.reason||'')}</p>`,esc(i.change||i.description||i.text||pretty(i)),`${badge(i.status||'pending')}<p class="hint">${esc(i.applied_revision||i.verification||'No applied evidence recorded')}</p>`]),'No documentation increment or justified no-impact finding has been recorded.')}<div class="action-row">${link('Review product documentation','documentation','button-link')}${action('Reconcile workspace documentation','reverse-engineer',{mode:'incremental'})}</div></div><div class="card section-gap"><h2>Repair budget</h2><p class="hint">Budget exhaustion blocks further repair attempts. A deliberate extension records the failure, additional attempts, and reason.</p>${failures.map(f=>`<div class="callout section-gap"><strong>${esc(f.summary||f.id)}</strong><p class="hint">${esc(f.attempts||0)} attempts · budget ${esc(f.budget||'not recorded')}</p>${field('repair-count-'+f.id,'Additional attempts',draft('repair-count-'+f.id,'1'),'number')}${field('repair-reason-'+f.id,'Extension reason',draft('repair-reason-'+f.id))}${action('Extend repair budget','extend-repair-budget',{failure_id:f.id})}</div>`).join('')||'<p class="muted">No repair-budget exhaustion is recorded.</p>'}</div><div class="card section-gap"><h2>Workspace and amendment reconciliation</h2><pre>${esc(pretty(S.status.workspace_reconciliation||r?.workspace_reconciliation||{status:'See current request blockers',amendments:r?.applied_amendments||[]}))}</pre></div>`;}
function outcomePage(){const r=active(),gates=asArray(S.status.completion_gates||r?.completion_gates),ready=!!(S.status.ready_to_close||r?.ready_to_close||r?.phase==='ready_to_close');return `<div class="card ${ready?'result-summary':''}"><div class="card-header"><h2>${ready?'Ready to close':'Completion conditions'}</h2>${badge(r?'Request remains open':'No open request',ready?'good':'')}</div><p class="muted">${ready?'All recorded gates are ready for a final transactional check. Only your explicit closure archives the request.':'Successful closure requires complete tasks, passing current required tests, applied documentation, and no unresolved completion blockers.'}</p>${gates.length?table(['Condition','Status','Evidence or blocker'],gates.map(g=>[esc(g.label||g.name||g.id),badge(g.passed?'Passed':g.status||'Not satisfied',g.passed?'good':'warn'),esc(g.reason||g.evidence||'')])):'<p class="hint">The backend revalidates every completion gate and checked version in the closure transaction.</p>'}<div class="action-row">${action('Close successfully','close',{outcome:'completed'},'primary',!r?'No request is open.':!ready?'The request has not reached Ready to close.':'')}${link('Review verification','request/verification','button-link')}</div></div>${artifactPanel('Final results summary',S.status.results_summary||r?.results_summary)}<div class="card danger-zone section-gap"><h2>Close without completion</h2><p class="muted">Cancellation or rejection retains existing product changes and archives evidence. It records a persistent current-state notice describing unfinished changes, documentation impacts, defects, and verification uncertainty.</p>${field('close-reason','Reason for cancellation or rejection (required)',draft('close-reason'),'textarea')}<div class="action-row">${action('Close as cancelled','close',{outcome:'cancelled'},'danger',!r?'No request is open.':'')}${action('Close as rejected','close',{outcome:'rejected'},'danger',!r?'No request is open.':'')}</div><p class="reason">Stopping execution leaves the request open. Closure requires idle ownership and does not roll back changes.</p></div>`;}
function questionsPage(parts){const records=asArray(S.status.questions||S.status.question_history),selected=parts[1]?records.find(q=>q.id===parts[1]):null;if(parts[1]&&!selected)return unavailable(parts[1],'questions');return pageHead('Ask a question','Ask about the product without changing it. Answers are saved separately from change work.')+`<div class="grid wide-left"><div class="stack">${selected?`<div class="card"><div class="card-header"><h2>${esc(selected.title||selected.id)}</h2>${badge(selected.status||'Recorded')}</div><p class="readable">${esc(selected.question||selected.text)}</p>${selected.answer?markdown(selected.answer):'<p class="muted">No answer has been recorded yet.</p>'}<div class="action-row">${artifactLink('Open / download answer',selected.answer_path||selected.path)}${link('New question','questions','button-link')}</div><details><summary>Sources, observations, and uncertainty</summary><pre>${esc(pretty(selected.sources||selected))}</pre></details></div>`:`<div class="card"><div class="card-header"><h2>Question draft</h2><span id="draft-state-question">${dirtyState(['question-title','question-text'])}</span></div>${field('question-title','Question title (optional)',draft('question-title',S.status.draft?.question?.title||''))}${field('question-text','Your question (required)',draft('question-text',S.status.draft?.question?.text??S.status.draft?.question??''),'textarea','Ask about existing product behavior, implementation, or evidence. This lane cannot make product changes.')}${field('question-reference','Related request or prior answer (optional)',draft('question-reference'))}<details><summary>Optional attachments</summary><p class="hint">Inert text formats only; maximum 1 MiB each.</p><input type="file" aria-label="Add question attachments" data-upload="question-attachment" accept=".txt,.md,.json,.csv,.yaml,.yml" multiple ${busy()?'disabled':''}>${attachmentList(S.status.draft?.question?.attachments,'question')}</details><div class="action-row">${action('Save question draft','save-draft',{lane:'question'})}${action('Ask','ask',{},'primary',savedConstraint(['question-text','question-title','question-reference']))}</div><p class="reason">Ask starts a separate read-only action. It is available when the operational slot is idle, even if change work is blocked.</p></div>`}<div class="card"><h2>Read-only scope</h2><p class="muted">Answers preserve source references and uncertainties. Findings do not update product documentation or authorize implementation.</p><p class="hint">If a question requires modifications, use a change request explicitly. No product changes are authorized by Ask.</p></div></div><div class="card"><h2>Question history</h2>${records.length?records.map(q=>`<div class="run-row"><div>${link(q.title||q.question?.slice(0,70)||q.id,'questions/'+q.id)}<div class="hint">${date(q.created_at||q.created||q.time)}</div></div>${badge(q.status||'Recorded')}</div>`).join(''):'<p class="muted">Your submitted questions and recorded answers will appear here.</p>'}</div></div>`;}
function docsNodes(){const docs=S.status.documentation||{};return asArray(docs.nodes||docs.tree?.nodes||S.status.docs||docs.catalog).map(n=>({...n,path:n.path&&!n.path.startsWith('documentation/')&&!n.path.includes(':')?'documentation/'+n.path:n.path}));}
function documentationPage(parts){const nodes=docsNodes(),search=S.searches.docs||'',selected=parts[1]&&parts[1]!=='defects'?nodes.find(n=>(n.id||n.node_id)===decodeURIComponent(parts[1])):null,docs=S.status.documentation||{};if(parts[1]&&parts[1]!=='defects'&&!selected)return unavailable(parts[1],'documentation');const filtered=nodes.filter(n=>pretty(n).toLowerCase().includes(search.toLowerCase()));return pageHead('Product documentation','Current product knowledge, evidence, and known gaps.',button('Refresh documentation','documentation-form'))+`${asArray(docs.current_state_notices||S.status.current_state_notices).map(n=>notice(n.message||n.text||pretty(n),'warning')).join('')}<div class="card" style="margin-bottom:1rem"><div class="flex between"><div class="badge-row">${badge(docs.status||docs.baseline_status||'Baseline pending',docs.complete?'good':'warn')}${badge('Reconstruction: '+(docs.reconstruction_status||'specified, not demonstrated'))}</div>${link('Known defects','documentation/defects','subtle-link')}</div><p class="hint section-gap">${esc(docs.summary||'Documentation coverage records observed facts, inferences, provenance, and unknowns for each registered product area.')}</p>${workspaceRoots().length?table(['Product area','Access','Documentation coverage'],workspaceRoots().map(r=>[esc(r.name||r.id),badge(r.access),esc(r.baseline_status||r.baseline||r.documentation_status||'See baseline evidence')])):''}</div><div class="doc-grid"><aside class="card"><h2>Explore</h2><div class="field"><label for="docs-search">Search documentation</label><input type="search" id="docs-search" value="${esc(search)}" placeholder="Title, scope, or source…" data-search="docs"></div><ul class="tree">${filtered.map(n=>`<li>${link(n.title||n.name||n.id,'documentation/'+encodeURIComponent(n.id||n.node_id))}<div class="hint">${esc(n.applicability||n.scope||'Product knowledge')}</div></li>`).join('')||'<li class="hint">No matching documentation nodes.</li>'}</ul></aside><article class="card doc-body">${parts[1]==='defects'?knownDefects():selected?`<div class="breadcrumb">${link('Documentation','documentation')} / ${esc(selected.parent_title||selected.title||selected.id)}</div><div class="card-header"><h2>${esc(selected.title||selected.id)}</h2>${badge(selected.freshness||selected.status||'Recorded')}</div><p class="hint">${esc(selected.applicability||'Applicable to the documented product scope')} · Updated ${date(selected.updated_at||selected.updated)}</p>${selected.content||selected.markdown?markdown(selected.content||selected.markdown):S.detail?.id===(selected.id||selected.node_id)?markdown(S.detail.content):'<p class="muted">Select View document to read the stored content.</p>'}<div class="action-row">${selected.path?button('View document','read-document','',`data-path="${esc(selected.path)}" data-id="${esc(selected.id||selected.node_id)}"`):''}${artifactLink('Download document',selected.path)}</div><details><summary>Sources and provenance</summary><pre>${esc(pretty(selected.sources||selected.provenance||selected))}</pre></details>`:`<h2>Product knowledge, progressively</h2><p class="muted">Choose a document from the catalog to inspect its scope, evidence, and current content.</p>${docs.gaps?.length?notice('Outstanding gaps: '+docs.gaps.map(x=>x.description||x.message||pretty(x)).join('; '),'warning'):''}<div class="callout section-gap"><h3>Knowledge and verification are different</h3><p class="hint">Documentation records what was observed and inferred. Only current executed checks demonstrate verified behavior. Independent reconstruction remains specified but not demonstrated unless an actual exercise provides evidence.</p></div>`}</article></div>`;}
function knownDefects(){return `<h2>Known defects</h2><p class="muted">Catalog entries describe evidence and current disposition. They do not create a backlog or authorize repairs.</p>${table(['Defect','Status','Evidence / disposition'],asArray(S.status.known_defects||S.status.defects).map(d=>[esc(d.title||d.summary||d.symptoms||d.id),badge(d.confirmed?'Confirmed':d.status||'Suspected'),`<p>${esc(d.disposition||'Undecided')}</p><details><summary>View evidence</summary><pre>${esc(pretty(d))}</pre></details>${link('Review in current request','request/analysis','subtle-link')}`]))}`;}
function runsPage(parts){let runs=asArray(S.status.runs);const current=operation();if(current&&!runs.some(r=>r.id===current.id))runs=[current,...runs];const selected=parts[1]?runs.find(r=>r.id===parts[1]):current||runs[0];if(parts[1]&&!selected)return unavailable(parts[1],'runs');const filter=S.searches.runs||'',filtered=runs.filter(r=>pretty(r).toLowerCase().includes(filter.toLowerCase()));return pageHead('Runs and recovery','Observe execution, inspect evidence, and resume only after ownership is reconciled.',button('Refresh status','refresh'))+`<div class="grid wide-left"><div class="stack">${selected?`<div class="card"><div class="card-header"><h2>${esc(label(selected.action||selected.operation||selected.type||'Run'))}</h2>${badge(label(selected.status||'Recorded'))}</div><dl class="meta-list"><dt>Operation</dt><dd>${esc(selected.id)}</dd><dt>Owner</dt><dd>${esc(selected.owner||selected.request_id||selected.kind||'System operation')}</dd><dt>Started / ended</dt><dd>${date(selected.started_at||selected.started||selected.created_at||selected.created)} / ${date(selected.ended_at||selected.finished_at||selected.completed)}</dd><dt>Process outcome</dt><dd>${esc(selected.process_status||(selected.exit_code??selected.status)||'Not recorded')}</dd><dt>Task outcome</dt><dd>${esc(selected.task_outcome||selected.outcome||'See task evidence')}</dd><dt>Checkpoint</dt><dd>${esc(pretty(selected.checkpoint||'Not recorded'))}</dd><dt>Token usage</dt><dd>${esc(selected.usage?.total_tokens??'Unavailable')}</dd></dl><div class="action-row">${action('Stop execution','stop',{},'danger')}${action('Resume','resume',{operation_id:selected.id})}${action('Inspect recovery','recover',{inspect:true})}</div><details><summary>Segments, sources, and execution details</summary><pre>${esc(pretty(selected))}</pre></details></div><div class="card"><div class="card-header"><h2>Sanitized events</h2>${button(S.logFollow?'Pause log scrolling':'Follow latest','toggle-log')}</div><div id="live-events">${eventList(selected.events||S.status.events)}</div><div class="action-row">${artifactLink('Download sanitized log',selected.log_path||selected.log)}${artifactLink('Results summary',selected.results_path||selected.summary_path)}</div></div>${selected.handoff?handoffCard(selected.handoff):''}`:empty('No runs yet','Explicit actions create durable operation records before workers launch. Refreshing this page never starts work.')}<div class="card"><h2>Recovery is evidence based</h2><p class="muted">Age alone does not prove a process has stopped. Uncertain ownership blocks new operations until termination and recorded work have been reconciled.</p><div class="action-row">${action('Inspect diagnostics','doctor')}${action('Recover interrupted operation','recover')}</div></div></div><div class="card"><h2>Run history</h2><div class="field"><label for="runs-search">Filter runs</label><input id="runs-search" type="search" value="${esc(filter)}" data-search="runs" placeholder="Action, owner, or status…"></div>${filtered.map(r=>`<div class="run-row"><div>${link(label(r.action||r.operation||r.type||r.id),'runs/'+r.id)}<div class="hint">${esc(r.id)} · ${date(r.created_at||r.created||r.started_at||r.started)}</div></div>${badge(label(r.status||'Recorded'))}</div>`).join('')||'<p class="muted">No matching runs.</p>'}</div></div>`;}
function historyPage(parts){const tab=parts[1]==='ledger'?'ledger':'requests',records=asArray(S.status.history||S.status.closed_requests),events=asArray(S.status.ledger||S.status.decisions),list=tab==='ledger'?events:records,selected=parts[2]?list.find(r=>r.id===parts[2]||r.event_id===parts[2]):null,search=S.searches.history||'';if(parts[2]&&!selected)return unavailable(parts[2],'history/'+tab);return pageHead('History and decisions','Read archived requests and immutable decisions without reopening work.')+tabs([['requests','Closed requests'],['ledger','Changes and decisions']],'history',tab)+(selected?`<div class="card"><div class="card-header"><h2>${esc(selected.title||selected.summary||selected.id)}</h2>${badge('Historical record — read only')}</div><p class="hint">Historical passing evidence describes the checked content at that time. It is not current product verification.</p><pre>${esc(pretty(selected))}</pre>${selected.path?button('Read historical record','read-document','',`data-path="${esc(selected.path)}" data-id="${esc(selected.id)}"`):''}${S.detail?.id===selected.id?markdown(S.detail.content):''}<div class="action-row">${artifactLink('Download archived evidence',selected.path)}${link('Back to catalog','history/'+tab,'button-link')}</div></div>`:`<div class="card"><div class="filter-row"><label for="history-search">Search ${tab==='ledger'?'decisions':'closed requests'}</label><input id="history-search" type="search" value="${esc(search)}" data-search="history" placeholder="Title, outcome, ID, or date…"></div>${tab==='ledger'?table(['Event','Date / actor','Description'],events.filter(e=>pretty(e).toLowerCase().includes(search.toLowerCase())).map(e=>[link(e.type||e.event||e.id,'history/ledger/'+(e.id||e.event_id)),`${date(e.time||e.created_at||e.created)}<p class="hint">${esc(e.actor||e.source||'Framework')}</p>`,esc(e.summary||e.description||e.message||'Recorded state transition')])):table(['Request','Scope / result','Outcome','Closed'],records.filter(r=>pretty(r).toLowerCase().includes(search.toLowerCase())).map(r=>[`${link(r.title||r.id,'history/requests/'+r.id)}<p class="hint">${esc(r.id)}</p>`,esc(r.summary||r.scope||r.result||''),badge(label(r.outcome||r.status)),date(r.closed_at||r.closed)]),'No requests have been closed yet. There is no request backlog.')}</div>`);}
function unavailable(id,parent){return pageHead('This item is unavailable','The recorded reference could not be resolved. No other item has been substituted.')+`<div class="card"><p>Reference: <code>${esc(id)}</code></p>${link('Return to parent page',parent,'button-link')}</div>`;}
function settingsPage(parts){const section=parts[1]||S.settingsSection;S.settingsSection=settingsTabs.some(t=>t[0]===section)?section:'workspace';return pageHead('Settings','Deliberate workspace administration and preferences. Saving never launches product work.')+tabs(settingsTabs,'settings',S.settingsSection)+({workspace:workspaceSettings,profiles:profileSettings,skills:skillsSettings,product:productSettings,instructions:instructionsSettings,appearance:appearanceSettings}[S.settingsSection])();}
function workspaceSettings(){const roots=workspaceRoots(),rev=S.status.workspace?.revision||S.status.config?.workspace?.revision||'Not recorded';return `<div class="card"><div class="card-header"><h2>Registered product folders</h2>${badge('Revision '+rev)}</div><p class="muted">One framework home owns all product state. Additional disjoint folders are registered explicitly; neighboring folders remain outside the workspace.</p>${table(['Identity / purpose','Canonical path','Access / availability','Administration'],roots.map(r=>[`${esc(r.name||r.id)}<p class="hint">${esc(r.id)} · ${esc(r.purpose||'Product source')}</p>`,`<code>${esc(r.path||r.location)}</code>`,`${badge(r.access||'read-write')}<p class="hint">${esc(r.availability||r.status||'Registered')}</p>`,`${button('Edit','edit-root','btn-small',`data-id="${esc(r.id)}" ${busy()?'disabled':''}`)} ${r.id!=='home'?button('Remove membership','remove-root','btn-small',`data-id="${esc(r.id)}" ${busy()?'disabled':''}`):'<span class="hint">Mandatory home</span>'}`]))}<p class="reason">Removing membership never deletes files or unresolved test obligations. Location changes update the mapping without moving product content.</p></div><div class="card section-gap"><h2 id="root-form-title">Add or edit a folder</h2>${field('root-id','Stable folder ID (required)',draft('root-id'),'text','Lowercase identifier. Existing folder IDs remain stable across deliberate relocation.')}${field('root-name','Display name (required)',draft('root-name'))}${field('root-path','Canonical folder path (required)',draft('root-path'),'text','An explicitly selected absolute local path. This field does not grant browsing or write access before validation.')}${field('root-purpose','Product purpose (required)',draft('root-purpose'))}${choice('root-access','Access mode',[['read-write','Read and write'],['read-only','Read only']],draft('root-access','read-write'))}<div class="action-row">${action('Validate candidate','workspace-validate')}${action('Save workspace folder','workspace-save',{},'primary')}${button('Clear form','clear-root','',busy()?'disabled':'')}</div><p class="settings-boundary">Effective after this idle transaction. Workspace changes mark affected scope, plans, approval, tests, and documentation for reassessment. Newly included areas require explicit documentation baselining. Saving never starts an agent.</p></div>${S.status.workspace_reconciliation?artifactPanel('Pending workspace reconciliation',S.status.workspace_reconciliation):''}`;}
function profileSettings(){const profiles=asArray(S.status.profiles||S.status.config?.profiles),defaultId=S.status.config?.default_profile||S.status.default_profile;return `<div class="card"><h2>Agent profiles</h2><p class="muted">Profiles select explicit adapters and settings. Credentials stay in the environment; configuration stores references only.</p>${table(['Profile','Adapter / model','Diagnostics','Actions'],profiles.map(p=>[`${esc(p.name||p.id)}${p.id===defaultId?' '+badge('Default','accent'):''}`,`${esc(p.adapter||'Not configured')}<p class="hint">${esc(p.model||'Adapter default')}</p>`,`${esc(p.compatibility||p.status||'Not checked')}<p class="hint">Authentication: ${esc(p.authentication||p.auth_status||'Not checked')}</p>`,`${button('Edit','edit-profile','btn-small',`data-id="${esc(p.id)}" ${busy()?'disabled':''}`)} ${action('Run diagnostics','profile-diagnose',{profile_id:p.id},'btn-small')}`]),'No profiles configured. Add an authenticated compatible agent to run semantic work.')}</div><div class="grid section-gap"><div class="card"><h2>Profile settings</h2>${field('profile-id','Profile ID (required)',draft('profile-id'))}${field('profile-name','Display name',draft('profile-name'))}${choice('profile-adapter','Adapter',[['codex','Codex CLI'],['claude','Claude Code CLI'],['manual','Manual handoff']],draft('profile-adapter','codex'))}${field('profile-model','Model (optional)',draft('profile-model'))}${field('profile-timeout','Timeout in seconds',draft('profile-timeout','1800'),'number')}${field('profile-credential','Credential environment variable (optional)',draft('profile-credential'),'text','Reference name only, for example OPENAI_API_KEY. Never paste its value.')}${choice('profile-default','Use as default',[['no','Keep existing default'],['yes','Set this profile as default']],draft('profile-default','no'))}<div class="action-row">${action('Save profile','profile-save',{},'primary')}</div><p class="settings-boundary">Applies to the next explicitly accepted action or reconciled execution segment. Historical runs retain their recorded effective profile.</p></div><div class="stack"><div class="card"><h2>Privileged local settings</h2><p class="hint">Executable and permission settings require deliberate human administration. Ordinary product content cannot supply them.</p>${field('profile-executable','Configured executable path',draft('profile-executable'),'text','Use the installed supported CLI executable. Saving does not install or run it.')}${choice('profile-permissions','Permission mode',[['workspace-write','Registered writable workspace'],['read-only','Read only']],draft('profile-permissions','workspace-write'))}${choice('profile-network','Network access',[['no','Disabled'],['yes','Explicitly allowed for agent service']],draft('profile-network','no'))}<p class="hint">These settings are included only when you explicitly save this profile.</p></div><div class="card"><h2>Capability assignments</h2><p class="hint">Explicit capability assignments take precedence over the product default. No automatic fallback to another installed executable occurs.</p>${field('profile-capabilities','Assignments (JSON object)',draft('profile-capabilities',pretty(S.status.config?.capability_profiles||S.status.config?.capabilities||{})),'textarea','Map supported capability names to configured profile IDs.')}<div class="action-row">${action('Save capability assignments','profile-save',{assignments_only:true})}</div></div></div></div>`;}
function skillsSettings(){const skills=asArray(S.status.skills),search=S.searches.skills||'';return `<div class="card"><div class="card-header"><h2>Installed skills</h2>${action('Validate skills','validate',{target:'skills'})}</div><p class="muted">Metadata is discovered without loading skill bodies. Enablement affects future execution boundaries; it does not invoke a skill or authorize work.</p><div class="field"><label for="skills-search">Search skills</label><input type="search" id="skills-search" data-search="skills" value="${esc(search)}" placeholder="Purpose, package, or capability…"></div></div><div class="skill-list section-gap">${skills.filter(s=>pretty(s).toLowerCase().includes(search.toLowerCase())).map(s=>`<div class="card"><div class="card-header"><h3>${esc(s.name||s.id)}</h3>${badge(s.enabled?'Enabled':'Disabled',s.enabled?'good':'')}</div><p class="muted">${esc(s.description||s.purpose||'See installed skill metadata.')}</p><p class="hint">Version ${esc(s.version||'Not recorded')} · ${esc(s.compatibility||s.status||'Installed')}</p><details><summary>Dependencies, input/output, and availability</summary><pre>${esc(pretty(s))}</pre>${artifactLink('Usage details',s.usage_path||s.readme_path)}</details>${action(s.enabled?'Disable':'Enable','settings-save',{skills:{[s.id||s.name]:!s.enabled}},'')}</div>`).join('')||empty('No matching skills','Inspect framework validation diagnostics to identify missing, malformed, duplicate, or incompatible packages.')}</div>`;}
function productSettings(){const config=S.status.config||{};return `<div class="grid"><div class="card"><h2>Product settings</h2>${field('product-name','Product display name',draft('product-name',config.product_name||S.status.product?.name||''))}${field('product-repair-budget','Default repair attempt budget',draft('product-repair-budget',String(config.repair_budget?.max_cycles??config.repair_budget??3)),'number')}${field('product-doc-limit','Documentation catalog child limit',draft('product-doc-limit',String(config.documentation?.max_children??12)),'number')}${field('product-retention','Retain operation history (days)',draft('product-retention',String(config.retention?.verbose_days??config.retention_days??365)),'number')}<div class="action-row">${action('Save product settings','settings-save',{section:'product'},'primary')}</div><p class="settings-boundary">Supported declarative settings are validated before saving. Changes affect subsequent operations. Existing authorizations and evidence remain subject to their original scope.</p></div><div class="card"><h2>Core maintenance</h2><p class="muted">Core repair and upgrades require no open request and no operational owner. A paused, blocked, or Ready to close request still counts as open.</p><div class="action-row">${action('Validate framework and state','validate')}${action('Inspect diagnostics','doctor')}${action('Clean abandoned inert temporary data','cleanup',{},'',active()?'Close the current request before core maintenance.':'')}</div><p class="hint section-gap">Ordinary runs do not modify the installed core. Core upgrade instructions are available in the maintained guide.</p>${link('Maintenance and upgrades','help/maintenance','subtle-link')}</div></div>`;}
function instructionsSettings(){const value=S.status.instructions?.text||S.status.instructions||'';return `<div class="card"><h2>Human-owned instructions</h2><p class="muted">Only deliberate human edits change these instructions. Agent suggestions are separate outputs and cannot apply themselves.</p>${field('instructions-text','Product custom instructions (Markdown)',draft('instructions-text',value),'textarea','Behavioral supplements or explicit overrides cannot bypass schemas, permissions, approval, or validation contracts.')}<div class="action-row">${action('Save my edits','instructions-save',{},'primary')}${button('Preview instructions','preview-instructions')}</div><p class="settings-boundary">Effective on the next explicit action or acknowledged execution segment after reassessment. Saving does not change the instructions of a live operation.</p><details><summary>Current instruction revisions</summary><pre>${esc(pretty(S.status.instructions?.files||S.status.instruction_versions||{}))}</pre></details></div>`;}
function appearanceSettings(){const original=S.status.config?.appearance||{theme:'clear',size:16},prefs=S.themeDraft||JSON.parse(JSON.stringify(typeof original==='string'?{theme:original,size:16}:original)),selected=prefs.theme||'clear',effective=selected==='custom'?prefs.custom:themes[selected]||themes.clear;S.themeDraft=prefs;return `<div class="card"><div class="card-header"><h2>Appearance</h2>${S.dirty.has('appearance')?badge('Unsaved preview','warn'):badge('Saved appearance')}</div><p class="muted">Choose a local font and color palette. Appearance changes do not affect scope, plan authorization, evidence freshness, or agent behavior.</p><div class="theme-grid">${Object.entries(themes).map(([id,t])=>`<button class="theme-card" data-local="select-theme" data-theme="${id}" aria-pressed="${selected===id}" style="--preview-bg:${t.colors.bg};--preview-text:${t.colors.text};--preview-surface:${t.colors.surface};--preview-accent:${t.colors.accent};--preview-on:${t.colors['on-accent']};--preview-font:${fonts[t.font]}" ${busy()?'disabled':''}><span class="sample"><strong>AI Harness</strong><span class="sample-line">Your product workspace</span><span class="sample-button">Next action</span></span><span class="theme-label"><strong>${t.name}</strong><br><span class="hint">${t.description}</span></span></button>`).join('')}</div><div class="field-row">${button('Use system appearance','system-theme','',busy()?'disabled':'')}${button('Customize current theme','customize-theme','',busy()?'disabled':'')}</div><div class="field"><label for="appearance-size">Base text size: <output id="appearance-size-value">${Number(prefs.size)||16}</output> px</label><div class="field-row"><input type="range" id="appearance-size" min="14" max="22" value="${Number(prefs.size)||16}" data-theme-size ${busy()?'disabled':''}><span class="hint">14–22 px · browser zoom remains available</span></div></div>${selected==='custom'&&effective?`<div class="separator"></div><h3>Custom product-owned preset</h3>${field('theme-name','Preset name',draft('theme-name',effective.name||'My theme'))}${choice('theme-font','Body font',Object.keys(fonts).filter(x=>x!=='mono'),draft('theme-font',effective.font||'system'))}${choice('theme-heading','Heading font',Object.keys(fonts).filter(x=>x!=='mono'),draft('theme-heading',effective.heading||'system'))}<div class="colors">${Object.entries(effective.colors||themes.clear.colors).map(([key,value])=>`<div class="color-field"><input id="color-${key}" type="color" value="${esc(value)}" data-theme-color="${key}" ${busy()?'disabled':''}><label for="color-${key}">${esc(label(key))}</label></div>`).join('')}</div><div class="action-row">${button('Reset draft to Clear preset','reset-theme','',busy()?'disabled':'')}${button('Delete custom theme','delete-theme','danger',busy()?'disabled':'')}</div>`:''}<div class="action-row">${button('Preview changes','preview-theme','',busy()?'disabled':'')}${action('Save appearance','settings-save',{section:'appearance'},'primary')}${button('Discard preview','discard-theme','',busy()?'disabled':'')}</div><p class="reason">Preview changes are local and unsaved. Save validates semantic colors, local font presets, text size, and selected contrast pairs.</p></div><div class="card section-gap"><h2>Component preview</h2><div class="font-preview"><div class="preview-type">A clear view of what comes next.</div><p>Readable text for requirements, decisions, and evidence.</p><div class="badge-row">${badge('Current passed','good')}${badge('Needs review','warn')}${badge('Failed check','bad')}</div><div class="action-row"><button class="primary" type="button">Primary action preview</button><button type="button" disabled>Unavailable action</button><a href="#help/appearance">Example link</a></div><p class="hint section-gap">Code and evidence: <code>home:src/application.py</code></p></div></div>`;}
function helpPage(parts){const help=typeof S.help==='string'?S.help:S.help.markdown||S.help.content||'',query=S.searches.help||'',headings=Array.from(help.matchAll(/^(#{1,3})\s+(.+)$/gm)).map(m=>({title:m[2],id:m[2].toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')})),anchor=parts.slice(1).join('/')||'quick-start';const filtered=query?help.split(/(?=^#{1,3} )/m).filter(section=>section.toLowerCase().includes(query.toLowerCase())).join('\n'):help;return pageHead('Help','Learn the workflow and find instructions for your current step.',`<label for="help-platform" class="hint">Command examples</label><select id="help-platform" data-help-platform><option value="linux" ${S.helpPlatform==='linux'?'selected':''}>Linux</option><option value="windows" ${S.helpPlatform==='windows'?'selected':''}>Windows</option></select>${button('Copy CLI help command','copy-command','', 'data-command="'+(S.helpPlatform==='windows'?'py':'python3')+' -B .aih/engine/cli.py --help"')}${button('Open quick start','quickstart')}`)+`<div class="doc-grid"><aside class="card"><h2>User guide</h2><div class="field"><label for="help-search">Search the installed guide</label><input type="search" id="help-search" data-search="help" value="${esc(query)}" placeholder="Clarify, workspace, recovery…"></div><ul class="tree">${headings.filter(h=>!query||h.title.toLowerCase().includes(query.toLowerCase())).map(h=>`<li>${link(h.title,'help/'+h.id)}</li>`).join('')}</ul><p class="hint">Guide/core version ${esc(S.help.version||S.status.version||S.status.core_version||'Installed')}</p>${link('Return to Overview','overview','subtle-link')}</aside><article class="card doc-body">${help?markdown(filtered||'No guide sections match this search.'):notice('The maintained user guide is not available from this installation. Inspect framework validation diagnostics.','warning')}<details class="section-gap"><summary>Shared operation catalog</summary><p class="hint">The portal and CLI dispatch these same typed Python operations.</p>${table(['Operation','Inputs / effects','Help'],S.catalog.map(o=>[`<code>${esc(o.id||o.operation)}</code>`,`${esc(o.description||o.label||o.effects||'')}<details><summary>Contract</summary><pre>${esc(pretty(o))}</pre></details>`,link('Guide topic','help/'+String(o.help||o.help_anchor||'commands').replace(/^#/,''))]))}</details></article></div>`;}
function render(){const previousRoute=$('#main').dataset.route;if(previousRoute)S.expanded[previousRoute]=$$('#main details[open]').map(d=>d.querySelector('summary')?.textContent);renderShell();const parts=S.route.split('/');const renderers={overview,request:requestPage,questions:questionsPage,documentation:documentationPage,runs:runsPage,history:historyPage,settings:settingsPage,help:helpPage};$('#main').innerHTML=(renderers[parts[0]]||(()=>unavailable(S.route,'overview')))(parts)+`<p class="footer-note">AI Harness · Local product workspace · ${esc(S.status.version||S.status.core_version||'Installed core')} · ${link('Guide and limitations','help/limitations')}</p>`;$('#main').dataset.route=S.route;const opened=S.expanded[S.route]||[];$$('#main details').forEach(d=>{if(opened.includes(d.querySelector('summary')?.textContent))d.open=true;});applyAvailability();if(parts[0]==='help'&&parts[1])setTimeout(()=>$('#guide-'+CSS.escape(parts[1]))?.scrollIntoView({block:'start'}),30);}
function applyAvailability(){const isBusy=busy();$$('[data-draft], [data-upload], [data-theme-size], [data-theme-color], [data-question-choice], [data-question-answer], [data-question-respondent], [data-local="select-theme"]').forEach(el=>el.disabled=isBusy&&el.id!=='external-evidence');$$('[data-operation]').forEach(el=>{const a=allowed(el.dataset.operation),constraint=el.dataset.constraint||'',runDirty=['clarify','analyze','implement','implement-directly'].includes(el.dataset.operation)?savedConstraint(['change-text','change-title','questionnaire-text']):el.dataset.operation==='ask'?savedConstraint(['question-text','question-title','question-reference']):'',reason=!a.ok?a.reason:runDirty||constraint;el.disabled=!!reason;const holder=el.closest('.operation-control');if(holder){let explanation=holder.querySelector('.reason');if(reason&&!explanation){explanation=document.createElement('span');explanation.className='reason';holder.appendChild(explanation);}if(explanation){explanation.textContent=reason||'';explanation.hidden=!reason;}}});$$('[data-local="edit-root"],[data-local="remove-root"],[data-local="edit-profile"],[data-local="clear-root"],[data-local="customize-theme"],[data-local="system-theme"],[data-local="preview-theme"],[data-local="discard-theme"],[data-local="discard-change"],[data-local="reset-theme"],[data-local="delete-theme"],[data-local="documentation-form"]').forEach(el=>el.disabled=isBusy);}
async function api(path,options={}){const response=await fetch(path,{cache:'no-store',...options,headers:{'Accept':'application/json',...(options.body?{'Content-Type':'application/json'}:{}),...(S.status.csrf_token?{'X-CSRF-Token':S.status.csrf_token,'X-AIH-Token':S.status.csrf_token}:{}),...options.headers}});const type=response.headers.get('content-type')||'';let result=type.includes('json')?await response.json():await response.text();if(!response.ok||result?.ok===false){const error=new Error(typeof result==='string'?result:result.error?.message||result.error||result.message||'The operation failed.');error.status=response.status;error.result=result;throw error;}return result;}
function normalizeStatus(raw){let s=raw.status&&typeof raw.status==='object'&&!raw.active_request?{...raw,...raw.status}:raw;if(s.data&&typeof s.data==='object'&&s.revision===undefined)s=s.data;for(const lane of ['change','question']){if(typeof s.draft?.[lane]==='string')s.draft[lane]={text:s.draft[lane],attachments:s.attachments?.[lane]||[]};}const r=s.active_request;const docs=s.documentation||{};s.documentation={...docs,status:docs.status||docs.baseline?.status,complete:docs.baseline?.status==='complete',current_state_notices:docs.notices||docs.current_state_notices};s.known_defects=docs.known_defects?.defects||s.known_defects||[];s.defects=s.known_defects;s.workspace_reconciliation=s.state?.workspace_impacts||s.workspace_reconciliation;s.current_state_notices=docs.notices||s.current_state_notices;if(r){s.plan={...(r.plan_data||{}),revision:r.plan_revision,markdown:typeof r.plan==='string'?r.plan:undefined,tasks:r.tasks||r.plan_data?.tasks||[],approval:r.approval};s.documentation_increment=r.documentation_increment;s.interpretation=r.interpretation;s.assessment=r.assessment;s.results_summary=r.results_summary||r.implementation_summary;s.receipts=s.receipts?.map(x=>({...x,current_review_revision:r.review_revision}));}return s;}
async function refresh(force=false){if(S.polling)return;S.polling=true;try{const result=normalizeStatus(await api('/api/status'));const previous=S.status.revision;S.status=result;if(previous!==result.revision)Object.keys(S.drafts).filter(k=>!S.dirty.has(k)).forEach(k=>delete S.drafts[k]);S.online=true;$('#connection').hidden=true;if(result.available_actions&&Array.isArray(result.available_actions)&&result.available_actions[0]?.id)S.catalog=S.catalog.map(c=>({...c,...result.available_actions.find(a=>a.id===c.id)}));if(!S.dirty.size&&!S.themeDraft)applyTheme();if(force||previous===undefined||(!S.dirty.size&&!document.activeElement?.matches('input,textarea,select')&&!$('#modal').open&&previous!==result.revision))render();else{renderShell();applyAvailability();const live=$('#live-events');if(live&&!S.dirty.size){const oldScroll=live.querySelector('.log')?.scrollTop||0;const selected=S.route.startsWith('runs/')?asArray(result.runs).find(r=>r.id===S.route.split('/')[1]):result.current_run||asArray(result.runs)[0];live.innerHTML=eventList(selected?.events||result.events);const log=live.querySelector('.log');if(log)log.scrollTop=S.logFollow?log.scrollHeight:oldScroll;}}S.lastRevision=result.revision;}catch(error){S.online=false;$('#connection').hidden=false;$('#connection').textContent='Status connection interrupted. Your unsaved edits are preserved. '+error.message;if(force&&!S.status.revision){renderShell();$('#main').innerHTML=pageHead('Workspace unavailable','The local server did not return a valid status.')+notice(error.message,'error')+button('Retry status','refresh');}}finally{S.polling=false;}}
function value(id){return document.getElementById(id)?.value??S.drafts[id]??'';}
function receiptPayload(payload){
 const receipt=asArray(S.status.receipts).find(r=>r.id===payload.receipt_id),decisions=[],seen=new Set();
 for(const [i,m] of asArray(receipt?.rows||receipt?.matches||receipt?.answers||receipt?.mappings).entries()){
  const key=`receipt-${receipt.id}-${i}`,choice=value(key+'-choice');
  if(!choice||choice==='unresolved')continue;
  const question_id=value(key+'-target');
  if(!question_id){if(choice==='retain')continue;throw new Error('Match each accepted or edited answer to a current question.');}
  if(seen.has(question_id))throw new Error('Choose one decision for '+question_id+' and leave its other duplicate rows unresolved.');
  seen.add(question_id);
  decisions.push({question_id,source_question_id:m.question_id||m.id,resolve_conflict:choice==='edit',answer:value(key+'-answer'),selected:value(key+'-selected').split('\n').map(x=>x.trim()).filter(Boolean),comments:value(key+'-comments'),choice});
 }
 return {...payload,decisions};
}
function rootPayload(removeId){let roots=workspaceRoots().map(({id,name,path,location,purpose,access})=>({id,name,path:path||location,purpose,access}));if(removeId)return{roots:roots.filter(r=>r.id!==removeId)};const candidate={id:value('root-id').trim(),name:value('root-name').trim(),path:value('root-path').trim(),purpose:value('root-purpose').trim(),access:value('root-access')};if(!candidate.id||!candidate.name||!candidate.path||!candidate.purpose)throw new Error('Provide the folder ID, display name, canonical path, and purpose.');const index=roots.findIndex(r=>r.id===candidate.id);if(index>=0)roots[index]=candidate;else roots.push(candidate);return{roots};}
function collectPayload(id,provided={}){let payload={...provided};switch(id){case 'save-draft':payload={...payload,text:value(payload.lane==='question'?'question-text':'change-text'),title:value(payload.lane==='question'?'question-title':'change-title')};if(payload.lane==='question')payload.reference=value('question-reference');break;case 'save-questionnaire':payload.text=value('questionnaire-text');break;case 'receive-answers':payload.text=value('returned-answers');break;case 'review-answers':case 'import-and-clarify':payload=receiptPayload(payload);break;case 'save-amendment':if(!payload.id)payload={...payload,title:value('amendment-title'),text:value('amendment-text'),source:'framework user',reason:value('amendment-reason'),...(value('amendment-supersedes')?{supersedes:value('amendment-supersedes')}:{})};break;case 'defect-disposition':payload.disposition=value('defect-'+payload.id);payload.reason=value('defect-reason-'+payload.id);break;case 'extend-repair-budget':payload.additional=Number(value('repair-count-'+payload.failure_id));payload.reason=value('repair-reason-'+payload.failure_id);break;case 'workspace-save':case 'workspace-validate':if(!payload.roots)payload=rootPayload();break;case 'profile-save':{let assignments;try{assignments=JSON.parse(value('profile-capabilities')||'{}');}catch{throw new Error('Capability assignments must be a JSON object.');}if(payload.assignments_only){const pid=value('profile-id')||S.status.config?.default_profile,profile=asArray(S.status.profiles||S.status.config?.profiles).find(p=>p.id===pid);if(!profile)throw new Error('Select or configure a profile before saving assignments.');payload={profile_id:pid,profile,capabilities:assignments};break;}const profile={name:value('profile-name'),adapter:value('profile-adapter'),model:value('profile-model'),timeout:Number(value('profile-timeout')||1800),credential_env:value('profile-credential').split(/[\s,]+/).filter(Boolean),executable:value('profile-executable'),permissions:{product_write:value('profile-permissions')==='workspace-write',network:value('profile-network')==='yes'}};payload={profile_id:value('profile-id'),profile,default:value('profile-default')==='yes',capabilities:assignments};break;}case 'settings-save':if(payload.section==='appearance'){const prefs=S.themeDraft||{theme:'clear',size:16};if(prefs.theme==='custom'){prefs.custom.name=value('theme-name');prefs.custom.font=value('theme-font');prefs.custom.heading=value('theme-heading');validateTheme(prefs.custom);}payload={appearance:prefs};}else if(payload.section==='product')payload={product_name:value('product-name'),repair_budget:{max_cycles:Number(value('product-repair-budget'))},documentation:{...(S.status.config?.documentation||{}),max_children:Number(value('product-doc-limit'))},retention:{...(S.status.config?.retention||{}),verbose_days:Number(value('product-retention'))}};break;case 'instructions-save':payload.text=value('instructions-text');break;case 'close':if(payload.outcome!=='completed'){payload.reason=value('close-reason');if(!payload.reason.trim())throw new Error('Provide a reason before closing without completion.');}break;case 'reverse-engineer':if(active())payload.within_request=true;break;case 'handoff':if(active()&&payload.action==='reverse-engineer')payload.within_request=true;payload.instruction=payload.instruction||'Perform only the identified submitted action and scoped plan. Preserve request/scope/workspace references, source fingerprints, task checkpoints, test evidence, documentation impact, and a concise results summary. Stop and return evidence before releasing this external reservation.';break;case 'ask':payload.reference=value('question-reference');break;}return payload;}
function changedFields(id,payload){const map={'save-draft':payload.lane==='question'?['question-text','question-title','question-reference']:['change-text','change-title'],'save-questionnaire':['questionnaire-text'],'receive-answers':['returned-answers'],'save-amendment':['amendment-title','amendment-text','amendment-reason','amendment-supersedes'],'instructions-save':['instructions-text'],'workspace-save':['root-id','root-name','root-path','root-purpose','root-access'],'profile-save':['profile-id','profile-name','profile-adapter','profile-model','profile-timeout','profile-credential','profile-executable','profile-permissions','profile-network','profile-default','profile-capabilities'],'settings-save':payload.appearance?['appearance','theme-name','theme-font','theme-heading']:['product-name','product-repair-budget','product-doc-limit','product-retention'],'defect-disposition':['defect-'+payload.id,'defect-reason-'+payload.id]};if(['review-answers','import-and-clarify'].includes(id))return Object.keys(S.drafts).filter(k=>k.startsWith('receipt-'+payload.receipt_id));return map[id]||[];}
const successMessages={'save-draft':'Draft saved. Nothing has been submitted or started.','save-questionnaire':'Questionnaire draft saved. Submit through Clarify or Analyze when ready.','save-amendment':'Amendment saved with its immutable revision history.','review-answers':'Reviewed answers saved as drafts. No clarification run started.','receive-answers':'Returned form received. Review the answer mappings before applying them.','approve':'The specified plan revision was approved. Implementation has not started.','workspace-save':'Workspace saved. Affected evidence requires explicit reassessment; no agent was started.','instructions-save':'Your instructions were saved for the next explicit execution boundary.','profile-save':'Profile configuration saved. No agent was started.','stop':'Stop requested. Ownership is retained until termination and reconciliation are confirmed.'};
async function perform(id,provided={},options={}){if(S.posting)return false;if(!allowed(id).ok){toast(allowed(id).reason);return false;}let payload;try{payload=options.raw?{...provided}:collectPayload(id,provided);if(S.runProfile&&!payload.profile&&['clarify','analyze','implement','implement-directly','resume','reverse-engineer','ask','import-and-clarify','verify','run-tests','handoff'].includes(id))payload.profile=S.runProfile;}catch(e){toast(e.message);return false;}if(!S.online){toast('Refresh status before an operational action; the local connection is unavailable.');return false;}const revision=S.dirty.size&&S.editRevision!==undefined&&!options.currentRevision?S.editRevision:S.status.revision,key=crypto.randomUUID?crypto.randomUUID():Date.now()+'-'+Math.random().toString(36).slice(2);S.posting=true;renderShell();applyAvailability();try{const result=await api('/api/operation',{method:'POST',body:JSON.stringify({operation:id,expected_revision:revision,idempotency_key:key,payload})});S.posting=false;changedFields(id,payload).forEach(k=>S.dirty.delete(k));if(id==='settings-save'&&payload.appearance){S.themeDraft=null;applyTheme(payload.appearance);}await refresh(true);S.editRevision=S.dirty.size?S.status.revision:undefined;const operationId=result.operation_id||result.operation?.id||result.run_id||result.id;let message=result.message||successMessages[id]||(id==='settings-save'?'Settings saved. Workflow authority is unchanged.':label(id)+' accepted. Inspect the recorded outcome and evidence.');if(operationId)message+=' Reference: '+operationId+'.';toast(message);if(id==='stop'&&(result.requires_confirmation||result.external_confirmation_required||result.status==='external-stop-awaiting-confirmation'||S.status.operation?.status==='external_stop_awaiting_confirmation'))externalStopDialog();if(['doctor','validate','workspace-validate','profile-diagnose','recover'].includes(id))dialog('Operation result',`<pre>${esc(pretty(result))}</pre>`,[{label:'Close',run:closeDialog}]);return true;}catch(error){S.posting=false;await refresh(false);renderShell();applyAvailability();if(error.status===409||['revision_conflict','revision-conflict'].includes(error.result?.error?.code)||error.result?.code==='revision_conflict')conflictDialog(error.result);else dialog('The operation was not completed',notice(error.message,'error')+`<p class="hint">Operation: ${esc(id)}. Inspect current status before repeating. Your unsaved local edits are preserved.</p><details><summary>Recovery details</summary><pre>${esc(pretty(error.result||{}))}</pre></details>`,[{label:'Keep editing',run:()=>{closeDialog();render();}},{label:'Refresh status',run:()=>{closeDialog();refresh(true);}}]);return false;}finally{S.posting=false;}}
function conflictDialog(result){const local=Object.fromEntries([...S.dirty].map(k=>[k,S.drafts[k]])),saved=result?.saved||result?.error?.details?.current||S.status.draft||S.status;dialog('Revision conflict — your edits are preserved',`<p>Another saved revision changed while this editor was open. Review both versions before a new save.</p><div class="diff-grid"><section><h3>Your draft</h3><pre>${esc(pretty(local))}</pre></section><section><h3>Saved version</h3><pre>${esc(pretty(saved))}</pre></section></div><p class="hint">Draft revision ${esc(S.editRevision)} · saved revision ${esc(S.status.revision)}. Continuing creates an editable merged draft; no force overwrite is performed.</p>`,[{label:'Use saved version',run:()=>{S.dirty.forEach(k=>delete S.drafts[k]);S.dirty.clear();S.editRevision=undefined;closeDialog();render();}},{label:'Continue editing a merged draft',class:'primary',run:()=>{S.editRevision=S.status.revision;closeDialog();render();toast('Review and merge the competing content before saving with the new revision.');}},{label:'Download my draft',run:()=>download('aih-unsaved-draft.json',pretty(local))}]);}
function download(name,text){const blob=new Blob([text],{type:'text/plain;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),5000);}
function externalStopDialog(){dialog('Confirm external worker termination','<p>Confirm only after the external worker has stopped or never started. Ownership remains reserved until returned evidence is reconciled.</p>'+field('external-evidence','Returned evidence (JSON object or array, optional)',draft('external-evidence','[]'),'textarea').replace('disabled',''),[{label:'Keep reservation',run:closeDialog},{label:'Confirm stopped and reconcile',class:'danger',run:async()=>{let evidence;try{evidence=JSON.parse(value('external-evidence')||'[]');}catch{toast('Provide returned evidence as valid JSON.');return;}closeDialog();await perform('stop',{confirm_external_stopped:true,evidence},{raw:true,currentRevision:true});}}]);}
function luminance(color){const c=color.slice(1).match(/.{2}/g).map(x=>parseInt(x,16)/255).map(v=>v<=.04045?v/12.92:((v+.055)/1.055)**2.4);return c[0]*.2126+c[1]*.7152+c[2]*.0722;}
function validateTheme(theme){for(const [a,b,min] of [['text','bg',4.5],['text','surface',4.5],['muted','surface',4.5],['on-accent','accent',4.5],['focus','surface',3],['positive','surface',4.5],['warning','surface',4.5],['danger','surface',4.5]]){if(!/^#[\da-f]{6}$/i.test(theme.colors[a]||'')||!/^#[\da-f]{6}$/i.test(theme.colors[b]||''))throw new Error('Use valid six-digit semantic color values.');const values=[luminance(theme.colors[a]),luminance(theme.colors[b])].sort((x,y)=>y-x),ratio=(values[0]+.05)/(values[1]+.05);if(ratio<min)throw new Error(`${label(a)} on ${label(b)} has contrast ${ratio.toFixed(2)}:1; at least ${min}:1 is required. Adjust the draft colors.`);}return true;}
function markDirty(id,val){if(busy())return;S.drafts[id]=val;if(!S.dirty.size)S.editRevision=S.status.revision;S.dirty.add(id);if(id.startsWith('change-'))$('#draft-state-change')&&( $('#draft-state-change').innerHTML=badge('Unsaved changes','warn'));if(id.startsWith('question-'))$('#draft-state-question')&&( $('#draft-state-question').innerHTML=badge('Unsaved changes','warn'));if(['change-text','change-title','question-text','question-title','question-reference','questionnaire-text'].includes(id))$$('[data-operation="clarify"],[data-operation="analyze"],[data-operation="implement"],[data-operation="implement-directly"],[data-operation="ask"]').forEach(el=>{el.disabled=true;const holder=el.closest('.operation-control');if(!holder.querySelector('.reason')){const p=document.createElement('span');p.className='reason';p.textContent='Save these edits before submitting.';holder.appendChild(p);}});}
async function saveAllDrafts(){const entries=[{keys:['change-text','change-title'],id:'save-draft',payload:{lane:'change'}},{keys:['question-text','question-title','question-reference'],id:'save-draft',payload:{lane:'question'}},{keys:['questionnaire-text'],id:'save-questionnaire'},{keys:['amendment-title','amendment-text','amendment-reason','amendment-supersedes'],id:'save-amendment'},{keys:['instructions-text'],id:'instructions-save'},{keys:['appearance','theme-name','theme-font','theme-heading'],id:'settings-save',payload:{section:'appearance'}},{keys:['root-id','root-name','root-path','root-purpose','root-access'],id:'workspace-save'},{keys:['profile-id','profile-name','profile-adapter','profile-model','profile-timeout','profile-credential','profile-executable','profile-permissions','profile-network','profile-default','profile-capabilities'],id:'profile-save'},{keys:['product-name','product-repair-budget','product-doc-limit','product-retention'],id:'settings-save',payload:{section:'product'}}];for(const e of entries){if(e.keys.some(k=>S.dirty.has(k))&&!await perform(e.id,e.payload||{}))return false;}if(S.dirty.size){toast('Save the remaining edited form with its specific action before leaving.');return false;}return true;}
function navigate(target){if(target===S.route)return;if(S.dirty.size){history.replaceState(null,'','#'+S.route);dialog('Leave with unsaved changes?','<p>Saving drafts does not submit inputs or start an agent. Keep editing to preserve the current view.</p>',[{label:'Save draft and leave',class:'primary',run:async()=>{closeDialog();if(await saveAllDrafts())go(target);}},{label:'Discard unsaved edits',run:()=>{S.dirty.forEach(k=>delete S.drafts[k]);S.dirty.clear();S.editRevision=undefined;S.themeDraft=null;applyTheme();closeDialog();go(target);}},{label:'Keep editing',run:closeDialog}]);return;}go(target);}
function go(target){S.route=target||'overview';if(location.hash!=='#'+S.route)history.pushState(null,'','#'+S.route);closeNav();render();$('#main').focus({preventScroll:true});window.scrollTo(0,0);}
function closeNav(){$('#sidebar').classList.remove('open');const menu=$('[data-local="open-nav"]');menu?.setAttribute('aria-expanded','false');}
function openNav(){$('#sidebar').classList.add('open');$('[data-local="open-nav"]')?.setAttribute('aria-expanded','true');$('#navigation a')?.focus();}
async function readArtifact(path,id){try{const result=await api('/api/artifacts?path='+encodeURIComponent(path));S.detail={id,content:typeof result==='string'?result:result.content||result.text||pretty(result)};render();}catch(error){dialog('This artifact is unavailable',notice(error.message,'error'),[{label:'Close',run:closeDialog}]);}}
async function localAction(id,el){switch(id){case 'accept-matched':{const receipt=asArray(S.status.receipts).find(r=>r.id===el.dataset.receipt),rows=asArray(receipt?.rows||receipt?.matches||receipt?.answers||receipt?.mappings);for(const [i,row] of rows.entries()){if(row.status!=='matched'||rows.filter(r=>r.question_id===row.question_id).length!==1)continue;const key=`receipt-${receipt.id}-${i}`;markDirty(key+'-choice','accept');}render();toast('Matched answers selected for acceptance. Review and explicitly save or import them.');break;}case 'refresh':await refresh(true);break;case 'quickstart':dialog('Quick start',markdown(S.help.quick_start||'The installed quick-start document is unavailable.'),[{label:'Close',run:closeDialog}]);break;case 'open-nav':openNav();break;case 'close-dialog':closeDialog();break;case 'preview-input':S.preview=!S.preview;render();break;case 'discard-change':['change-text','change-title'].forEach(k=>{delete S.drafts[k];S.dirty.delete(k);});render();toast('Unsaved local request edits discarded. Submitted records are unchanged.');break;case 'compare-input':dialog('Saved draft and latest submission',`<div class="diff-grid"><section><h3>Saved draft</h3><pre>${esc(S.status.draft?.change?.text||S.status.draft?.change||'')}</pre></section><section><h3>Submitted input</h3><pre>${esc(active()?.submitted_input||active()?.input||'No submission recorded.')}</pre></section></div>`,[{label:'Close',run:closeDialog}]);break;case 'compare-plan':dialog('Plan revisions',`<p class="hint">Immutable preceding and current revisions preserve completed work and required rework.</p><pre>${esc(pretty(S.status.plan_history||active()?.plan_history||S.status.plan||{}))}</pre>`,[{label:'Close',run:closeDialog}]);break;case 'view-results':dialog('Results summary',artifactPanel('Recorded outcome',S.status.results_summary||active()?.results_summary||operation()?.summary),[{label:'Close',run:closeDialog}]);break;case 'view-test-contracts':dialog('Registered test-run contracts',`<pre>${esc(pretty(S.status.test_contracts||S.status.test_inventory?.suites||S.status.tests?.contracts||S.status.config?.test_suites||[]))}</pre>`,[{label:'Close',run:closeDialog}]);break;case 'toggle-log':S.logFollow=!S.logFollow;render();break;case 'read-document':await readArtifact(el.dataset.path,el.dataset.id);break;case 'documentation-form':dialog('Explicit documentation operation',`<p>Inventory and document changed registered sources. With an open request, this operation belongs to that request’s bounded documentation increment; it does not expand implementation scope.</p>${choice('documentation-mode','Inspection mode',[['incremental','Incremental — inspect affected sources'],['initial','Initial baseline'],['full','Full deliberate reinspection']],'incremental')}<p class="hint">The effective configured profile generates semantic documentation. Source files, tests, and custom instructions remain unchanged.</p>`,[{label:'Cancel',run:closeDialog},{label:'Start reverse engineering',class:'primary',run:async()=>{const mode=value('documentation-mode');closeDialog();await perform('reverse-engineer',{mode,within_request:!!active()},{raw:true});}}]);break;case 'edit-root':{const root=workspaceRoots().find(r=>r.id===el.dataset.id);if(root){for(const k of ['id','name','path','purpose','access'])S.drafts['root-'+k]=root[k]||'';render();$('#root-id')?.focus();}break;}case 'clear-root':for(const k of ['id','name','path','purpose','access']){delete S.drafts['root-'+k];S.dirty.delete('root-'+k);}render();break;case 'remove-root':{const root=workspaceRoots().find(r=>r.id===el.dataset.id);dialog('Remove folder membership',`<p>Remove ${esc(root?.name||el.dataset.id)} from the registered workspace? Files are retained. Existing test, defect, and recovery obligations remain recorded and require reassessment.</p>`,[{label:'Cancel',run:closeDialog},{label:'Remove membership',class:'danger',run:async()=>{const payload=rootPayload(el.dataset.id);closeDialog();await perform('workspace-save',payload,{raw:true});}}]);break;}case 'edit-profile':{const p=asArray(S.status.profiles||S.status.config?.profiles).find(p=>p.id===el.dataset.id);if(p){for(const [key,v] of Object.entries({id:p.id,name:p.name,adapter:p.adapter,model:p.model,timeout:p.timeout||1800,credential:(p.credential_env||[]).join(', '),executable:p.executable,permissions:p.permissions?.product_write?'workspace-write':'read-only',network:p.permissions?.network?'yes':'no',default:p.id===S.status.config?.default_profile?'yes':'no'}))S.drafts['profile-'+key]=String(v||'');render();$('#profile-id')?.focus();}break;}case 'preview-instructions':dialog('Instructions preview',markdown(value('instructions-text')),[{label:'Close',run:closeDialog}]);break;case 'select-theme':S.themeDraft={theme:el.dataset.theme,size:S.themeDraft?.size||16};markDirty('appearance',pretty(S.themeDraft));render();break;case 'system-theme':S.themeDraft={theme:'system',size:S.themeDraft?.size||16};markDirty('appearance',pretty(S.themeDraft));render();break;case 'customize-theme':{const current=S.themeDraft||{theme:'clear',size:16};S.themeDraft={theme:'custom',size:current.size,custom:JSON.parse(JSON.stringify(current.custom||themes[current.theme]||themes.clear))};S.themeDraft.custom.name='My theme';markDirty('appearance',pretty(S.themeDraft));render();break;}case 'reset-theme':S.themeDraft={theme:'custom',size:16,custom:JSON.parse(JSON.stringify(themes.clear))};markDirty('appearance',pretty(S.themeDraft));render();break;case 'delete-theme':S.themeDraft={theme:'clear',size:S.themeDraft?.size||16};markDirty('appearance',pretty(S.themeDraft));render();toast('Custom preset removed from the draft. Save appearance to use Clear.');break;case 'preview-theme':if(S.themeDraft?.theme==='custom'){S.themeDraft.custom.font=value('theme-font');S.themeDraft.custom.heading=value('theme-heading');}applyTheme(S.themeDraft);toast('Preview applied locally. Save appearance to persist it.');break;case 'discard-theme':S.themeDraft=null;['appearance','theme-name','theme-font','theme-heading'].forEach(k=>{S.dirty.delete(k);delete S.drafts[k];});applyTheme();render();break;case 'copy-command':case 'copy-handoff':{const text=id==='copy-command'?el.dataset.command:operation()?.handoff?.instructions||S.status.handoff?.instructions||'';try{await navigator.clipboard.writeText(text);toast('Copied. Nothing was executed.');}catch{dialog('Copy text',`<pre>${esc(text)}</pre>`,[{label:'Close',run:closeDialog}]);}break;}}}
document.addEventListener('click',async event=>{const target=event.target.closest('button,a');if(!target||target.disabled)return;if(target.matches('a[href^="#"]')){const href=target.getAttribute('href').slice(1);if(href==='main')return;event.preventDefault();navigate(href);return;}if(target.dataset.dialog!==undefined){await S.modalActions[Number(target.dataset.dialog)]?.run();return;}if(target.dataset.operation){const id=target.dataset.operation;if(id==='stop'&&operation()?.handoff&&['stopping','external_stop_awaiting_confirmation'].includes(operation()?.status)){externalStopDialog();return;}await perform(id,JSON.parse(target.dataset.payload||'{}'));return;}if(target.dataset.local){if(busy()&&['accept-matched','edit-root','remove-root','clear-root','edit-profile','select-theme','system-theme','customize-theme','reset-theme','delete-theme','preview-theme','discard-theme','documentation-form','discard-change'].includes(target.dataset.local)){toast('Stop is the only operational action while execution owns the slot.');return;}await localAction(target.dataset.local,target);}});
document.addEventListener('input',event=>{const el=event.target;if(el.dataset.questionAnswer){editQuestion(el.dataset.questionAnswer,'answer',el.value);return;}if(el.dataset.draft){markDirty(el.dataset.draft,el.value);return;}if(el.dataset.search){const selection=el.selectionStart;S.searches[el.dataset.search]=el.value;const id=el.id;render();const replacement=document.getElementById(id);replacement?.focus();replacement?.setSelectionRange?.(selection,selection);return;}if(el.hasAttribute('data-theme-size')&&!busy()){S.themeDraft.size=Number(el.value);markDirty('appearance',pretty(S.themeDraft));$('#appearance-size-value').textContent=el.value;return;}if(el.dataset.themeColor&&!busy()){S.themeDraft.custom.colors[el.dataset.themeColor]=el.value;markDirty('appearance',pretty(S.themeDraft));}});
document.addEventListener('change',async event=>{const el=event.target;if(el.id==='run-profile'){if(!busy())S.runProfile=el.value;return;}if(el.hasAttribute('data-help-platform')){S.helpPlatform=el.value;render();return;}if(el.dataset.questionChoice){editQuestion(el.dataset.questionChoice,'choice',el.value);return;}if(el.dataset.questionRespondent){editQuestion(el.dataset.questionRespondent,'respondent',el.value);return;}if(el.dataset.draft)markDirty(el.dataset.draft,el.value);if(el.dataset.upload){if(busy()){el.value='';toast('Uploads are unavailable while an action owns the slot.');return;}for(const file of el.files){if(file.size>1048576||!/\.(txt|md|json|csv|ya?ml)$/i.test(file.name)){toast('Choose an allowed inert text file of at most 1 MiB.');continue;}const content=await file.text();if(el.dataset.upload==='answers'){S.drafts['returned-answers']=content;markDirty('returned-answers',content);render();toast('Returned form loaded locally. Receive answers for review to persist a receipt.');}else await perform('add-attachment',{lane:el.dataset.upload==='question-attachment'?'question':'change',name:file.name,type:file.type||'text/plain',size:file.size,content},{raw:true});}}});
window.addEventListener('hashchange',()=>navigate(location.hash.slice(1)||'overview'));
window.addEventListener('popstate',()=>navigate(location.hash.slice(1)||'overview'));
window.addEventListener('beforeunload',event=>{if(S.dirty.size){event.preventDefault();event.returnValue='';}});
document.addEventListener('keydown',event=>{if(event.key==='Tab'&&$('#sidebar').classList.contains('open')){const focusables=$$('#sidebar a, #sidebar button'),first=focusables[0],last=focusables[focusables.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus();}}if(event.key==='Escape'&&$('#sidebar').classList.contains('open')){closeNav();$('[data-local="open-nav"]')?.focus();}});
$('#toast').addEventListener('click',()=>$('#toast').textContent='');
$('#mobile-scrim').addEventListener('click',()=>{closeNav();$('[data-local="open-nav"]')?.focus();});
matchMedia('(prefers-color-scheme: dark)').addEventListener('change',()=>{if(!S.themeDraft)applyTheme();});
async function start(){const results=await Promise.allSettled([api('/api/catalog'),api('/api/help')]);if(results[0].status==='fulfilled')S.catalog=asArray(results[0].value.operations||results[0].value);if(results[1].status==='fulfilled')S.help=results[1].value;await refresh(true);setInterval(()=>refresh(false),2500);}
start();
})();
