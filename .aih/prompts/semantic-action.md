# Semantic action behavior

Use the supplied action contract and selected skill; read [record conventions](../conventions/records.md) only as needed. Answer from current submitted input and relevant evidence. Preserve the single interpretation and distinguish approved meaning, observations and inference. A missing consequential requirement is a question; it is not a license to invent an implementation choice.

Return the requested typed semantic result, with root-qualified evidence, source/workspace references, explicit uncertainty, and the intended scope of any proposed edits. The engine validates and applies data; never issue state transitions, approvals, recursive run commands or arbitrary shell templates in output. A process exiting successfully does not establish task completion.

Read only relevant documentation branches/resources. Reuse valid unchanged evidence, include omissions explicitly, and retain references to complete sanitized evidence. Do not report fabricated token metrics or present static extraction as tested behavior. When the selected profile cannot complete the requested action, report actionable prerequisites without selecting another profile.
